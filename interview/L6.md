# L6 · 连接器 / CDC / 湖仓

> 共 24 题。先自答，再展开「参考答案」。链回 `docs/`/`ai/`/`projects/` 仅作延伸，不替代本题答案正文。

152. Flink CDC YAML Pipeline 相比手写 DataStream CDC 的价值？Schema Evolution？

**参考答案：** 答案：声明式源汇、转换链短，统一运维；Schema Evolution 按连接器策略（加列/宽表）配置并需下游兼容。手写 DS 灵活但成本高。见 docs/08-cdc、e08。

**机制展开（L6-152）：**
题干关键动作是「Flink CDC YAML Pipeline 相比手写 DataStream CDC 的价值？Schema Evolu…」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 152 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Flink CDC YAML Pipeline 相比手写 DataStream 」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 152 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

153. Paimon 与 Iceberg 在流式更新场景的根本差异？

**参考答案：** 答案：Paimon 面向流更新/主键表与 LSM 式更新友好；Iceberg 强项表格式生态与批分析，行级更新模型不同。选型看「流式变更频率」与湖仓工具链。见 docs/09-lakehouse。

**机制展开（L6-153）：**
题干关键动作是「Paimon 与 Iceberg 在流式更新场景的根本差异？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 153 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Paimon 与 Iceberg 在流式更新场景的根本差异」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 153 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

154. Kafka Source 起始位点与组管理注意点？

**参考答案：** 答案：committed vs earliest/latest；组 id 冲突会抢消费。作业参数外置 group-id。见 docs/07-connectors、best-practice #7。

**机制展开（L6-154）：**
题干关键动作是「Kafka Source 起始位点与组管理注意点？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 154 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Kafka Source 起始位点与组管理注意点」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 154 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

155. ClickHouse Sink 的幂等与批量？

**参考答案：** 答案：CH 常 at-least-once + 可重表/去重模型；批量与 flush 影响延迟。p03/p01 用 HTTP/JDBC SinkV2 模式按项目文档。见 e07、projects。

**机制展开（L6-155）：**
题干关键动作是「ClickHouse Sink 的幂等与批量？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 155 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「ClickHouse Sink 的幂等与批量」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 155 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

156. 连接器版本为何必须跟 Flink 版本矩阵走？

**参考答案：** 答案：API 与 shaded 依赖绑定；错版本是 NoClassDef/序列化地狱。SSOT：根 README + pom 属性。

**机制展开（L6-156）：**
题干关键动作是「连接器版本为何必须跟 Flink 版本矩阵走？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 156 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「连接器版本为何必须跟 Flink 版本矩阵走」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 156 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

157. Debezium 心跳与 WAL 膨胀？

**参考答案：** 答案：低流量表需心跳防 slot/位点停滞；忽略则 CDC 延迟飙升。见 docs/08-cdc。

**机制展开（L6-157）：**
题干关键动作是「Debezium 心跳与 WAL 膨胀？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 157 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Debezium 心跳与 WAL 膨胀」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 157 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

158. 多表 CDC 合并拓扑注意？

**参考答案：** 答案：表过滤、路由、主键冲突、schema 漂移。YAML pipeline 路由规则要测。

**机制展开（L6-158）：**
题干关键动作是「多表 CDC 合并拓扑注意？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 158 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「多表 CDC 合并拓扑注意」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 158 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

159. Kafka 分区数与并行度关系？

**参考答案：** 答案：源并行度 ≤ 分区数才有意义；过多并行度空转。

**机制展开（L6-159）：**
题干关键动作是「Kafka 分区数与并行度关系？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 159 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Kafka 分区数与并行度关系」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 159 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

160. 事务性 sink 与非事务性切换的数据风险？

**参考答案：** 答案：语义从 EOS 退到 at-least-once 会放大重复；需下游幂等并沟通业务。

**机制展开（L6-160）：**
题干关键动作是「事务性 sink 与非事务性切换的数据风险？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 160 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「事务性 sink 与非事务性切换的数据风险」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 160 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

161. 对象存储作为 sink 的小文件问题？

**参考答案：** 答案：高分区+短 checkpoint → 海量小文件。用分区合并、更长滚动、湖格式 compaction。见 docs/09-lakehouse。

**机制展开（L6-161）：**
题干关键动作是「对象存储作为 sink 的小文件问题？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 161 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「对象存储作为 sink 的小文件问题」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 161 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

162. Redis 作维表/特征的超时与降级？

**参考答案：** 答案：Lookup 超时走默认值/旁路；禁止同步客户端阻塞算子线程。见 p02 规划、best-practice #4。

**机制展开（L6-162）：**
题干关键动作是「Redis 作维表/特征的超时与降级？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 162 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Redis 作维表/特征的超时与降级」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 162 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

163. Postgres logical decoding 前提？

**参考答案：** 答案：wal_level=logical 等；本仓库 PG 镜像已开。见 docker、docs/08-cdc。

**机制展开（L6-163）：**
题干关键动作是「Postgres logical decoding 前提？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 163 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Postgres logical decoding 前提」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 163 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

164. 连接器指标哪些最有用？

**参考答案：** 答案：源 lag、checkpoint 对齐、错误计数、事务提交失败。结合值班五指标。

**机制展开（L6-164）：**
题干关键动作是「连接器指标哪些最有用？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 164 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「连接器指标哪些最有用」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 164 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

165. Schema Registry 何时引入？

**参考答案：** 答案：多消费者契约稳定时；学习工程可用 JSON+文档契约，避免未登记组件挡门禁。

**机制展开（L6-165）：**
题干关键动作是「Schema Registry 何时引入？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 165 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Schema Registry 何时引入」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 165 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

166. FileSystem sink 与 checkpoint 的可见性？

**参考答案：** 答案：pending→finished 提交与 CP 对齐；读端需理解分区提交语义。

**机制展开（L6-166）：**
题干关键动作是「FileSystem sink 与 checkpoint 的可见性？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 166 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「FileSystem sink 与 checkpoint 的可见性」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 166 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

167. Kafka 事务性读-处理-写链路？

**参考答案：** 答案：源位移进 CP + 事务 sink commit on CP；超时参数对齐。

**机制展开（L6-167）：**
题干关键动作是「Kafka 事务性读-处理-写链路？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 167 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Kafka 事务性读-处理-写链路」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 167 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

168. Iceberg 流写的提交周期？

**参考答案：** 答案：受 checkpoint 影响；过频小文件。

**机制展开（L6-168）：**
题干关键动作是「Iceberg 流写的提交周期？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 168 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Iceberg 流写的提交周期」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 168 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

169. CDC 删除事件如何保留？

**参考答案：** 答案：下游 tombstone/软删字段；湖表按格式支持 delete。

**机制展开（L6-169）：**
题干关键动作是「CDC 删除事件如何保留？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 169 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「CDC 删除事件如何保留」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 169 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

170. 连接器报序列化错误优先查什么？

**参考答案：** 答案：类型推导、时区、嵌套 ROW、版本错。

**机制展开（L6-170）：**
题干关键动作是「连接器报序列化错误优先查什么？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 170 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「连接器报序列化错误优先查什么」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 170 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

171. 多环境 endpoint 如何注入？

**参考答案：** 答案：作业参数/ENV，禁写死 jar。best-practice #7。

**机制展开（L6-171）：**
题干关键动作是「多环境 endpoint 如何注入？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 171 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「多环境 endpoint 如何注入」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 171 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

172. 湖仓主键表与 append 表如何选？

**参考答案：** 答案：需要更新/删除用主键表；纯事实流用 append。

**机制展开（L6-172）：**
题干关键动作是「湖仓主键表与 append 表如何选？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 172 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「湖仓主键表与 append 表如何选」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 172 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

173. 连接器敏感配置如何防泄漏？

**参考答案：** 答案：K8s Secret/环境变量；文档用占位符；勿提交云密钥。

**机制展开（L6-173）：**
题干关键动作是「连接器敏感配置如何防泄漏？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 173 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「连接器敏感配置如何防泄漏」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 173 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

174. HTTP Sink 重试策略？

**参考答案：** 答案：有界重试+死信；无限重试会卡反压。

**机制展开（L6-174）：**
题干关键动作是「HTTP Sink 重试策略？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 174 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「HTTP Sink 重试策略」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 174 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

175. Paimon compact 与流写并发？

**参考答案：** 答案：需理解快照隔离；生产按湖仓文档设 compact。

**机制展开（L6-175）：**
题干关键动作是「Paimon compact 与流写并发？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L6，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 175 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/07-connectors/README.md`
- `docs/01-runtime/README.md`
- `best-practice/05-backpressure.md`
- `benchmark/baseline.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Paimon compact 与流写并发」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 175 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。
