# L3 · 状态管理

> 共 30 题。先自答，再展开「参考答案」。链回 `docs/`/`ai/`/`projects/` 仅作延伸，不替代本题答案正文。

62. HashMap、RocksDB、ForSt 三种 state backend 的选型矩阵？

**参考答案：** 答案：HashMap：小状态、低延迟、全在堆，不适合 GB 级。RocksDB：大状态落盘+托管内存，增量 checkpoint，生产默认候选。ForSt：新一代/学习仓库中按版本矩阵评估的大状态选项，需按 SSOT 登记版本再启用。
判据：状态体积、TTL、checkpoint 时长、运维复杂度。见 docs/03-state。

**机制展开（L3-62）：**
题干关键动作是「HashMap、RocksDB、ForSt 三种 state backend 的选型矩阵？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 62 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「HashMap、RocksDB、ForSt 三种 state backend 的」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 62 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

63. Keyed State 与 Operator State 区别？

**参考答案：** 答案：Keyed 随 key 分区，rescale 按 key group 迁移；Operator State（如 source offset 列表）按并行实例重分配，需定义 redistribute 语义。Kafka source 位移多为 operator state。见 docs/03-state。

**机制展开（L3-63）：**
题干关键动作是「Keyed State 与 Operator State 区别？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 63 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Keyed State 与 Operator State 区别」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 63 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

64. ValueState / ListState / MapState 选型？

**参考答案：** 答案：单值用 Value；追加元素用 List（注意长度）；大量子 key 用 MapState 避免大 List 每次反序列化。热点 key 的 MapState 仍可能成为瓶颈。

**机制展开（L3-64）：**
题干关键动作是「ValueState / ListState / MapState 选型？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 64 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「ValueState / ListState / MapState 选型」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 64 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

65. 状态 TTL 的语义？过期后读取返回什么？

**参考答案：** 答案：可为状态条目设置 TTL，过期后访问视作不存在并清理（懒/后台策略依配置）。错误 TTL 会「丢维表/丢会话」。SQL 用 `table.exec.state.ttl`。见 docs/03-state、docs/05-sql。

**机制展开（L3-65）：**
题干关键动作是「状态 TTL 的语义？过期后读取返回什么？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 65 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「状态 TTL 的语义」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 65 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

66. 为什么说「状态是流计算的本质」？

**参考答案：** 答案：无状态 map 可重放；聚合、去重、join、CEP 都依赖跨事件记忆。容错即「把状态与输入位置一致快照」。见 docs/00-landscape。

**机制展开（L3-66）：**
题干关键动作是「为什么说「状态是流计算的本质」？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 66 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「为什么说「状态是流计算的本质」」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 66 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

67. Broadcast State 的读写约束？

**参考答案：** 答案：广播流写入 BroadcastState；主流只读。用于动态规则（p03 Pattern 切换）。规则集需有界，否则 TM 内存爆。见 docs/10-cep、p03。

**机制展开（L3-67）：**
题干关键动作是「Broadcast State 的读写约束？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 67 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Broadcast State 的读写约束」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 67 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

68. Queryable State 为何本仓库不作为主路径？

**参考答案：** 答案：运维复杂、一致性语义易误解；学习工程优先把结果写入 Kafka/CH 供查询。见架构约束。

**机制展开（L3-68）：**
题干关键动作是「Queryable State 为何本仓库不作为主路径？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 68 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Queryable State 为何本仓库不作为主路径」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 68 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

69. 状态序列化用 Kryo 的风险？

**参考答案：** 答案：跨版本不稳定、调试难。优先 TypeInformation / POJO / Avro 等明确 schema。升级作业要验证 serializer 兼容。

**机制展开（L3-69）：**
题干关键动作是「状态序列化用 Kryo 的风险？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 69 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「状态序列化用 Kryo 的风险」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 69 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

70. RocksDB 内存相关参数调优入口？

**参考答案：** 答案：managed memory fraction、block cache、write buffer 等；先保证 managed memory 足够，再看 checkpoint 时长与 thrashing。见 docs/03-state。

**机制展开（L3-70）：**
题干关键动作是「RocksDB 内存相关参数调优入口？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 70 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「RocksDB 内存相关参数调优入口」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 70 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

71. 什么是 key group？和并行度上限关系？

**参考答案：** 答案：key 空间分成固定 key group；并行实例各持有一组。maxParallelism 决定组数，从而限制可扩最大并行度。

**机制展开（L3-71）：**
题干关键动作是「什么是 key group？和并行度上限关系？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 71 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「什么是 key group」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 71 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

72. 本地状态与 checkpoint 存储的区别？

**参考答案：** 答案：本地（堆/RocksDB 目录）供运行读写；checkpoint 是持久化快照到远程（本仓库 MinIO/S3 兼容）。丢失本地盘可从 checkpoint 拉起。

**机制展开（L3-72）：**
题干关键动作是「本地状态与 checkpoint 存储的区别？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 72 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「本地状态与 checkpoint 存储的区别」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 72 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

73. 状态倾斜如何表现？

**参考答案：** 答案：少数 key 状态巨大/访问极热，单 subtask RocksDB 变慢、checkpoint 被拖长。解法：拆 key、局部聚合、两阶段、业务限流。见 best-practice、docs/13。

**机制展开（L3-73）：**
题干关键动作是「状态倾斜如何表现？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 73 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「状态倾斜如何表现」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 73 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

74. Heap 状态 + 大窗口的致命组合？

**参考答案：** 答案：窗口缓存元素在堆上易 OOM；应增量聚合或 RocksDB + 聚合函数。

**机制展开（L3-74）：**
题干关键动作是「Heap 状态 + 大窗口的致命组合？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 74 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Heap 状态 + 大窗口的致命组合」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 74 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

75. MapState.clear() 与逐 key 删除的成本？

**参考答案：** 答案：整表 clear 语义清晰；大量逐条删除可能放大 RocksDB compaction。TTL 批量过期更可取。

**机制展开（L3-75）：**
题干关键动作是「MapState.clear() 与逐 key 删除的成本？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 75 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「MapState.clear() 与逐 key 删除的成本」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 75 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

76. State Descriptor 命名冲突会怎样？

**参考答案：** 答案：同算子内名字必须唯一；uid 变化会导致「找不到状态」走新状态。这是强制 `.uid()` 的原因之一。见 docs/04-checkpoint。

**机制展开（L3-76）：**
题干关键动作是「State Descriptor 命名冲突会怎样？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 76 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「State Descriptor 命名冲突会怎样」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 76 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

77. UnionListState 用于什么？

**参考答案：** 答案：Operator state 在 rescale 时把列表 union 到各实例再自行拆分——适合 source 拆分列表。

**机制展开（L3-77）：**
题干关键动作是「UnionListState 用于什么？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 77 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「UnionListState 用于什么」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 77 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

78. CheckpointedFunction 接口何时实现？

**参考答案：** 答案：需要在 snapshotState/initializeState 管理 operator state 时（自定义 source/sink）。普通 Keyed 业务少直接实现。

**机制展开（L3-78）：**
题干关键动作是「CheckpointedFunction 接口何时实现？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 78 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「CheckpointedFunction 接口何时实现」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 78 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

79. 状态迁移跨版本的检查清单？

**参考答案：** 答案：uid 稳定、serializer 兼容、maxParallelism 不变或可迁移、savepoint 类型匹配、先在 staging 从 savepoint 启动验证。见 production bluegreen SOP。

**机制展开（L3-79）：**
题干关键动作是「状态迁移跨版本的检查清单？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 79 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「状态迁移跨版本的检查清单」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 79 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

80. TTL 与窗口状态如何协调？

**参考答案：** 答案：窗口有自己的清理；TTL 用于任意 keyed state。勿对仍需迟到更新的窗口状态设过短 TTL。

**机制展开（L3-80）：**
题干关键动作是「TTL 与窗口状态如何协调？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 80 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「TTL 与窗口状态如何协调」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 80 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

81. 如何观测状态大小？

**参考答案：** 答案：checkpoint size 指标、RocksDB native 度量、业务侧估计 key 基数×每 key 字节。Grafana job-deepdive 含 lastCheckpointSize。见 monitoring。

**机制展开（L3-81）：**
题干关键动作是「如何观测状态大小？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 81 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「如何观测状态大小」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 81 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

82. RocksDB 目录放空写层容器的后果？

**参考答案：** 答案：重启丢本地态，完全依赖远程 CP，恢复变慢。

**机制展开（L3-82）：**
题干关键动作是「RocksDB 目录放空写层容器的后果？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 82 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「RocksDB 目录放空写层容器的后果」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 82 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

83. 状态描述符更换类型后能否兼容？

**参考答案：** 答案：通常不能；需迁移作业或弃状态。

**机制展开（L3-83）：**
题干关键动作是「状态描述符更换类型后能否兼容？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 83 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「状态描述符更换类型后能否兼容」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 83 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

84. ListState 无限追加的事故模式？

**参考答案：** 答案：每事件 add 不清理→OOM；应窗口聚合或 TTL。

**机制展开（L3-84）：**
题干关键动作是「ListState 无限追加的事故模式？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 84 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「ListState 无限追加的事故模式」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 84 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

85. Broadcast 规则版本字段如何设计？

**参考答案：** 答案：单调 version，实例忽略旧版；与 p03 pattern_id 类似。

**机制展开（L3-85）：**
题干关键动作是「Broadcast 规则版本字段如何设计？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 85 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Broadcast 规则版本字段如何设计」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 85 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

86. KeyedProcessFunction 里 timer 服务语义？

**参考答案：** 答案：事件时间定时器随 watermark 触发；处理时间随墙钟。

**机制展开（L3-86）：**
题干关键动作是「KeyedProcessFunction 里 timer 服务语义？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 86 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「KeyedProcessFunction 里 timer 服务语义」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 86 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

87. 状态后端切换是否要重新积累状态？

**参考答案：** 答案：通常需兼容迁移路径；随意切换等同放弃旧状态。

**机制展开（L3-87）：**
题干关键动作是「状态后端切换是否要重新积累状态？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 87 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「状态后端切换是否要重新积累状态」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 87 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

88. 大 MapState 迭代的性能陷阱？

**参考答案：** 答案：全量 entries() 扫描昂贵；尽量点查与 TTL。

**机制展开（L3-88）：**
题干关键动作是「大 MapState 迭代的性能陷阱？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 88 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「大 MapState 迭代的性能陷阱」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 88 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

89. OperatorState 重分布失败的表现？

**参考答案：** 答案：源拆分错乱、重复或空洞消费；需测 rescale。

**机制展开（L3-89）：**
题干关键动作是「OperatorState 重分布失败的表现？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 89 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「OperatorState 重分布失败的表现」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 89 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

90. 状态访问线程模型？

**参考答案：** 答案：单 subtask 单线程处理，状态无并发；Async 回调需注意。

**机制展开（L3-90）：**
题干关键动作是「状态访问线程模型？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 90 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「状态访问线程模型」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 90 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

91. 状态迁移时并行度从 2→3 的关键？

**参考答案：** 答案：key group 重分配；maxParallelism 必须允许。

**机制展开（L3-91）：**
题干关键动作是「状态迁移时并行度从 2→3 的关键？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L3，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 91 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/03-state/README.md`
- `examples/e03-state/README.md`
- `best-practice/04-state-ttl.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「状态迁移时并行度从 2→3 的关键」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 91 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。
