# L5 · Table/SQL

> 共 29 题。先自答，再展开「参考答案」。链回 `docs/`/`ai/`/`projects/` 仅作延伸，不替代本题答案正文。

123. 无窗口 GROUP BY 的 SQL 为什么产生回撤流？下游 upsert-kafka 与普通 kafka 怎么落地？

**参考答案：** 答案：更新聚合需要撤回旧值再发新值（retract/upsert 语义）。upsert-kafka 以主键覆盖；普通 append-only kafka 不能表达撤回，需 changelog 序列化或换 sink。见 docs/05-sql。

**机制展开（L5-123）：**
题干关键动作是「无窗口 GROUP BY 的 SQL 为什么产生回撤流？下游 upsert-kafka 与普通 kafka 怎么落地？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 123 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「无窗口 GROUP BY 的 SQL 为什么产生回撤流」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 123 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

124. Regular / Interval / Temporal Join 状态规模分别由什么决定？

**参考答案：** 答案：Regular：双流历史无界，状态易爆。Interval：由时间窗口界限制。Temporal：维表版本/Lookup 缓存策略决定。见 docs/05-sql。

**机制展开（L5-124）：**
题干关键动作是「Regular / Interval / Temporal Join 状态规模分别由什么决定？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 124 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Regular / Interval / Temporal Join 状态规模分」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 124 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

125. COUNT(DISTINCT) 在流上为何昂贵？三种降本？

**参考答案：** 答案：需记住见过的值。降本：HLL 草图、预聚合下推 OLAP、放大窗口粒度/抽样。精确去重必须论证状态上界。见 best-practice #5。

**机制展开（L5-125）：**
题干关键动作是「COUNT(DISTINCT) 在流上为何昂贵？三种降本？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 125 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「COUNT(DISTINCT) 在流上为何昂贵」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 125 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

126. SQL 作业怎么设置状态 TTL？对正确性影响？

**参考答案：** 答案：`table.exec.state.ttl`。TTL 过短→维表/去重「忘却」导致重复或错连；过长→状态膨胀。必须业务论证。见 docs/05-sql。

**机制展开（L5-126）：**
题干关键动作是「SQL 作业怎么设置状态 TTL？对正确性影响？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 126 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「SQL 作业怎么设置状态 TTL」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 126 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

127. Materialized Table 解决什么问题？与定时批差异？

**参考答案：** 答案：声明式物化与持续刷新，缩短「批调度脚本」与「流结果表」鸿沟；本质仍是引擎维护新鲜度，而非 crontab 盲跑。见 docs/05-sql。

**机制展开（L5-127）：**
题干关键动作是「Materialized Table 解决什么问题？与定时批差异？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 127 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Materialized Table 解决什么问题」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 127 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

128. Changelog 模式 ALL / UPSERT 对 sink 约束？

**参考答案：** 答案：ALL 含 +/-；UPSERT 靠主键。Sink 必须能消化对应语义，否则结果错。见 docs/05-sql。

**机制展开（L5-128）：**
题干关键动作是「Changelog 模式 ALL / UPSERT 对 sink 约束？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 128 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Changelog 模式 ALL / UPSERT 对 sink 约束」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 128 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

129. Lookup Join 缓存过期策略怎么选？

**参考答案：** 答案：维表变更频率 vs 命中率。过短打爆 DB；过长读到旧维表。监控旁路延迟。见 docs/07-connectors。

**机制展开（L5-129）：**
题干关键动作是「Lookup Join 缓存过期策略怎么选？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 129 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Lookup Join 缓存过期策略怎么选」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 129 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

130. Flink SQL 与 DataStream 混用的边界？

**参考答案：** 答案：toChangelogStream/fromDataStream 需对齐类型与时区；uid 与并行度仍要管。适合 SQL 做管道、DS 做特殊算子。

**机制展开（L5-130）：**
题干关键动作是「Flink SQL 与 DataStream 混用的边界？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 130 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Flink SQL 与 DataStream 混用的边界」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 130 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

131. MiniBatch 聚合的收益与风险？

**参考答案：** 答案：降状态访问频率提吞吐；增大延迟、语义需理解。适合高基数组。见 docs/05-sql。

**机制展开（L5-131）：**
题干关键动作是「MiniBatch 聚合的收益与风险？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 131 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「MiniBatch 聚合的收益与风险」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 131 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

132. Over 窗口与 Group 窗口差异？

**参考答案：** 答案：Over 按行帧计算，状态按 key+帧；Group 是窗口算子。注意 SQL 写法对应的状态含义。

**机制展开（L5-132）：**
题干关键动作是「Over 窗口与 Group 窗口差异？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 132 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Over 窗口与 Group 窗口差异」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 132 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

133. Watermark 在 SQL 里如何定义？

**参考答案：** 答案：DDL WATERMARK FOR col AS expr。错误表达式=错误触发。见 docs/05-sql。

**机制展开（L5-133）：**
题干关键动作是「Watermark 在 SQL 里如何定义？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 133 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Watermark 在 SQL 里如何定义」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 133 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

134. 主键语义缺失会导致什么？

**参考答案：** 答案：upsert sink 拒写或结果重复；回撤无法对齐。建模先定主键。

**机制展开（L5-134）：**
题干关键动作是「主键语义缺失会导致什么？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 134 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「主键语义缺失会导致什么」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 134 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

135. 时区在 SQL 窗口的坑？

**参考答案：** 答案：TIMESTAMP_LTZ vs TIMESTAMP；窗口边界按本地日切需显式时区，否则「天窗」错位。

**机制展开（L5-135）：**
题干关键动作是「时区在 SQL 窗口的坑？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 135 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「时区在 SQL 窗口的坑」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 135 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

136. SQL 作业如何做单元级验证（本仓库）？

**参考答案：** 答案：案例 e0x + 项目 verify 脚本对照 CH/Kafka；禁止只 explain 不跑。见 examples、projects。

**机制展开（L5-136）：**
题干关键动作是「SQL 作业如何做单元级验证（本仓库）？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 136 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「SQL 作业如何做单元级验证（本仓库）」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 136 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

137. TopN 流式实现要点？

**参考答案：** 答案：追加/回撤更新排名；状态按分区键+排序键；注意结果回撤风暴。见 docs/05-sql。

**机制展开（L5-137）：**
题干关键动作是「TopN 流式实现要点？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 137 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「TopN 流式实现要点」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 137 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

138. CDC 入 SQL 表后如何保证主键更新？

**参考答案：** 答案：源表正确 PRIMARY KEY、changelog 解释正确、下游 upsert 支持。见 docs/08-cdc。

**机制展开（L5-138）：**
题干关键动作是「CDC 入 SQL 表后如何保证主键更新？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 138 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「CDC 入 SQL 表后如何保证主键更新」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 138 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

139. Plan 解释器里能看到什么排障信息？

**参考答案：** 答案：算子链、changelog mode、upsert key。用于确认是否意外 retract。

**机制展开（L5-139）：**
题干关键动作是「Plan 解释器里能看到什么排障信息？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 139 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Plan 解释器里能看到什么排障信息」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 139 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

140. SQL Client 与 jar 提交差异？

**参考答案：** 答案：Client 适合交互；生产用 jar/SQL gateway/Operator 声明。本仓库以 jar+Makefile 为主。

**机制展开（L5-140）：**
题干关键动作是「SQL Client 与 jar 提交差异？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 140 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「SQL Client 与 jar 提交差异」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 140 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

141. 双流 join 超时清理参数？

**参考答案：** 答案：idle state retention / TTL / interval 界。不设则状态无限涨。

**机制展开（L5-141）：**
题干关键动作是「双流 join 超时清理参数？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 141 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「双流 join 超时清理参数」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 141 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

142. 为什么「先 SQL 建模再下推 DS」是好路径？

**参考答案：** 答案：先保证关系语义可测，再对瓶颈算子定制；反之过早微优化难回归。

**机制展开（L5-142）：**
题干关键动作是「为什么「先 SQL 建模再下推 DS」是好路径？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 142 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「为什么「先 SQL 建模再下推 DS」是好路径」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 142 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

143. DDL 与 DML 分离的好处？

**参考答案：** 答案：环境差异只换 DDL 连接属性；DML 可复用。

**机制展开（L5-143）：**
题干关键动作是「DDL 与 DML 分离的好处？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 143 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「DDL 与 DML 分离的好处」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 143 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

144. 流式 SQL 测试如何固定时间？

**参考答案：** 答案：测试源注入时间戳+watermark；或有界回放。

**机制展开（L5-144）：**
题干关键动作是「流式 SQL 测试如何固定时间？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 144 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「流式 SQL 测试如何固定时间」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 144 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

145. upsert 主键变更 equates 删旧插新？

**参考答案：** 答案：语义上要撤回旧主键行；建模避免随意改主键。

**机制展开（L5-145）：**
题干关键动作是「upsert 主键变更 equates 删旧插新？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 145 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「upsert 主键变更 equates 删旧插新」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 145 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

146. Group Window TVF 与旧 Window 语法？

**参考答案：** 答案：新 TVF 更清晰；学习跟 2.2 文档与本仓库示例。

**机制展开（L5-146）：**
题干关键动作是「Group Window TVF 与旧 Window 语法？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 146 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Group Window TVF 与旧 Window 语法」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 146 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

147. SQL 反压时如何看算子名？

**参考答案：** 答案：打开 Flame/Metric 中 operator_name；与 explain 对照。

**机制展开（L5-147）：**
题干关键动作是「SQL 反压时如何看算子名？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 147 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「SQL 反压时如何看算子名」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 147 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

148. 回撤风暴的业务症状？

**参考答案：** 答案：下游更新频繁、Kafka 流量倍增；优化聚合或放大窗口。

**机制展开（L5-148）：**
题干关键动作是「回撤风暴的业务症状？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 148 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「回撤风暴的业务症状」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 148 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

149. SQL 中 PROCTIME() 用途？

**参考答案：** 答案：处理时间维表 lookup；与事件时间不同。

**机制展开（L5-149）：**
题干关键动作是「SQL 中 PROCTIME() 用途？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 149 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「SQL 中 PROCTIME() 用途」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 149 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

150. 动态表与 changelog 的关系？

**参考答案：** 答案：动态表持续更新；changelog 是其消息编码。

**机制展开（L5-150）：**
题干关键动作是「动态表与 changelog 的关系？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 150 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「动态表与 changelog 的关系」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 150 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。

151. Interval join 两边时间属性？

**参考答案：** 答案：都需事件时间+watermark，否则无法界定。

**机制展开（L5-151）：**
题干关键动作是「Interval join 两边时间属性？」。从控制面（调度/checkpoint/配置）与数据面（记录流/状态）拆解：先给定义，再用「若缺了该机制会怎样」的反事实，最后落到仓库路径。
模块接口：即使本题落在 L5，也要能指出 `docs/README.md` 中上下游编号，说明本题结论如何约束相邻模块（时间/状态/容错/连接器/AI）。

**反例：**
对第 151 题只背清单不讲失败模式；或引用仓库中不存在的类名；或把规划稿当生产证据。应指向可运行 `examples/` 与 `projects/*/docs/baseline.md`。

**仓库对照：**
- `docs/05-sql/README.md`
- `docs/06-table-api/README.md`
- `examples/e05-sql/README.md`
- `examples/e06-table-api/README.md`
- 门禁与计量：`scripts/qa_check.sh`、`scripts/count_docs.py`、`scripts/count_interview.py`

**口述结构（约 90 秒）：**
① 定义（扣题「Interval join 两边时间属性」）；② 机制两步；③ 反例；④ 仓库路径；⑤ 指标或验证动作。禁止复读题干。

**延伸思考：**
若落入 p01/p02/p03，第 151 题结论如何约束 SLA、降级与成本？笔记中写清，并确保不含违禁词。
