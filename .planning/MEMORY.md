# Project Memory · flink-learning-lab

> 跨会话项目记忆（决策、事故、禁令）。新 agent 在改文档/QA 门禁前应先读本文件。

## 2026-07-19 · 撤销文档 ≥30000 行硬指标 + Wave 2 注水回退

### 背景

Phase 7（P6 总装 QA）CONTEXT **D-04/D-06** 曾锁定「非 `.planning` `*.md` ≥30000」为 `qa_check` 硬门禁。07-02 Wave 2 为达成该数字，在下列提交中注入大量同构重复块（编号循环的「实质扩写 / Wave 2 / 机制展开 / 情景 / 检查项」模板），导致文档可读性与专业性严重下降：

| Commit | 说明 | 约增量 |
|--------|------|--------|
| `5b427d4` | docs/ 模块「实质扩写（Wave 2）」 | +7723 |
| `0435b7b` | interview L1–L8 每题模板脚手架 | +4580 |
| `3cc6882` | ai/best-practice/examples/production 等刷行 | +~7.6k |
| `c75c72c` / `d480eb4` | 规划态 SUMMARY/STATE 记录「30620≥30000」 | 元数据 |

审计信号：约 100 个 md 文件含重复组；归一化后重复行 ≈3500+；总增量 ≈+19938 行相对扩写前。CONTEXT **D-05** 本已写明「实质扩写，禁止注水」，但行数硬门禁与 D-05 激励冲突，执行期选择刷数。

### 整改动作（已落地）

1. **回退内容**：上述三批内容提交涉及的全部 `*.md` 恢复至各自父提交（扩写前）实质正文。
2. **拆除硬门**：`scripts/qa_check.sh` 移除「文档行数 ≥30000」硬失败；行数仅 `info` 诊断。
3. **诊断脚本**：`scripts/count_docs.py` 不再因低于阈值 `exit 1`。
4. **ENG-04**：`scripts/eng_audit.sh` 不再要求 PHASES 文案含 `30000|30k`。
5. **口径同步**：`PHASES.md` / 根 `README.md` / `CHANGELOG.md` / `docs/QA-REPORT.md` / `scripts/README.md` / REQUIREMENTS·PROJECT·CONTEXT 增补决定。

### 冻结决策（取代 D-04/D-06 硬门部分）

- **D-14（2026-07-19）：** 文档完成态 **不以行数阈值验收**。`count_docs.py` / qa_check 可打印行数供体量感知，但 **禁止** 将最小值写入硬门禁或激励刷数。
- **D-15（2026-07-19）：** 文档扩写必须增加可验证信息密度（命令、路径、失败模式、与 examples/projects 交叉引用）。禁止编号循环同句、禁止每题粘贴同一套「反例/仓库对照/口述结构」脚手架、禁止「深潜 N / 情景 N / 卡片 N」空壳段。
- **D-16（2026-07-19）：** QA-02 文档轴改为：**违禁词清零 + 相对断链清零 + 关键路径八段式完整 + 与 DEMO/项目互链可点开**；案例轴仍锁定 mains≥100（D-01/D-03 不变）。

### 复发信号（再出现即 FAIL 评审）

- 提交说明含「抬升至 ≥N 行」「补齐行数缺口」「刷 md」
- 多文件新增段落仅数字序号不同、正文相同
- interview 每题后出现相同「机制展开 / 口述结构 / 延伸思考」块且无题目特有机制

### 相关路径

- Quick task：`.planning/quick/260719-1ou-qa-3-5b427d4-0435b7b-3cc6882-c75c72c-d48/`
- Phase CONTEXT：`.planning/phases/07-p6-qa/07-CONTEXT.md`（见 D-14…D-16 补丁段）
