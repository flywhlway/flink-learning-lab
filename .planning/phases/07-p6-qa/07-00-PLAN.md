---
phase: 07-p6-qa
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/qa_check.sh
  - scripts/eng_audit.sh
  - scripts/count_docs.py
  - scripts/README.md
  - examples/e12-06-streaming-feature/README.md
  - examples/e12-06-streaming-feature/src/main/java/com/flywhl/flinklab/e12/StreamingFeatureJob.java
  - examples/e12-07-agent-quickstart/README.md
autonomous: true
requirements:
  - QA-01
  - ENG-01
  - ENG-02
  - ENG-03
  - ENG-04
user_setup: []

must_haves:
  truths:
    - "维护者运行 bash scripts/qa_check.sh 时，案例阈值按 ≥100、文档行数按 ≥30000、mvn compile 硬失败、违禁词含「省略」；当前仓库预期 FAIL（故意先红，per D-13）"
    - "bash scripts/eng_audit.sh 可独立执行并覆盖 ENG-01…04 断言骨架；qa_check 末尾调用之（per D-10 Discretion 独立文件）"
    - "存量「省略」三处已改写，升级词表后 ② 段不因教学说明句误杀（per D-08 / Pitfall 1）"
  artifacts:
    - path: "scripts/qa_check.sh"
      provides: "六硬门 QA-01（compose/违禁词/断链/mains≥100/md≥30000/mvn hard fail）"
      contains: "30000|省略|ge 100"
    - path: "scripts/eng_audit.sh"
      provides: "ENG-01…04 可脚本化终检（D-10）"
      contains: "ENG-01|ENG-02|ENG-03|ENG-04"
    - path: "scripts/count_docs.py"
      provides: "分目录文档行数诊断（D-04 口径，可选门禁）"
      contains: "30000|.planning"
    - path: "scripts/README.md"
      provides: "脚本索引更新"
      contains: "eng_audit|qa_check|30000"
  key_links:
    - from: "scripts/qa_check.sh"
      to: "scripts/eng_audit.sh"
      via: "末尾调用或 FAIL 汇合"
      pattern: "eng_audit"
    - from: "scripts/qa_check.sh"
      to: "examples/**/*.java mains + *.md excl .planning"
      via: "④⑤ 计量口径 D-01/D-04"
      pattern: "public static void main|30000"
    - from: "scripts/eng_audit.sh"
      to: "README.md + examples/pom.xml"
      via: "ENG-01 版本抽样"
      pattern: "2\\.2\\.1|5\\.0\\.0-2\\.2"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 让 `qa_check` 六硬门与 ENG 终检可脚本化、案例≥100、文档≥30k、状态终稿一致，**so that** 里程碑达到可打 tag 的完成态（git tag 本身留给 `/gsd-complete-milestone`，per D-12）。

<objective>
Wave 0 门禁先红（D-13 步 1）：升级 `qa_check.sh` 为六硬门、交付独立 `eng_audit.sh` 骨架、清洗存量「省略」，使当前仓故意 FAIL。

Purpose: 建立诚实红灯反馈环；后续 Wave 用内容扩容把门禁推绿，禁止先改阈值再凑数假绿（T-07-01）。
Output: 升级后的 qa_check + eng_audit + count_docs + scripts/README；e12-06/07 「省略」改写；本波结束 qa_check exit≠0（案例/行数不足）。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-p6-qa/07-CONTEXT.md
@.planning/phases/07-p6-qa/07-RESEARCH.md
@.planning/phases/07-p6-qa/07-PATTERNS.md
@.planning/phases/07-p6-qa/07-VALIDATION.md
@scripts/qa_check.sh
@scripts/count_interview.py
@scripts/README.md
@README.md
@examples/pom.xml
@docs/README.md
@CHANGELOG.md
@PHASES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 清洗存量「省略」并升级 qa_check 六硬门（故意先红）</name>
  <files>scripts/qa_check.sh, examples/e12-06-streaming-feature/README.md, examples/e12-06-streaming-feature/src/main/java/com/flywhl/flinklab/e12/StreamingFeatureJob.java, examples/e12-07-agent-quickstart/README.md</files>
  <read_first>
    - .planning/phases/07-p6-qa/07-CONTEXT.md（D-03/D-06/D-07/D-08/D-13）
    - .planning/phases/07-p6-qa/07-RESEARCH.md（qa_check 升级映射表；Pitfall 1「省略」误杀）
    - .planning/phases/07-p6-qa/07-PATTERNS.md（qa_check ①–⑥ 段模板）
    - scripts/qa_check.sh（现状五段）
    - examples/e12-06-streaming-feature/README.md
    - examples/e12-06-streaming-feature/src/main/java/com/flywhl/flinklab/e12/StreamingFeatureJob.java
    - examples/e12-07-agent-quickstart/README.md
  </read_first>
  <action>
    同一交付内先改写再升词表（per D-08 / RESEARCH Pitfall 1）：
    1) 将 e12-06 README/Java、e12-07 README 中的「省略」改为等价表述（如「未包含」「必须保留」「简化掉」），语义不变；禁止扩大违禁词排除列表到 examples。
    2) 升级 `scripts/qa_check.sh` 头注释为六硬门；保持 `set -uo pipefail` 与 note/bad/ok。
    3) ① compose：保持现有 docker compose config / PyYAML 硬失败。
    4) ② 违禁词：正则改为 `TODO|FIXME|自行实现|请参考官网|省略`；禁止裸匹配「略」；排除集冻结为 D-08 列表（`.planning/`、`PHASES.md`、`CLAUDE.md`、`AGENTS.md`、`scripts/qa_check.sh`、`scripts/README.md`、`docs/README.md`），禁止加宽排除绕过门禁（T-07-01）。
    5) ③ 断链：保持相对路径存在性检查；仅检查路径存在、不读文件内容（T-07-03）。
    6) ④ 案例：阈值 67→100（D-03）；诊断行打印实际 mains；口径仅 `examples/` 下 `public static void main`（D-01）。
    7) ⑤ 新增文档行数：`find`+`wc`（或调用 count_docs），排除 `.planning/` 与 `.git/`（D-04/D-06）；阈值 ≥30000；打印实际行数。
    8) ⑥ mvn：无 mvn → bad；compile 失败 → bad；删除一切 warn 跳过路径（D-07）。
    9) 本任务结束不要求绿：预期因 mains&lt;100 与 md&lt;30000 而 FAIL（D-13 先红）。
  </action>
  <verify>
    <automated>bash -n scripts/qa_check.sh &amp;&amp; grep -E '省略|30000|ge 100' scripts/qa_check.sh | grep -v '^#' | head -20; (! grep -n '省略' examples/e12-06-streaming-feature/README.md examples/e12-06-streaming-feature/src/main/java/com/flywhl/flinklab/e12/StreamingFeatureJob.java examples/e12-07-agent-quickstart/README.md); bash scripts/qa_check.sh; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - qa_check 含六硬门逻辑；词表含「省略」且无裸「略」
    - 三处存量「省略」已清；② 段不因这三文件失败
    - 脚本 exit≠0（案例或文档未达标的故意红）
  </acceptance_criteria>
  <done>qa_check 升级完成且对本仓故意红；省略误杀点已清</done>
</task>

<task type="auto">
  <name>Task 2: 新建 eng_audit.sh + count_docs.py 并挂入索引</name>
  <files>scripts/eng_audit.sh, scripts/count_docs.py, scripts/README.md, scripts/qa_check.sh</files>
  <read_first>
    - .planning/phases/07-p6-qa/07-CONTEXT.md（D-10/D-11/D-12）
    - .planning/phases/07-p6-qa/07-RESEARCH.md（ENG 审计设计表；Wave 1 soft / Wave 4 hard for PHASES）
    - .planning/phases/07-p6-qa/07-PATTERNS.md（eng_audit / count_docs 模式）
    - scripts/qa_check.sh（Task 1 升级后）
    - scripts/count_interview.py（exit 码与诊断样板）
    - README.md（版本矩阵）
    - examples/pom.xml（properties）
    - docs/README.md（编号 SSOT）
    - CHANGELOG.md（Unreleased）
    - PHASES.md（P6 行）
  </read_first>
  <action>
    Discretion 默认：独立 `scripts/eng_audit.sh`，由 qa_check 末尾调用（per RESEARCH Q1）：
    1) `eng_audit.sh`：复用 note/bad/ok；失败非 0。
    2) ENG-01：从根 README 版本矩阵抽样关键版本（至少 `2.2.1`、`5.0.0-2.2`、`3.6.0`、`0.3.0`）断言出现在 `examples/pom.xml` `<properties>`；README-only 白名单允许 Operator Helm chart / Milvus / Ollama（不要求进 pom）。
    3) ENG-02：`docs/README.md` 存在；`docs/00`–`docs/14` 相关模块目录存在；模块 15 三项目 README 相对链接可解析。
    4) ENG-03：硬存在证据指针——`projects/p01-log-ai-platform/docs/baseline.md`、`projects/p02-realtime-reco/docs/baseline.md`、`projects/p03-vehicle-monitoring/docs/baseline.md`、`benchmark/baseline.md`、`production/docs/bluegreen-timeline.md`；违禁词可依赖 qa_check ② 或复用同词表子集。
    5) ENG-04 Wave 0：仅断言 `CHANGELOG.md` 含未发布/Unreleased 区；**不要**在本波强制 PHASES P6 已是 ✅（避免先红死锁；严格 PHASES 断言留给 07-03，per RESEARCH）。
    6) 新建 `scripts/count_docs.py`：排除 `.planning/`与`.git/`；打印分目录行数；`total &lt; 30000` 时 exit 1（诊断用；硬门仍以 qa_check ⑤ 为准）。
    7) 更新 `scripts/README.md` 索引行：六硬门 qa_check、eng_audit、count_docs、count_interview。
    8) qa_check 尾部：`bash scripts/eng_audit.sh || bad "eng_audit"`，再统一 PASS/FAIL。
    9) 禁止本 Phase 打 git tag（D-12）。
  </action>
  <verify>
    <automated>bash -n scripts/eng_audit.sh &amp;&amp; python3 -m py_compile scripts/count_docs.py &amp;&amp; bash scripts/eng_audit.sh; echo eng_exit=$?; python3 scripts/count_docs.py; echo docs_exit=$?; grep -E 'eng_audit|count_docs|30000' scripts/README.md</automated>
  </verify>
  <acceptance_criteria>
    - eng_audit.sh 可独立跑；覆盖 ENG-01…04（ENG-04 对本波仅 CHANGELOG）
    - count_docs.py 可跑并打印分目录；当前应 exit 1（行数不足）
    - qa_check 调用 eng_audit；scripts/README 已登记
  </acceptance_criteria>
  <done>ENG 终检骨架与文档计数诊断就位；门禁入口单一</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 开发者工作区 → qa_check/eng_audit | 本地/CI 执行门禁；脚本输出决定合入可否 |
| Markdown 链接路径 → 文件系统 | 断链检查拼接 `dirname(f)/tgt`；不可读任意敏感内容 |
| 计量口径 → 完成态声明 | mains/行数数字驱动 QA-02；可被虚增游戏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | qa_check 排除列表 / warn 跳过 | mitigate | 排除集冻结 D-08；删除 compile warn；Wave 0 故意红禁止假绿 |
| T-07-02 | Tampering | 案例/行数计量 | mitigate | D-01/D-04 口径硬编码；禁止 .planning 计入；禁止 README C 标签充数 |
| T-07-03 | Information Disclosure | 断链扫描路径 | mitigate | 仅 `-e` 存在性；不 `cat` 目标文件 |
| T-07-04 | Tampering | eng_audit 版本抽样 | mitigate | 抽样固定关键版本串；README-only 白名单显式列出 |
| T-07-SC | Tampering | 包安装 | accept | 本 Phase 不新增 npm/pip/cargo 包（RESEARCH Package Audit） |
</threat_model>

<verification>
- `bash scripts/qa_check.sh` exit≠0（先红）
- `bash scripts/eng_audit.sh` 可执行；ENG-04 不因 PHASES 未勾 ✅ 失败
- `python3 scripts/count_docs.py` 打印行数且当前应失败
</verification>

<success_criteria>
Wave 0 完成当且仅当：六硬门脚本已落地、eng_audit/count_docs 就位、省略误杀已清、qa_check 对本仓诚实红。
</success_criteria>

<output>
Create `.planning/phases/07-p6-qa/07-00-SUMMARY.md` when done
</output>
