---
phase: 07-p6-qa
plan: 01
type: execute
wave: 1
depends_on:
  - "07-00"
files_modified:
  - examples/pom.xml
  - examples/README.md
  - examples/e12-05-streaming-rag-lite/
  - examples/e12-09-streaming-tool-call/
  - examples/e12-11-streaming-workflow/
  - examples/e12-12-multi-agent-topology/
  - examples/e12-13-langgraph-mock/
  - examples/e12-14-knowledge-graph-events/
  - examples/e12-16-trace-propagation/
  - examples/e12-19-ai-gateway-route/
  - examples/e12-20-embedding-cache/
  - examples/e12-21-streaming-evaluation/
  - examples/e12-23-online-learning-sample/
  - examples/e08-cdc/
  - examples/e01-hello-flink/
  - examples/e02-time-window/
  - examples/e04-checkpoint/
  - examples/e10-cep/
  - examples/e11-async-io/
  - ai/README.md
autonomous: true
requirements:
  - QA-02
user_setup: []

must_haves:
  truths:
    - "examples/ 下 public static void main 计数 ≥100（D-01/D-02/D-03）；projects/ 主类不计入硬门禁"
    - "全部新进主构建模块可 `mvn -pl … -am -DskipTests compile`；全 examples `mvn -T1C -DskipTests compile` 通过"
    - "每个新案例有 README 八段要点或模块 README 登记；无空壳 main、无 TODO/自行实现占位（D-02）"
  artifacts:
    - path: "examples/pom.xml"
      provides: "新零依赖 e12 模块登记（不含 Preview Agents）"
      contains: "e12-05|e12-09|e12-11"
    - path: "examples/README.md"
      provides: "案例表登记新 Demo"
      contains: "e12-05|Phase 7"
    - path: "examples/e12-05-streaming-rag-lite/"
      provides: "e12 缺口批首个可运行 Job"
      contains: "main"
  key_links:
    - from: "examples/pom.xml"
      to: "e12-*-*/pom.xml"
      via: "<modules> 聚合"
      pattern: "<module>e12-"
    - from: "新 *Job.java"
      to: "Labs.events / DataGeneratorSource"
      via: "零依赖源（D-02）"
      pattern: "Labs\\.events|DataGeneratorSource"
    - from: "qa_check ④"
      to: "examples/**/*.java main"
      via: "grep mains ≥100"
      pattern: "public static void main"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 让 `qa_check` 六硬门与 ENG 终检可脚本化、案例≥100、文档≥30k、状态终稿一致，**so that** 里程碑达到可打 tag 的完成态（git tag 本身留给 `/gsd-complete-milestone`，per D-12）。

<objective>
Wave 1 案例扩容（D-13 步 2 / QA-02 案例轴）：真实可编译 Demo 将 examples mains 从 67 提升到 ≥100，优先 e12 编号缺口。

Purpose: 满足 D-01/D-02/D-03 硬门禁；禁止空壳与 SQL-only 充数。
Output: ≥33 个新 main（e12 优先批 + e0x/e08 回填）+ pom/README/ai 登记；compile 绿；mains≥100。本波文档行数仍可红。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-p6-qa/07-00-SUMMARY.md
@.planning/phases/07-p6-qa/07-CONTEXT.md
@.planning/phases/07-p6-qa/07-RESEARCH.md
@.planning/phases/07-p6-qa/07-PATTERNS.md
@examples/e12-01-polling-vs-event/
@examples/e12-17-streaming-guardrail/
@examples/e12-18-streaming-cost-control/
@examples/e11-async-io/
@examples/pom.xml
@examples/README.md
@ai/README.md
@scripts/qa_check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: 补全 e12 编号缺口零依赖模块（优先批 ~11–12 mains）</name>
  <files>examples/pom.xml, examples/README.md, ai/README.md, examples/e12-05-streaming-rag-lite/, examples/e12-09-streaming-tool-call/, examples/e12-11-streaming-workflow/, examples/e12-12-multi-agent-topology/, examples/e12-13-langgraph-mock/, examples/e12-14-knowledge-graph-events/, examples/e12-16-trace-propagation/, examples/e12-19-ai-gateway-route/, examples/e12-20-embedding-cache/, examples/e12-21-streaming-evaluation/, examples/e12-23-online-learning-sample/</files>
  <read_first>
    - .planning/phases/07-p6-qa/07-CONTEXT.md（D-01/D-02）
    - .planning/phases/07-p6-qa/07-RESEARCH.md（e12 缺口表；禁止 Preview 进主 pom）
    - .planning/phases/07-p6-qa/07-PATTERNS.md（e12-01 pom/Job/README 模板；Broadcast/Async 类比）
    - examples/e12-01-polling-vs-event/pom.xml
    - examples/e12-01-polling-vs-event/src/main/java/com/flywhl/flinklab/e12/PollingVsEventDrivenJob.java
    - examples/e12-01-polling-vs-event/README.md
    - examples/e12-17-streaming-guardrail/src/main/java/com/flywhl/flinklab/e12/StreamingGuardrailJob.java
    - examples/e11-async-io/C2TimeoutRetryJob.java
    - examples/pom.xml
    - ai/README.md
  </read_first>
  <action>
    按 RESEARCH e12 缺口表实现零依赖可运行 Job（Discretion 类名可微调，编号与主题锁定）：
    1) 必做模块（各 ≥1 main）：e12-05 streaming-rag-lite（Keyed State 片段索引+规则检索，无 Milvus）；e12-09 tool-call（幂等键+副作用侧输出）；e12-11 workflow FSM ProcessFunction；e12-12 multi-agent 双流 connect；e12-13 langgraph-mock（AsyncIO mock HTTP+超时降级，复用 e11）；e12-14 knowledge-graph 三元组 MapState；e12-16 traceId 传播；e12-19 AI Gateway Broadcast 路由；e12-20 embedding cache LRU MapState+命中率；e12-21 evaluation 窗口指标；e12-23 online-learning 样本侧输出。
    2) e12-10 MCP：可选跳过（A1）；若跳过须在 Task 2 用 e0x 补足差额。e12-24 不强制 Java。
    3) 每个模块：克隆 e12-01 pom 依赖集（common + streaming + clients + datagen + connector-base + slf4j）；`public final class …Job`；包 `com.flywhl.flinklab.e12`；关键算子 `.uid("e12-XX-…")`；`env.execute("e12-XX-…")`；README 80–120 行含背景/验证/源码要点/面试题（八段要点即可）。
    4) 禁止：空壳 main；TODO/自行实现/请参考官网；flink-agents / Milvus 新坐标进主 pom；把 e12-03/04 SQL 包装成无逻辑 Java。
    5) 登记：`examples/pom.xml` `<modules>`、`examples/README.md` 表、`ai/README.md` 章↔Demo 映射。
    6) 每模块 `mvn -pl &lt;module&gt; -am -DskipTests compile` 通过后再提交下一批。
  </action>
  <verify>
    <automated>cd examples &amp;&amp; mvn -q -T1C -pl e12-05-streaming-rag-lite,e12-09-streaming-tool-call,e12-11-streaming-workflow,e12-12-multi-agent-topology,e12-13-langgraph-mock,e12-14-knowledge-graph-events,e12-16-trace-propagation,e12-19-ai-gateway-route,e12-20-embedding-cache,e12-21-streaming-evaluation,e12-23-online-learning-sample -am -DskipTests compile &amp;&amp; grep -rl --include='*.java' 'public static void main' e12-05-streaming-rag-lite e12-09-streaming-tool-call e12-11-streaming-workflow e12-12-multi-agent-topology e12-13-langgraph-mock e12-14-knowledge-graph-events e12-16-trace-propagation e12-19-ai-gateway-route e12-20-embedding-cache e12-21-streaming-evaluation e12-23-online-learning-sample | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 上表 11 个新模块均在父 pom modules 且 compile 绿
    - 每模块 ≥1 个真实 main + README 要点
    - 无 Preview Agents 坐标进入父构建
  </acceptance_criteria>
  <done>e12 优先批可编译并登记</done>
</task>

<task type="auto">
  <name>Task 2: e08/e0x/e11 配额回填至 mains ≥100</name>
  <files>examples/e08-cdc/, examples/e01-hello-flink/, examples/e02-time-window/, examples/e04-checkpoint/, examples/e10-cep/, examples/e11-async-io/, examples/README.md</files>
  <read_first>
    - .planning/phases/07-p6-qa/07-RESEARCH.md（分模块 mains 表；剩余 ~21 分配）
    - .planning/phases/07-p6-qa/07-PATTERNS.md（同模块多 Job 模式）
    - examples/e01-hello-flink/HelloEventTimeWindowJob.java
    - examples/e08-cdc/（现有唯一 main）
    - examples/e11-async-io/（FakeDimClient / C2）
    - 当前 `grep -rl 'public static void main' examples | wc -l` 实测值
  </read_first>
  <action>
    在 Task 1 之后测量缺口，补足至 ≥100（目标净增使总数≥100，通常还需 ~21）：
    1) e08-cdc：+3 零依赖 Job（datagen/Labs，禁止为凑数扩散真 Postgres CDC 依赖）。
    2) e11-async-io：+2～3（超时/缓存/降级教学变体，复用 FakeDimClient 模式）。
    3) e01 +2；e04 +2；e02 +2；e10 +2；其余差额用热门已有 e12 模块第二 Job 或上述模块追加（同模块多 main 合法且计入 ④ 段）。
    4) 若 Task 1 跳过 e12-10，本任务必须多补 1 个 main。
    5) 每个新 Job：独立教学点、uid、execute 名、模块 README 登记一行；禁止复制粘贴仅改类名无逻辑（T-07-02 计量诚实）。
    6) 更新 `examples/README.md` 相关行状态。
  </action>
  <verify>
    <automated>MAINS=$(grep -rl --include='*.java' 'public static void main' examples | wc -l | tr -d ' '); echo mains=$MAINS; test "$MAINS" -ge 100; cd examples &amp;&amp; mvn -q -T1C -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl … examples | wc -l` ≥100
    - `mvn -T1C -DskipTests compile` 在 examples 全绿
    - 无空壳、无违禁词新引入
  </acceptance_criteria>
  <done>案例硬门禁阈值可被满足（compile+mains）</done>
</task>

<task type="auto">
  <name>Task 3: 案例轴门禁回归（允许文档行数仍红）</name>
  <files>scripts/qa_check.sh</files>
  <read_first>
    - scripts/qa_check.sh
    - .planning/phases/07-p6-qa/07-VALIDATION.md（Wave 采样）
  </read_first>
  <action>
    跑全量 `bash scripts/qa_check.sh` 并解读输出：
    1) 断言④案例 ≥100 ok；⑥ mvn compile ok。
    2) ⑤文档行数预期仍 FAIL（留给 07-02）——本任务不扩文档注水。
    3) 若②③因新 README 断链/违禁词失败：当场修复相对链接与用词，禁止放宽排除列表。
    4) 在 SUMMARY 记录实际 mains 数与仍红的门禁项。
  </action>
  <verify>
    <automated>MAINS=$(grep -rl --include='*.java' 'public static void main' examples | wc -l | tr -d ' '); test "$MAINS" -ge 100; bash scripts/qa_check.sh &gt; /tmp/qa01.out 2&gt;&amp;1; ec=$?; grep -E '案例数|mvn compile|文档行数' /tmp/qa01.out; grep -q '案例数 ≥ 100' /tmp/qa01.out; grep -q 'mvn compile 全模块' /tmp/qa01.out; grep -E '文档行数不足|文档行数 ≥ 30000' /tmp/qa01.out</automated>
  </verify>
  <acceptance_criteria>
    - 案例与 compile 段通过
    - 文档段仍可失败（诚实红）
    - 无新增断链/违禁词阻挡
  </acceptance_criteria>
  <done>QA-02 案例轴达成；文档轴留给下一波</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo 源码 → mains 计数 | 每个 main 文件抬升 QA-02 指标 |
| 新模块 → 父 pom 构建 | 依赖坐标进入主构建供应链 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | 空壳 main / SQL 包装充数 | mitigate | D-02 要求真实逻辑+README；审核 uid/execute/教学点；禁止 e12-03/04 无逻辑包装 |
| T-07-05 | Tampering | Preview 依赖潜入主 pom | mitigate | 新模块仅零依赖集；禁止 flink-agents/Milvus |
| T-07-01 | Spoofing | 改阈值而非补案例 | mitigate | 阈值保持 100；本波只抬内容 |
| T-07-SC | Tampering | 新 Maven 坐标 | mitigate | 不新增未登记坐标；若必须先走 ENG-01 矩阵（本波不应需要） |
</threat_model>

<verification>
- mains ≥100；examples 全模块 compile 绿
- qa_check ④⑥ 绿；⑤ 可仍红
</verification>

<success_criteria>
examples 可运行作业数 ≥100 且全编译通过；无空壳、无 Preview 污染主构建。
</success_criteria>

<output>
Create `.planning/phases/07-p6-qa/07-01-SUMMARY.md` when done
</output>
