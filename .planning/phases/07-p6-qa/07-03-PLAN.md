---
phase: 07-p6-qa
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - scripts/eng_audit.sh
  - scripts/qa_check.sh
  - README.md
  - PHASES.md
  - CHANGELOG.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - docs/README.md
  - docs/QA-REPORT.md
autonomous: false
requirements:
  - QA-01
  - QA-02
  - ENG-01
  - ENG-02
  - ENG-03
  - ENG-04
user_setup: []

must_haves:
  truths:
    - "在 OrbStack arm64 本机 bash scripts/qa_check.sh 与 bash scripts/eng_audit.sh 均 exit 0（D-09；禁止沙箱伪绿）"
    - "根 README 完成态表述、PHASES.md P6 可验证完成态、CHANGELOG 未发布区含 P6 发布说明草稿一致（D-11/D-12）"
    - "REQUIREMENTS/PROJECT 中 QA-* / ENG-* 仅在实测双绿后标完成；未打 git tag（D-12）"
  artifacts:
    - path: "PHASES.md"
      provides: "P6 可验证完成态行"
      contains: "可验证完成态|QA-01|30000|100"
    - path: "CHANGELOG.md"
      provides: "Unreleased P6 发布说明草稿"
      contains: "P6|qa_check|eng_audit"
    - path: "README.md"
      provides: "完成态与版本矩阵 SSOT 对齐"
      contains: "版本|Flink"
    - path: "scripts/eng_audit.sh"
      provides: "ENG-04 严格 PHASES 断言启用"
      contains: "ENG-04|P6"
  key_links:
    - from: "qa_check.sh"
      to: "eng_audit.sh"
      via: "单一入口双绿"
      pattern: "eng_audit"
    - from: "PHASES.md P6"
      to: "scripts/qa_check.sh 实测"
      via: "完成态断言与证据"
      pattern: "qa_check|30000|100"
    - from: "CHANGELOG Unreleased"
      to: "PHASES / REQUIREMENTS"
      via: "ENG-04 会话收尾三联"
      pattern: "Unreleased|未发布|P6"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 让 `qa_check` 六硬门与 ENG 终检可脚本化、案例≥100、文档≥30k、状态终稿一致，**so that** 里程碑达到可打 tag 的完成态（git tag 本身留给 `/gsd-complete-milestone`，per D-12）。

<objective>
Wave 3 终态全绿（D-13 步 4）：清违禁词/断链，README/PHASES/CHANGELOG/PROJECT/REQUIREMENTS 同步，OrbStack 上 qa_check+eng_audit 双绿。

Purpose: 关闭 QA-01/QA-02 与 ENG-01…04；仓库进入「可打 tag」状态但不执行 tag（D-12）。
Output: 状态终稿 + 可选 docs/QA-REPORT.md + 双绿证据；eng_audit ENG-04 升级为严格 PHASES 断言。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-p6-qa/07-02-SUMMARY.md
@.planning/phases/07-p6-qa/07-CONTEXT.md
@.planning/phases/07-p6-qa/07-RESEARCH.md
@.planning/phases/07-p6-qa/07-PATTERNS.md
@.planning/phases/07-p6-qa/07-VALIDATION.md
@scripts/qa_check.sh
@scripts/eng_audit.sh
@README.md
@PHASES.md
@CHANGELOG.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@docs/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 清违禁词/断链并硬化 eng_audit ENG-04</name>
  <files>scripts/eng_audit.sh, scripts/qa_check.sh</files>
  <read_first>
    - .planning/phases/07-p6-qa/07-CONTEXT.md（D-08/D-09/D-10/D-11）
    - .planning/phases/07-p6-qa/07-RESEARCH.md（ENG-04 Wave 4 hard；Pitfall 4 断链）
    - scripts/qa_check.sh
    - scripts/eng_audit.sh
    - PHASES.md（P5 完成态行作措辞样板）
  </read_first>
  <action>
    1) 跑 `bash scripts/qa_check.sh`，修复全部 ② 违禁词与 ③ 断链命中；禁止加宽排除列表；禁止裸匹配「略」。
    2) 升级 `eng_audit.sh` ENG-04：在 CHANGELOG 未发布区检查之外，严格断言 PHASES.md P6 行含可验证完成态表述（非单独 `📋` 占位）——仅在即将勾 ✅ 的同会话启用（本 plan Task 2 同步改 PHASES）。
    3) 确认 ENG-01…03 仍绿；证据指针路径仍存在。
    4) 可选 Discretion：生成 `docs/QA-REPORT.md`（命令输出摘要 + mains/行数表），不挡门禁；若生成须先在 docs/README 登记。
    5) 不打 git tag / 不建 GitHub Release（D-12）。
  </action>
  <verify>
    <automated>bash scripts/qa_check.sh &gt; /tmp/qa03a.out 2&gt;&amp;1; echo qa_ec=$?; tail -30 /tmp/qa03a.out; grep -E '违禁词|断链|QA PASS|QA FAIL' /tmp/qa03a.out</automated>
  </verify>
  <acceptance_criteria>
    - 违禁词与断链段通过（或仅剩 Task 2 状态文件待改造成的短暂不一致，须在本 plan 内消掉）
    - eng_audit 已具备严格 ENG-04 PHASES 断言逻辑
  </acceptance_criteria>
  <done>门禁噪声清净；ENG-04 严格模式就绪</done>
</task>

<task type="auto">
  <name>Task 2: README / PHASES / CHANGELOG / PROJECT / REQUIREMENTS 终稿同步</name>
  <files>README.md, PHASES.md, CHANGELOG.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, docs/README.md, docs/QA-REPORT.md</files>
  <read_first>
    - .planning/phases/07-p6-qa/07-CONTEXT.md（D-11/D-12）
    - .planning/phases/07-p6-qa/07-PATTERNS.md（PHASES/CHANGELOG 终稿样板）
    - PHASES.md（P5 ✅ 行；P6 现状）
    - CHANGELOG.md（Unreleased P5 条目风格）
    - README.md（版本矩阵与完成态表述）
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md（QA/ENG 勾选）
  </read_first>
  <action>
    仅在本机已能预见 qa_check+eng_audit 将绿时执行状态回填（与 Task 3 实测绑定；若未绿不得勾完成）：
    1) `PHASES.md` P6 行改为可验证完成态（对齐 P5 风格）：写明 qa_check 全绿、mains≥100、md≥30k、eng_audit ENG-01…04；可注「扫描匹配整词省略，不裸匹配略」（D-08）。
    2) `CHANGELOG.md` Unreleased Added：P6 总装 QA 条目（六硬门、案例、文档、eng_audit）；作为发布说明草稿，不发 tag。
    3) 根 `README.md`：完成态表述与版本矩阵一致；交叉引用三生产项目与 production/ 无断链。
    4) `.planning/PROJECT.md` / `.planning/REQUIREMENTS.md`：QA-01/02、ENG-01…04 标完成（仅双绿后）。
    5) `docs/README.md`：若有 QA-REPORT 或新小节，完成登记。
    6) 禁止 git tag / GitHub Release（D-12）。
  </action>
  <verify>
    <automated>grep -E '可验证完成态|QA-01|30000|100' PHASES.md | head -5; grep -E 'P6|qa_check|eng_audit' CHANGELOG.md | head -10; grep -E 'QA-01|ENG-01' .planning/REQUIREMENTS.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - PHASES P6 为可验证完成态表述
    - CHANGELOG Unreleased 含 P6 草稿
    - REQUIREMENTS/PROJECT 完成勾选与实测绿一致（不得超前假勾）
  </acceptance_criteria>
  <done>状态轴终稿与门禁证据对齐</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: OrbStack 双绿终验（D-09）</name>
  <files>scripts/qa_check.sh, scripts/eng_audit.sh, PHASES.md, README.md</files>
  <read_first>
    - .planning/phases/07-p6-qa/07-VALIDATION.md（Manual-Only OrbStack）
    - .planning/phases/07-p6-qa/07-CONTEXT.md（D-09/D-12）
  </read_first>
  <action>
    在 OrbStack arm64 本机执行终验（per D-09），禁止沙箱伪绿、禁止打 git tag（D-12）：
    1) 确认 `uname -m`=arm64 与 Docker/OrbStack 可用。
    2) 跑通 `bash scripts/qa_check.sh` 与 `bash scripts/eng_audit.sh` 均 exit 0。
    3) 打印并记录 mains≥100、md lines≥30000。
    4) 请维护者目视确认 PHASES/README 完成态措辞一致且未打 tag；等待 resume-signal。
  </action>
  <what-built>
    升级后的六硬门 qa_check、独立 eng_audit、案例≥100、文档≥30k、README/PHASES/CHANGELOG/REQUIREMENTS 终稿。执行器应已在本机跑通脚本；本检查确认人眼认可完成态措辞与双绿输出。
  </what-built>
  <how-to-verify>
    1) 确认 `uname -m` 为 arm64，Docker context 为 orbstack（或等价本机 OrbStack）。
    2) 执行 `bash scripts/qa_check.sh` → 期望 `== QA PASS ==`，exit 0。
    3) 执行 `bash scripts/eng_audit.sh` → 期望 `== ENG AUDIT PASS ==`，exit 0。
    4) 目视 PHASES.md P6 行与根 README 完成态措辞一致；CHANGELOG Unreleased 含 P6；确认**没有**新建 git tag。
    5) 记录实际 mains 与文档行数到 SUMMARY（或可选 QA-REPORT）。
  </how-to-verify>
  <resume-signal>Type "approved" 或列出仍红/措辞问题</resume-signal>
  <verify>
    <automated>uname -m; bash scripts/qa_check.sh; bash scripts/eng_audit.sh; MAINS=$(grep -rl --include='*.java' 'public static void main' examples | wc -l | tr -d ' '); DOC=$(find . -name '*.md' -not -path './.planning/*' -not -path './.git/*' -print0 | xargs -0 wc -l | tail -1 | awk '{print $1}'); echo mains=$MAINS doc=$DOC; test "$MAINS" -ge 100; test "$DOC" -ge 30000</automated>
    <human-check>确认 PHASES/README 完成态措辞一致且未打 tag</human-check>
  </verify>
  <acceptance_criteria>
    - qa_check 与 eng_audit 双绿
    - mains≥100 且 md≥30000
    - 无 git tag；人审措辞通过
  </acceptance_criteria>
  <done>Phase 7 达到可打 tag 完成态</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI 绿 → 本机 OrbStack | CI 不替代 D-09 本机实测 |
| 状态文档 ✅ → 真实门禁 | 勾选必须落后于或同步于双绿 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | 沙箱伪绿 / 先勾后测 | mitigate | D-09 OrbStack 强制；REQUIREMENTS 仅双绿后勾；checkpoint 人审 |
| T-07-02 | Tampering | 放宽排除列表换绿 | mitigate | 排除集保持 D-08；修复内容而非绕过 |
| T-07-07 | Elevation | 本 Phase 打 tag 提前发布 | mitigate | D-12 禁止 tag；任务明示留给 complete-milestone |
| T-07-04 | Tampering | ENG 版本漂移 | mitigate | eng_audit ENG-01 终检必须绿 |
| T-07-SC | Tampering | 包安装 | accept | 无新包 |
</threat_model>

<verification>
- OrbStack：`bash scripts/qa_check.sh` &amp;&amp; `bash scripts/eng_audit.sh` 双绿
- mains≥100；md≥30000；无 tag
</verification>

<success_criteria>
QA-01/QA-02 与 ENG-01…04 全部关闭；仓库可打 tag 但未打 tag。
</success_criteria>

<output>
Create `.planning/phases/07-p6-qa/07-03-SUMMARY.md` when done
</output>
