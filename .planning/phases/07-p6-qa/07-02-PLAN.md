---
phase: 07-p6-qa
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - docs/
  - interview/L1.md
  - interview/L2.md
  - interview/L3.md
  - interview/L4.md
  - interview/L5.md
  - interview/L6.md
  - interview/L7.md
  - interview/L8.md
  - ai/chapters/
  - best-practice/
  - production/docs/
  - examples/
autonomous: true
requirements:
  - QA-02
user_setup: []

must_haves:
  truths:
    - "全仓 *.md（排除 .planning/ 与 .git/）行数 ≥30000（D-04/D-05/D-06）；实质扩写非注水"
    - "docs/ 模块按八段式补全并互链 examples；interview 答案加厚含机制/反例/仓库路径"
    - "qa_check ⑤ 文档行数段通过；禁止把 .planning/ 计入刷数（T-07-02）"
  artifacts:
    - path: "docs/"
      provides: "模块正文实质扩写（最大行数杠杆）"
      min_lines: 5000
    - path: "interview/L1.md"
      provides: "题库加厚样板"
      contains: "参考答案|docs/"
    - path: "best-practice/"
      provides: "规范正文+production 互链"
      contains: "检查清单|反"
  key_links:
    - from: "docs/*/README.md"
      to: "examples/**/README.md"
      via: "相对路径交叉引用（docs/README 规则）"
      pattern: "\\.\\./\\.\\./examples/"
    - from: "interview/L*.md"
      to: "docs/ 与 projects/"
      via: "答案内仓库路径对照"
      pattern: "docs/|examples/|projects/"
    - from: "qa_check ⑤"
      to: "find+wc excl .planning"
      via: "D-04 口径"
      pattern: "30000"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 让 `qa_check` 六硬门与 ENG 终检可脚本化、案例≥100、文档≥30k、状态终稿一致，**so that** 里程碑达到可打 tag 的完成态（git tag 本身留给 `/gsd-complete-milestone`，per D-12）。

<objective>
Wave 2 文档实质扩写（D-13 步 3 / QA-02 文档轴）：将非 `.planning` Markdown 从 ~10k 抬到 ≥30000 行。

Purpose: 满足 D-04/D-05/D-06；内容禁注水、禁粘贴官网、禁重复段落刷行。
Output: docs/ + interview/ + ai/chapters + best-practice（及必要 production/examples README）扩写；`count_docs.py`/`qa_check` ⑤ 绿。本波可不改 PHASES ✅（留给 07-03）。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-p6-qa/07-01-SUMMARY.md
@.planning/phases/07-p6-qa/07-CONTEXT.md
@.planning/phases/07-p6-qa/07-RESEARCH.md
@.planning/phases/07-p6-qa/07-PATTERNS.md
@docs/README.md
@docs/01-runtime/README.md
@interview/L1.md
@interview/README.md
@ai/chapters/
@best-practice/02-uid-savepoint.md
@scripts/count_docs.py
@scripts/qa_check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实质扩写 docs/ 偏薄模块（目标增益 +10k～12k）</name>
  <files>docs/</files>
  <read_first>
    - .planning/phases/07-p6-qa/07-CONTEXT.md（D-04/D-05）
    - .planning/phases/07-p6-qa/07-RESEARCH.md（文档增益目标表；docs 918→11k–13k）
    - .planning/phases/07-p6-qa/07-PATTERNS.md（docs 八段式与交叉引用规则）
    - docs/README.md（编号 SSOT；交叉引用相对路径）
    - docs/01-runtime/README.md（实质章节体样板）
    - python3 scripts/count_docs.py 当前分目录基线
  </read_first>
  <action>
    按八段式扩写 `docs/` 仍偏薄模块（01–11、13–14 等；以 RESEARCH 表与 count_docs 诊断为准）：
    1) 每模块补齐：背景→架构→代码锚点→启动→验证→踩坑→最佳实践→面试题；互链 `../../examples/...` 相对路径。
    2) 新增内容必须可核对仓库真实路径；禁止大段复制官网；禁止同一段落复制粘贴刷行（D-05 / T-07-02）。
    3) 不修改 `.planning/` 充数；不把规划稿移入 docs 刷行。
    4) 扩写后跑 `python3 scripts/count_docs.py` 看 docs/ 分目录增益；修断链时统一相对路径（Pitfall 4）。
    5) 若 `docs/README.md` 需登记新小节，先登记再写正文（ENG-02 纪律；本波可登记但不勾 REQUIREMENTS 完成）。
  </action>
  <verify>
    <automated>python3 scripts/count_docs.py &gt; /tmp/docs01.txt 2&gt;&amp;1 || true; grep -E 'docs/|total|FAIL|ok' /tmp/docs01.txt | head -40; DOC=$(find docs -name '*.md' -print0 | xargs -0 wc -l | tail -1 | awk '{print $1}'); echo docs_lines=$DOC; test "$DOC" -ge 5000</automated>
  </verify>
  <acceptance_criteria>
    - docs/ 合计较基线显著增长（目标进入数千行以上，向 11k+ 迈进）
    - 无官网粘贴块；交叉引用为相对路径
    - 新链不故意引入断链（发现即修）
  </acceptance_criteria>
  <done>docs/ 主杠杆扩写完成一截可测增益</done>
</task>

<task type="auto">
  <name>Task 2: 加厚 interview/L1–L8 参考答案（目标增益 +6k～8k）</name>
  <files>interview/L1.md, interview/L2.md, interview/L3.md, interview/L4.md, interview/L5.md, interview/L6.md, interview/L7.md, interview/L8.md</files>
  <read_first>
    - .planning/phases/07-p6-qa/07-RESEARCH.md（interview ~4.1 行/题 → 25–40 行/题）
    - .planning/phases/07-p6-qa/07-PATTERNS.md（L1 题块结构）
    - interview/L1.md
    - interview/README.md
    - scripts/count_interview.py
  </read_first>
  <action>
    在不强制增题的前提下加厚答案（题量已 ≥150，保持 `count_interview.py` 绿）：
    1) 每题保留题干；**参考答案**扩为：定义→机制→反例→与仓库路径对照（docs/examples/projects）。
    2) 目标约 25–40 行/题或等价总增益 +6k～8k；优先薄答案题。
    3) 禁止重复粘贴同一答案模板无具体考点；禁止违禁词；链接用相对路径。
    4) 跑 `python3 scripts/count_interview.py` 确认题量门禁仍过。
  </action>
  <verify>
    <automated>python3 scripts/count_interview.py; INT=$(find interview -name '*.md' -print0 | xargs -0 wc -l | tail -1 | awk '{print $1}'); echo interview_lines=$INT; test "$INT" -ge 4000</automated>
  </verify>
  <acceptance_criteria>
    - interview 总行数较 ~1012 基线大幅上升（≥4000 为最低中间门槛；继续向 7k–9k）
    - count_interview.py exit 0
    - 答案含机制/反例/路径，非空话注水
  </acceptance_criteria>
  <done>面试题库答案深度达标增益</done>
</task>

<task type="auto">
  <name>Task 3: ai/chapters + best-practice + 余量补齐至 ≥30000</name>
  <files>ai/chapters/, best-practice/, production/docs/, examples/</files>
  <read_first>
    - .planning/phases/07-p6-qa/07-RESEARCH.md（增益表 ai/best-practice/production/examples）
    - ai/chapters/09-streaming-tool-call.md
    - best-practice/02-uid-savepoint.md
    - production/docs/（勿重复堆 bluegreen-timeline）
    - scripts/count_docs.py
    - scripts/qa_check.sh
  </read_first>
  <action>
    补齐剩余行数缺口直至全仓 excl `.planning` ≥30000：
    1) ai/chapters 薄章：补踩坑实证、与 p01/e11 对照、降级决策树（+2.5k～3.5k）。
    2) best-practice：每条规范补为何/怎么做/检查清单/反模式/链 production（+1.8k～2.5k）。
    3) production 非 timeline：补 SOP/排障树互链（+0.7k）；**禁止**再堆重复 bluegreen-timeline。
    4) 新 Demo README 若仍薄：补到 80–120 行八段要点。
    5) 循环：`python3 scripts/count_docs.py` → 定位缺口目录 → 实质扩写 → 再测；禁止重复段落宏注水。
    6) 最终 `bash scripts/qa_check.sh`：⑤ 必须绿；④⑥ 应保持绿；②③ 若断链/违禁词失败则本任务内修净（仍允许 ENG/PHASES 终稿留 07-03）。
  </action>
  <verify>
    <automated>DOC_LINES=$(find . -name '*.md' -not -path './.planning/*' -not -path './.git/*' -print0 | xargs -0 wc -l | tail -1 | awk '{print $1}'); echo doc_lines=$DOC_LINES; test "$DOC_LINES" -ge 30000; python3 scripts/count_docs.py; bash scripts/qa_check.sh &gt; /tmp/qa02.out 2&gt;&amp;1; ec=$?; grep -E '文档行数|案例数|违禁词|断链|mvn compile|QA PASS|QA FAIL' /tmp/qa02.out; grep -q '文档行数 ≥ 30000' /tmp/qa02.out</automated>
  </verify>
  <acceptance_criteria>
    - 全仓 md（excl .planning/.git）≥30000
    - qa_check ⑤ 通过；案例/compile 不回退
    - 抽检无大段重复注水块
  </acceptance_criteria>
  <done>QA-02 文档轴达成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown 内容 → 行数门禁 | 行数直接决定 QA-02 文档轴 |
| 文档相对链接 → 断链扫描 | 新链进入 qa_check ③ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | 注水/复制官网/.planning 刷行 | mitigate | D-05 实质内容；排除 .planning；增益表分配；禁止重复块 |
| T-07-03 | Information Disclosure | 断链路径拼接 | mitigate | 仅存在性检查；新链统一相对路径 |
| T-07-06 | Repudiation | 无仓库路径的空泛答案 | mitigate | interview 强制链 docs/examples/projects |
| T-07-SC | Tampering | 包安装 | accept | 无新包 |
</threat_model>

<verification>
- `python3 scripts/count_docs.py` exit 0
- qa_check ⑤ 绿；④⑥ 保持绿
</verification>

<success_criteria>
文档行数 ≥30000 且为实质扩写；门禁⑤通过。
</success_criteria>

<output>
Create `.planning/phases/07-p6-qa/07-02-SUMMARY.md` when done
</output>
