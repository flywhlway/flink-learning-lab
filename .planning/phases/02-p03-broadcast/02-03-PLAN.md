---
phase: 02-p03-broadcast
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py
  - projects/p03-vehicle-monitoring/scripts/verify.sh
  - projects/p03-vehicle-monitoring/Makefile
  - projects/p03-vehicle-monitoring/README.md
  - docs/README.md
  - CHANGELOG.md
  - PHASES.md
autonomous: false
requirements:
  - VEH-03
  - VEH-04
user_setup:
  - service: orbstack-base
    why: "切换剧本 e2e 需要 docker 基座 + up-p03 + 已 submit 改造后的作业"
    env_vars: []
    dashboard_config:
      - task: "确保 OrbStack 下 kafka/flink/clickhouse 健康，并 make up-p03 使 control topic + pattern_id DDL 生效"
        location: "cd docker && make up && make init && make up-p03"

must_haves:
  truths:
    - "造数支持 --scenario match-harsh-fault / match-triple-harsh / match-dtc-pair（match 别名→match-harsh-fault）且每场景含尾心跳推进 watermark（D-12）"
    - "支持 --publish-control 向 vehicle.pattern.control 发送确定性 JSON activePatterns+version（D-04/D-12）"
    - "维护者执行切换剧本：TRUNCATE → publish-control → gen 目标模式 → PATTERN_ID=… verify，ClickHouse 为唯一权威出口断言 pattern_id 变化（D-10）"
    - "未发控制消息时默认路径 match-harsh-fault + 默认 PATTERN_ID=HARSH_THEN_FAULT 仍绿（D-06 回归）"
    - "README 交叉引用 docs/PATTERN-LIBRARY.md 并说明静态三 CEP + Broadcast 门控路线（D-03）；qa_check 全绿；会话收尾 CHANGELOG/PHASES"
  artifacts:
    - path: "projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py"
      provides: "三 scenario + publish-control（D-12）"
      contains: "match-triple-harsh"
    - path: "projects/p03-vehicle-monitoring/scripts/verify.sh"
      provides: "PATTERN_ID CH 权威断言（D-10）"
      contains: "PATTERN_ID"
    - path: "projects/p03-vehicle-monitoring/README.md"
      provides: "八段式更新 + PATTERN-LIBRARY 链接（D-03）"
      contains: "PATTERN-LIBRARY"
    - path: "projects/p03-vehicle-monitoring/Makefile"
      provides: "verify-switch 或等价串联目标（D-10）"
      contains: "verify"
  key_links:
    - from: "gen --publish-control"
      to: "PatternActivationGate"
      via: "Kafka vehicle.pattern.control"
      pattern: "publish-control|pattern.control"
    - from: "gen --scenario match-*"
      to: "verify.sh PATTERN_ID → vehicle_alerts.pattern_id"
      via: "TRUNCATE→control→gen→CH count"
      pattern: "PATTERN_ID|pattern_id|TRUNCATE"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 维护恰好 3 条可评审 CEP 模式库并通过 Kafka Broadcast 控制消息切换预编译激活集、在 ClickHouse 按 pattern_id 断言匹配变化，**so that** 无 within 不得合入且不必重启作业即可演示开源 CEP 动态化路线。

<objective>
垂直切片 VEH-03/VEH-04（验收半段）：造数场景 + 控制消息 + PATTERN_ID 切换 e2e + README/登记/qa_check，并经人工确认匹配行为变化可观察。

Purpose: 把「可切换」变成 OrbStack 可复现剧本；文档交叉引用 PATTERN-LIBRARY。
Output: gen/verify/Makefile 切换路径转绿；README/CHANGELOG/PHASES；人工 approved；qa_check 绿。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-p03-broadcast/02-CONTEXT.md
@.planning/phases/02-p03-broadcast/02-RESEARCH.md
@.planning/phases/02-p03-broadcast/02-PATTERNS.md
@.planning/phases/02-p03-broadcast/02-02-SUMMARY.md
@.planning/phases/01-p03/01-03-SUMMARY.md
@projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py
@projects/p03-vehicle-monitoring/scripts/verify.sh
@projects/p03-vehicle-monitoring/docs/PATTERN-LIBRARY.md
@projects/p03-vehicle-monitoring/README.md
@scripts/qa_check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: 造数 scenarios + publish-control + verify-switch</name>
  <files>projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py, projects/p03-vehicle-monitoring/scripts/verify.sh, projects/p03-vehicle-monitoring/Makefile</files>
  <read_first>
    - projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py（现有 match + 尾心跳纪律）
    - projects/p03-vehicle-monitoring/scripts/verify.sh（Wave 0 PATTERN_ID 白名单）
    - .planning/phases/02-p03-broadcast/02-RESEARCH.md（Pitfall 1/2、Open Q2/Q3、切换剧本）
    - .planning/phases/02-p03-broadcast/02-CONTEXT.md（D-10/D-12）
    - .planning/phases/01-p03/01-03-SUMMARY.md（WM 尾心跳、CH 权威）
  </read_first>
  <action>
    扩展 gen_vehicle_events.py（per D-12）：
    - choices：match（别名→match-harsh-fault）、match-harsh-fault、match-triple-harsh、match-dtc-pair
    - match-triple-harsh：连续 3× HARSH_ACCEL(value&gt;450)，中间禁止 HEARTBEAT；序列前后可心跳；末尾 +12s/+18s 级尾心跳推进 WM
    - match-dtc-pair：两 DTC(value&gt;480) 落在 15s 窗内 + 尾心跳
    - match-harsh-fault：保留 Phase 1 harsh→DTC + 尾心跳
    - --publish-control 接收 JSON 字符串（或文件），发到 vehicle.pattern.control（可用 --control-topic 覆盖），bootstrap 宿主机 localhost:9094，acks=all
    巩固 verify.sh：PATTERN_ID/MIN_COUNT；白名单；CH 唯一权威。Makefile 增加目标（名称可用 verify-switch）：文档化串联 TRUNCATE flinklab.vehicle_alerts → publish-control（如激活仅 TRIPLE_HARSH,version≥2）→ gen match-triple-harsh → PATTERN_ID=TRIPLE_HARSH verify；另保留默认 gen/verify 回归。禁止以 Kafka 有消息作为放行条件（D-10）。
  </action>
  <verify>
    <automated>bash -n projects/p03-vehicle-monitoring/scripts/verify.sh &amp;&amp; python3 -m py_compile projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py &amp;&amp; rg -n 'match-triple-harsh|match-dtc-pair|publish-control|match-harsh-fault' projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py &amp;&amp; rg -n 'PATTERN_ID|pattern_id' projects/p03-vehicle-monitoring/scripts/verify.sh &amp;&amp; rg -n 'verify-switch|publish-control|PATTERN_ID' projects/p03-vehicle-monitoring/Makefile</automated>
  </verify>
  <acceptance_criteria>
    - gen 脚本含三 scenario 名与 publish-control
    - match-triple-harsh 路径不在三次 HARSH 之间插入 HEARTBEAT
    - verify.sh 仍禁止 Kafka 单独放行
    - Makefile 存在可演示切换的目标或清晰注释命令块
  </acceptance_criteria>
  <done>D-10/D-12 脚本与 Makefile 切换路径可执行。</done>
</task>

<task type="auto">
  <name>Task 2: OrbStack 切换 e2e + README/登记/qa_check</name>
  <files>projects/p03-vehicle-monitoring/README.md, docs/README.md, CHANGELOG.md, PHASES.md</files>
  <read_first>
    - projects/p03-vehicle-monitoring/README.md（八段式结构）
    - projects/p03-vehicle-monitoring/docs/PATTERN-LIBRARY.md
    - docs/README.md（模块登记惯例）
    - CHANGELOG.md / PHASES.md
    - scripts/qa_check.sh
    - .planning/phases/02-p03-broadcast/02-CONTEXT.md（D-03/D-10）
  </read_first>
  <action>
    在 OrbStack arm64 实测（预算建议 &lt;300s，可复用 Phase 1 节奏）：
    1) docker make up-p03（确保 control topic + DDL）
    2) p03 mvn package + make submit
    3) 默认回归：不发 control → match-harsh-fault → PATTERN_ID=HARSH_THEN_FAULT verify 应绿（D-06）
    4) 切换：TRUNCATE CH → --publish-control 激活 TRIPLE_HARSH（version 递增）→ match-triple-harsh → PATTERN_ID=TRIPLE_HARSH verify 绿；可选再断言未激活 HARSH_THEN_FAULT count 不增（D-10）
    更新 README：交叉引用 docs/PATTERN-LIBRARY.md；写清控制 topic、--publish-control、PATTERN_ID verify、静态三 CEP+出口门控叙事与「门控≠停 CEP 状态」；保持八段式，禁止 TODO/省略/请参考官网。
    若 docs/README 需登记 PATTERN-LIBRARY 路径则增量登记。更新 CHANGELOG 未发布区与 PHASES.md 状态。跑 bash scripts/qa_check.sh 必须绿。
  </action>
  <verify>
    <automated>cd projects/p03-vehicle-monitoring &amp;&amp; mvn -q test &amp;&amp; rg -n 'PATTERN-LIBRARY' README.md &amp;&amp; rg -n 'pattern.control|publish-control|PATTERN_ID' README.md &amp;&amp; cd ../.. &amp;&amp; bash scripts/qa_check.sh</automated>
  </verify>
  <acceptance_criteria>
    - README 链接或路径指向 docs/PATTERN-LIBRARY.md
    - README 含 control topic 或 publish-control 与 PATTERN_ID 验收说明
    - CHANGELOG 未发布区与 PHASES 反映 Phase 2 模式库/Broadcast 进展
    - qa_check.sh exit 0
    - 执行记录中默认回归与至少一条切换剧本 CH 断言成功（写入 SUMMARY）
  </acceptance_criteria>
  <done>e2e 切换与文档/门禁收尾完成（D-03/D-06/D-10）。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工确认模式切换可观察</name>
  <files>projects/p03-vehicle-monitoring/README.md</files>
  <read_first>
    - projects/p03-vehicle-monitoring/README.md（启动/验证/切换步骤）
    - projects/p03-vehicle-monitoring/docs/PATTERN-LIBRARY.md（五元组评审）
  </read_first>
  <what-built>
    三模式库 + Broadcast 门控作业、control topic、造数场景、PATTERN_ID verify、PATTERN-LIBRARY 文档与 README 切换剧本。
  </what-built>
  <how-to-verify>
    1. 打开 docs/PATTERN-LIBRARY.md，确认三模式五元组齐全（业务含义/within/连接语义/skip/状态上界）
    2. 按 README：up-p03 → submit → 默认 match-harsh-fault → verify（HARSH_THEN_FAULT）成功
    3. 执行切换：TRUNCATE → publish-control 切到 TRIPLE_HARSH → match-triple-harsh → PATTERN_ID=TRIPLE_HARSH verify 成功
    4. 确认未依赖「重启作业换参数」作为主路径；Kafka 诊断不能单独放行
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <action>
    暂停等待维护者按 how-to-verify 确认；收到 approved 后写 SUMMARY 标记人工验收通过。若有问题按反馈修脚本/作业后重跑自动化再请求确认。
  </action>
  <verify>
    <automated>test -f projects/p03-vehicle-monitoring/docs/PATTERN-LIBRARY.md &amp;&amp; rg -n 'HARSH_THEN_FAULT|TRIPLE_HARSH|DTC_PAIR' projects/p03-vehicle-monitoring/docs/PATTERN-LIBRARY.md >/dev/null</automated>
    <human-check>维护者确认五元组齐全且 Broadcast 切换后 CH pattern_id 行为变化可观察</human-check>
  </verify>
  <acceptance_criteria>
    - 用户回复 approved，或明确列出且已修复的问题清单后再次 approved
    - SUMMARY 记录人工验收结论
  </acceptance_criteria>
  <done>Phase 2 人工验收通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 宿主机 gen/publish-control → Kafka | 控制 JSON 与事件注入 lab 总线 |
| verify PATTERN_ID → CH SQL | 环境变量进入查询 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | --publish-control JSON / verify PATTERN_ID | mitigate | 沿用 Gate 白名单求交；verify PATTERN_ID 白名单；文档标明 lab-only |
| T-02-09 | Information Disclosure | README 凭据 | mitigate | 仅引用既有 flinklab 本地账号；不新增真实密钥 |
| T-02-SC | Tampering | confluent-kafka | accept | 已有 Approved + slopcheck [OK]；无新包 |
</threat_model>

<verification>
- gen/verify/Makefile 切换路径静态检查通过
- OrbStack：默认回归 + TRIPLE_HARSH 切换 CH 断言绿
- qa_check.sh 绿；人工 approved
</verification>

<success_criteria>
- D-10/D-12 切换 e2e 可复现，CH 为唯一权威出口
- D-03 README↔PATTERN-LIBRARY 交叉引用
- D-06 默认路径回归绿
- qa_check 与人工确认完成
</success_criteria>

<output>
Create `.planning/phases/02-p03-broadcast/02-03-SUMMARY.md` when done
</output>
