---
phase: 02-p03-broadcast
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/PatternRegistryWithinTest.java
  - projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/TripleHarshPatternTest.java
  - projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/DtcPairPatternTest.java
  - projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/PatternActivationGateTest.java
  - projects/p03-vehicle-monitoring/scripts/verify.sh
autonomous: true
requirements:
  - VEH-03
  - VEH-04
user_setup: []

must_haves:
  truths:
    - "PatternRegistryWithinTest 可被 surefire 发现且当前 RED（PatternRegistry 尚未交付或 size≠3 / 缺 within）— 建立 D-11 反馈环"
    - "TripleHarshPatternTest 与 DtcPairPatternTest 对 within(20s)/within(15s) 有非平凡断言且当前 RED（D-02）"
    - "PatternActivationGateTest 对默认激活集 HARSH_THEN_FAULT、version 单调、按 patternId 过滤有断言且当前 RED（D-05/D-06）"
    - "verify.sh 支持 PATTERN_ID 白名单环境变量（默认 HARSH_THEN_FAULT）与 MIN_COUNT，CH 仍为唯一权威出口（D-08/D-10）"
  artifacts:
    - path: "projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/PatternRegistryWithinTest.java"
      provides: "注册表 size==3 + 每条 getWindowSize 门禁（D-11）"
      contains: "PatternRegistry"
    - path: "projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/PatternActivationGateTest.java"
      provides: "门控默认集 / version / 过滤 RED 夹具（D-05/D-06）"
      contains: "HARSH_THEN_FAULT"
    - path: "projects/p03-vehicle-monitoring/scripts/verify.sh"
      provides: "按 pattern_id 断言骨架（D-08/D-10）"
      contains: "PATTERN_ID"
  key_links:
    - from: "PatternRegistryWithinTest"
      to: "PatternRegistry.all()"
      via: "JUnit 扫描每条 Pattern.getWindowSize()"
      pattern: "PatternRegistry|getWindowSize"
    - from: "verify.sh"
      to: "flinklab.vehicle_alerts.pattern_id"
      via: "CH count WHERE alert_type=MATCH AND pattern_id=白名单值"
      pattern: "pattern_id|PATTERN_ID"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 维护恰好 3 条可评审 CEP 模式库并通过 Kafka Broadcast 控制消息切换预编译激活集、在 ClickHouse 按 pattern_id 断言匹配变化，**so that** 无 within 不得合入且不必重启作业即可演示开源 CEP 动态化路线。

<objective>
Wave 0（Nyquist）：落地 VEH-03/VEH-04 失败态单测夹具与 verify PATTERN_ID 骨架，建立 RED→GREEN 反馈环。

Purpose: 02-VALIDATION.md 要求先有 PatternRegistryWithinTest、TripleHarsh/DtcPair 测、PatternActivationGateTest、verify PATTERN_ID；否则后续切片无法自动化门禁。
Output: 四个可运行测试类（期望 RED）+ verify.sh 扩展（白名单 PATTERN_ID）；不实现 PatternRegistry / Gate / 三工厂生产代码。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-p03-broadcast/02-CONTEXT.md
@.planning/phases/02-p03-broadcast/02-RESEARCH.md
@.planning/phases/02-p03-broadcast/02-PATTERNS.md
@.planning/phases/02-p03-broadcast/02-VALIDATION.md
@projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPatternTest.java
@projects/p03-vehicle-monitoring/scripts/verify.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED 夹具 — Registry within + 两新模式单测</name>
  <files>projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/PatternRegistryWithinTest.java, projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/TripleHarshPatternTest.java, projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/DtcPairPatternTest.java</files>
  <read_first>
    - projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPatternTest.java（getWindowSize / 谓词断言风格）
    - .planning/phases/02-p03-broadcast/02-VALIDATION.md（W0-01/W0-02 命令）
    - .planning/phases/02-p03-broadcast/02-RESEARCH.md（PatternRegistryWithinTest 示意、D-02 within 秒数）
    - .planning/phases/02-p03-broadcast/02-CONTEXT.md（D-01/D-02/D-11）
  </read_first>
  <behavior>
    - PatternRegistryWithinTest：遍历 PatternRegistry.all()，每条 assertTrue(getWindowSize().isPresent())；assertEquals(3, all().size())；ID 集合恰好含 HARSH_THEN_FAULT、TRIPLE_HARSH、DTC_PAIR（per D-01/D-02/D-11）
    - TripleHarshPatternTest：TripleHarshPattern.build() 的 within 为 Duration.ofSeconds(20)；谓词接受 HARSH_ACCEL value&gt;450（per D-02）
    - DtcPairPatternTest：DtcPairPattern.build() 的 within 为 Duration.ofSeconds(15)；可观察 skip 挂到 begin（文档/反射/API 可断言其一，不得仅 assertTrue(true)）（per D-02）
    - 本任务不创建生产类；编译失败或断言失败均算 RED 成功
  </behavior>
  <action>
    新建三个测试类于 cep 测试包，断言风格对齐 HarshThenFaultPatternTest（调用 getWindowSize、SimpleCondition/IterativeCondition 过滤）。引用尚未存在的 PatternRegistry / TripleHarshPattern / DtcPairPattern / PatternIds 符号——故意让 mvn -Dtest=... 失败，建立 Wave 0 RED。禁止在本任务实现这些生产类（留给 02-01）。禁止扩展 ParseVehicleJson signalType 白名单（D-01）。禁止把测试写成永远通过的空壳。
  </action>
  <verify>
    <automated>cd projects/p03-vehicle-monitoring &amp;&amp; mvn -q -Dtest=PatternRegistryWithinTest,TripleHarshPatternTest,DtcPairPatternTest test; ec=$?; test $ec -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - 三个测试文件存在且类名与文件名一致
    - PatternRegistryWithinTest 源码含 PatternRegistry、getWindowSize、以及三 ID 字符串之一（HARSH_THEN_FAULT 或 TRIPLE_HARSH 或 DTC_PAIR）
    - TripleHarshPatternTest 含 within 或 Duration.ofSeconds(20)
    - DtcPairPatternTest 含 within 或 Duration.ofSeconds(15) 或 skipPastLastEvent
    - 上述 surefire 命令退出码 ≠ 0（RED）
  </acceptance_criteria>
  <done>VEH-03 / D-11 / D-02 单测夹具可运行且为 RED。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED 夹具 — Gate 单测 + verify PATTERN_ID</name>
  <files>projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/PatternActivationGateTest.java, projects/p03-vehicle-monitoring/scripts/verify.sh</files>
  <read_first>
    - .planning/phases/02-p03-broadcast/02-RESEARCH.md（门控核心逻辑、默认集、version 单调、Open Q3 verify）
    - .planning/phases/02-p03-broadcast/02-CONTEXT.md（D-05/D-06/D-08/D-10）
    - projects/p03-vehicle-monitoring/scripts/verify.sh（现有 CH 权威出口）
    - examples/e03-state/src/main/java/com/flywhl/flinklab/e03/C7BroadcastRuleJob.java（Broadcast 读写纪律参考，不必起 MiniCluster）
  </read_first>
  <behavior>
    - PatternActivationGateTest（轻量：测 Gate 的静态/可测辅助，或直接实例化 Gate 的决策方法；不必 MiniCluster）：(1) 空 state ≡ 仅放行 HARSH_THEN_FAULT（D-06）；(2) version 较低的控制消息不覆盖较高 version（D-05）；(3) activePatterns 不含某 patternId 时该 AlertEvent 被丢弃（D-07/D-09）
    - verify.sh：PATTERN_ID 默认 HARSH_THEN_FAULT；MIN_COUNT 默认 1；CH 查询增加 AND pattern_id='…'；PATTERN_ID 必须经白名单校验（仅允许 HARSH_THEN_FAULT|TRIPLE_HARSH|DTC_PAIR），非法则 exit 1；Kafka 仍仅 diag（D-08/D-10）
  </behavior>
  <action>
    新建 PatternActivationGateTest，断言引用 PatternActivationGate / PatternControlMessage / PatternIds 等尚未实现符号以保持 RED。扩展 verify.sh：用环境变量 PATTERN_ID/MIN_COUNT；固定白名单 case 分支拼查询（禁止把未校验用户字符串直接拼进 SQL）。不改 docker-compose、不实现 Gate 生产类。本任务完成后 Gate 测试必 RED；verify.sh 语法必须 bash -n 通过。
  </action>
  <verify>
    <automated>bash -n projects/p03-vehicle-monitoring/scripts/verify.sh &amp;&amp; grep -q 'PATTERN_ID' projects/p03-vehicle-monitoring/scripts/verify.sh &amp;&amp; cd projects/p03-vehicle-monitoring &amp;&amp; mvn -q -Dtest=PatternActivationGateTest test; ec=$?; test $ec -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - PatternActivationGateTest.java 存在且含 HARSH_THEN_FAULT 与 version（或「单调」语义断言）
    - verify.sh 含 PATTERN_ID 与 pattern_id；含白名单三 ID 之一的校验逻辑
    - verify.sh 仍以 ClickHouse count 为唯一 exit 0 条件（Kafka 不可单独放行）
    - PatternActivationGateTest surefire 退出码 ≠ 0（RED）
  </acceptance_criteria>
  <done>VEH-04 门控 RED 夹具与 verify PATTERN_ID 骨架就位。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 宿主机 env → verify.sh SQL | PATTERN_ID 进入 ClickHouse 查询字符串 |
| 单测 → 未来控制 JSON | 门控将信任 control topic 消息体 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | verify.sh PATTERN_ID | mitigate | 仅允许白名单三常量拼入查询；非法 PATTERN_ID 立即 exit 1（本计划落地） |
| T-02-02 | Tampering | 未来 control JSON / patternId | mitigate | Wave 0 测试锁定「未知 patternId 不放行」期望；生产白名单求交在 02-02 |
| T-02-03 | Information Disclosure | CH 密码 | accept | 沿用 docker/.env flinklab/flinklab123；不新增密钥文件 |
| T-02-SC | Tampering | Maven/PyPI 安装 | accept | 本 Wave 无新包；仅 SSOT 已 Approved 坐标 |
</threat_model>

<verification>
- `mvn -q -Dtest=PatternRegistryWithinTest,TripleHarshPatternTest,DtcPairPatternTest,PatternActivationGateTest test` → 非 0
- `bash -n scripts/verify.sh` 且含 PATTERN_ID 白名单
</verification>

<success_criteria>
- Wave 0 四个测试类可发现且为 RED
- verify.sh 已支持白名单 PATTERN_ID，CH 仍为唯一权威出口
- 不实现 PatternRegistry / 三工厂 / Gate / 作业改线（留给后续 Wave）
</success_criteria>

<output>
Create `.planning/phases/02-p03-broadcast/02-00-SUMMARY.md` when done
</output>
