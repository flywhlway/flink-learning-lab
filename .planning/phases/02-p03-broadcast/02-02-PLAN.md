---
phase: 02-p03-broadcast
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/model/PatternControlMessage.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/PatternActivationGate.java
  - projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/PatternActivationGateTest.java
autonomous: true
requirements:
  - VEH-04
user_setup: []

must_haves:
  truths:
    - "控制消息模型含 activePatterns+version；门控仅 processBroadcastElement 写 Broadcast State 且内容来自消息本身；version 单调更新（D-04/D-05）"
    - "空 Broadcast State 时默认激活 [HARSH_THEN_FAULT]；未激活 patternId 被过滤（D-06/D-07）"
    - "PatternActivationGate 为非 keyed BroadcastProcessFunction；MATCH 与 TIMEOUT 同逻辑受激活集门控（D-09；RESOLVED Q1）"
    - "非法/未知 patternId 与超长 activePatterns 被忽略或与白名单求交（ASVS V5）"
  artifacts:
    - path: "projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/PatternActivationGate.java"
      provides: "BroadcastProcessFunction 出口门控（D-05/D-07）"
      contains: "processBroadcastElement"
    - path: "projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/model/PatternControlMessage.java"
      provides: "控制 JSON 模型 activePatterns+version（D-04）"
      contains: "version"
    - path: "projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/PatternActivationGateTest.java"
      provides: "默认集 / version / 过滤 GREEN（D-05/D-06）"
      contains: "HARSH_THEN_FAULT"
  key_links:
    - from: "PatternControlMessage"
      to: "PatternActivationGate Broadcast State"
      via: "processBroadcastElement version 单调 put"
      pattern: "processBroadcastElement|version|activePatterns"
    - from: "AlertEvent.patternId"
      to: "Gate processElement collect"
      via: "activePatterns.contains(patternId) 或默认 HARSH_THEN_FAULT"
      pattern: "patternId|DEFAULT|HARSH_THEN_FAULT"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 维护恰好 3 条可评审 CEP 模式库并通过 Kafka Broadcast 控制消息切换预编译激活集、在 ClickHouse 按 pattern_id 断言匹配变化，**so that** 无 within 不得合入且不必重启作业即可演示开源 CEP 动态化路线。

<objective>
垂直切片 VEH-04（门控半段）：PatternControlMessage + PatternActivationGate 单测 GREEN，锁定非 keyed BroadcastProcessFunction 与默认激活集/version 单调（D-04–D-06）。

Purpose: 先交付可测门控核心，作业接线 / DDL / compose 留给 02-02b，避免单计划文件膨胀。
Output: PatternActivationGateTest GREEN；门控与控制消息模型可被后续作业接线引用。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-p03-broadcast/02-CONTEXT.md
@.planning/phases/02-p03-broadcast/02-RESEARCH.md
@.planning/phases/02-p03-broadcast/02-PATTERNS.md
@.planning/phases/02-p03-broadcast/02-01-SUMMARY.md
@examples/e03-state/src/main/java/com/flywhl/flinklab/e03/C7BroadcastRuleJob.java
@projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/PatternIds.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PatternControlMessage + PatternActivationGate 单测 GREEN</name>
  <files>projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/model/PatternControlMessage.java, projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/PatternActivationGate.java, projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/PatternActivationGateTest.java</files>
  <read_first>
    - .planning/phases/02-p03-broadcast/02-RESEARCH.md（门控核心逻辑、BroadcastProcessFunction 推荐、version 单调、Open Questions RESOLVED Q1）
    - .planning/phases/02-p03-broadcast/02-PATTERNS.md（PatternActivationGate Apply）
    - examples/e03-state/src/main/java/com/flywhl/flinklab/e03/C7BroadcastRuleJob.java（仅写 processBroadcastElement）
    - .planning/phases/02-p03-broadcast/02-CONTEXT.md（D-04/D-05/D-06）
    - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/PatternIds.java
  </read_first>
  <behavior>
    - PatternControlMessage：public List/String[] activePatterns + long version；无参构造；JSON 字段名 activePatterns/version（per D-04）
    - PatternActivationGate 继承 BroadcastProcessFunction&lt;AlertEvent, PatternControlMessage, AlertEvent&gt;（Discretion：非 keyed，per RESEARCH RESOLVED Q1 / A1）
    - processBroadcastElement：仅当 msg.version &gt; storedVersion 才 put；activePatterns 与 PatternIds 白名单求交；长度上限 ≤3；坏消息跳过；禁止 System.currentTimeMillis/随机写入 state（per D-05）
    - processElement：只读 Broadcast State；空/空列表 ≡ DEFAULT {HARSH_THEN_FAULT}（per D-06）；仅 patternId ∈ active 才 collect（MATCH 与 TIMEOUT 同逻辑，per D-09）
    - PatternActivationGateTest 全部 GREEN（可用包内可见辅助方法测决策，避免 MiniCluster）
  </behavior>
  <action>
    实现控制消息模型与门控算子，把 Wave 0 PatternActivationGateTest 转绿。MapStateDescriptor 名建议 p03-active-patterns，state key 如 active。禁止在 processElement 写 Broadcast State。禁止文件热读/作业参数作为激活集来源（D-04）。不在本计划改 VehicleAlertJob / DDL / compose（留给 02-02b）。若 AlertEvent 尚无 patternId 字段，Gate 测试可用临时构造或最小 stub 字段访问——完整 AlertEvent.patternId 落地属 02-02b。
  </action>
  <verify>
    <automated>cd projects/p03-vehicle-monitoring &amp;&amp; mvn -q -Dtest=PatternActivationGateTest test</automated>
  </verify>
  <acceptance_criteria>
    - PatternActivationGate.java 含 processBroadcastElement 与 processElement
    - 源码对 version 比较（&gt; stored）可见；默认 HARSH_THEN_FAULT 可见
    - PatternActivationGate 为 BroadcastProcessFunction（非 KeyedBroadcastProcessFunction）
    - PatternActivationGateTest surefire exit 0
    - 无 System.currentTimeMillis / Random 写入 Broadcast State 的路径
  </acceptance_criteria>
  <done>门控确定性与默认集单测 GREEN（D-04/D-05/D-06；RESOLVED Q1）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka control topic → Broadcast State | 未认证 lab 网络上的控制 JSON 可改变激活集（接线在 02-02b） |
| 单测 → 控制 JSON 模型 | 测试夹具模拟坏消息 / 未知 patternId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | PatternControlMessage / Gate | mitigate | 白名单求交 PatternIds；activePatterns 长度≤3；坏消息跳过；version 单调防乱序分叉（D-05） |
| T-02-08 | Elevation of Privilege | control topic 无 ACL | accept | 学习环境本地 Compose；README 标明勿对公网暴露（本 Phase 不做 ACL） |
| T-02-SC | Tampering | 依赖安装 | accept | 无新 Maven/PyPI 包 |
</threat_model>

<verification>
- PatternActivationGateTest GREEN
- Gate 为非 keyed BroadcastProcessFunction；默认集与 version 单调可观察
</verification>

<success_criteria>
- 控制消息模型 + 门控算子单测 GREEN（D-04/D-05/D-06）
- 三 CEP 作业接线 / pattern_id DDL / control topic 留给 02-02b
</success_criteria>

<output>
Create `.planning/phases/02-p03-broadcast/02-02-SUMMARY.md` when done
</output>
