---
phase: 03-p03
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py
  - projects/p03-vehicle-monitoring/scripts/loadtest.sh
  - projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh
  - projects/p03-vehicle-monitoring/docs/baseline.md
  - projects/p03-vehicle-monitoring/Makefile
  - projects/p03-vehicle-monitoring/README.md
autonomous: true
requirements:
  - VEH-06
user_setup: []

must_haves:
  truths:
    - "维护者可用 gen --rate/--duration（或等价）驱动恒定负载，且不引入 k6/JMeter（D-07）"
    - "loadtest.sh 在 OrbStack 跑通并写出含环境快照/负载/指标表/结论的 docs/baseline.md（D-06/D-08）"
    - "drill_watermark_stall.sh 完成停滞可观察→恢复后 MATCH 继续，失败非 0；用冻结 eventTime 而非仅空闲分区（D-09/D-10）"
    - "Makefile/README 提供 loadtest 与 drill-watermark 一键入口（D-11）"
  artifacts:
    - path: "projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py"
      provides: "--rate/--duration 与 frozen-eventTime 能力（D-07/D-10）"
      contains: "rate"
    - path: "projects/p03-vehicle-monitoring/scripts/loadtest.sh"
      provides: "压测编排 + baseline 写入（D-06）"
      contains: "baseline.md"
    - path: "projects/p03-vehicle-monitoring/docs/baseline.md"
      provides: "OrbStack arm64 实测数字表"
      contains: "环境"
    - path: "projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh"
      provides: "watermark 停滞两阶段演练（D-09/D-10）"
      contains: "recover"
  key_links:
    - from: "loadtest.sh"
      to: "Prometheus /api/v1/query"
      via: "curl PromQL for lag/ckpt/restarts"
      pattern: "api/v1/query|currentEmitEventTimeLag|lastCheckpointDuration"
    - from: "drill_watermark_stall.sh"
      to: "scripts/verify.sh"
      via: "recover 后 PATTERN_ID CH 权威断言"
      pattern: "verify.sh|PATTERN_ID"
    - from: "gen_vehicle_events.py"
      to: "Kafka vehicle.events"
      via: "confluent-kafka produce localhost:9094"
      pattern: "rate|duration|frozen"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 复现 Grafana 双数据源大盘、执行 watermark 停滞与压测 baseline、并打开架构/ADR/简历陈述页，**so that** p03 达到 PHASES P4 单项目完成态（可简历陈述、可一键复现、可压测演练）。

<objective>
VEH-06 垂直切片：扩展造数速率模式 → loadtest 写 baseline → watermark 冻结心跳停滞演练可执行。

Purpose: 用可脚本化证据证明 p03 可压测、watermark 停滞可观察可恢复，数字仅来自 OrbStack 实测。
Output: gen --rate/--duration/--frozen-event-time（或等价）；loadtest.sh GREEN + docs/baseline.md；drill_watermark_stall.sh GREEN；Makefile/README 入口。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-p03/03-CONTEXT.md
@.planning/phases/03-p03/03-RESEARCH.md
@.planning/phases/03-p03/03-PATTERNS.md
@.planning/phases/03-p03/03-01-SUMMARY.md
@projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py
@scripts/gen_events.py
@projects/p03-vehicle-monitoring/scripts/verify.sh
@benchmark/README.md
@monitoring/README.md
@docs/02-time-window/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: gen --rate/--duration + loadtest → baseline.md</name>
  <files>projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py, projects/p03-vehicle-monitoring/scripts/loadtest.sh, projects/p03-vehicle-monitoring/docs/baseline.md, projects/p03-vehicle-monitoring/Makefile, projects/p03-vehicle-monitoring/README.md</files>
  <read_first>
    - projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py（scenario、_tail_heartbeats、uv header、默认 localhost:9094）
    - scripts/gen_events.py（--eps 恒定速率循环）
    - benchmark/README.md（环境快照→负载→指标表→结论）
    - monitoring/README.md（五指标名）
    - .planning/phases/03-p03/03-CONTEXT.md（D-06/D-07/D-08/D-11）
    - .planning/phases/03-p03/03-RESEARCH.md（Pattern 6、建议 100eps×120s warmup 45s）
  </read_first>
  <behavior>
    - gen_vehicle_events.py：新增 --rate（或 --eps 别名）与 --duration 秒；与纯 --scenario 互斥或组合规则在 --help 写清；速率模式发出 advancing eventTime 的 HEARTBEAT/HARSH 混合；保留现有 scenario 与尾心跳（per D-07）
    - 同步提供冻结 eventTime 能力（--frozen-event-time 或子命令），供 Task 2 drill 调用（per D-10 / RESEARCH Pattern 5）
    - loadtest.sh：热身 30–60s（写入 baseline 声明丢弃）；调用 gen rate/duration；刮取 Prometheus（currentEmitEventTimeLag、lastCheckpointDuration、numRestarts 等，job_name=~p03.*）；可选 CH window_metrics/alerts 计数；生成 docs/baseline.md 四段结构；依赖不可达 exit 1（per D-06/D-08）
    - 默认 bootstrap 仅 localhost:9094；禁止新镜像 k6/JMeter
    - Makefile loadtest 目标；README 验证/演练节链接
  </behavior>
  <action>
    扩展 gen_vehicle_events.py 速率模式与冻结时间戳参数（实现细节对齐 gen_events.py 循环，但事件 schema 保持 VehicleEvent JSON）。将 Wave 0 loadtest.sh 骨架替换为完整编排，OrbStack 实测后写入 baseline.md——数字必须来自本次运行，禁止编造 SLA。交叉引用 benchmark/README.md 方法论，不实现全矩阵。eps/时长由 executor 在可稳定跑通前提下选定并写入报告。
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py').read())" &amp;&amp; grep -E 'rate|eps' projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py | grep -v '^#' | head -1 &amp;&amp; grep -q duration projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py &amp;&amp; bash -n projects/p03-vehicle-monitoring/scripts/loadtest.sh &amp;&amp; grep -q baseline.md projects/p03-vehicle-monitoring/scripts/loadtest.sh &amp;&amp; grep -q loadtest projects/p03-vehicle-monitoring/Makefile</automated>
  </verify>
  <acceptance_criteria>
    - gen_vehicle_events.py 含 --rate 或 --eps，且含 --duration
    - gen 含 frozen 或 freeze 相关参数/模式供 drill 使用
    - loadtest.sh 含 api/v1/query 或 localhost:9090 与 baseline.md
    - Makefile 含 loadtest
    - 本计划完成时 docs/baseline.md 存在且含「环境」与「指标」类标题，并含至少一行实测数字（非占位 TBD）
  </acceptance_criteria>
  <done>p03 项目级压测可执行并留下 baseline 数字（VEH-06 / D-06/D-07/D-08）。</done>
</task>

<task type="auto">
  <name>Task 2: drill_watermark_stall 停滞→恢复断言</name>
  <files>projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh, projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py, projects/p03-vehicle-monitoring/Makefile, projects/p03-vehicle-monitoring/README.md</files>
  <read_first>
    - .planning/phases/03-p03/03-RESEARCH.md（Pattern 5：冻结 eventTime HEARTBEAT ≥45s；勿依赖 REST /watermarks；Prom lag + CH MATCH）
    - .planning/phases/03-p03/03-CONTEXT.md（D-09/D-10/D-11）
    - docs/02-time-window/README.md（idleness 语义）
    - projects/p03-vehicle-monitoring/scripts/verify.sh（CH PATTERN_ID 权威）
    - projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py（本计划 Task 1 新增冻结模式）
  </read_first>
  <behavior>
    - 恰好 2 条 MVP 演练入口之一：本脚本实现 watermark 停滞（另一条即 loadtest）（per D-09）
    - Stall：部分 CEP 序列（如仅 HARSH）+ 冻结 eventTime=T0 的 HEARTBEAT 涓流 ≥45s；断言 MATCH count 不增；Prometheus currentEmitEventTimeLag 上升或持平上升趋势（per D-10）
    - Recover：完成 DTC/scenario + advancing _tail_heartbeats；PATTERN_ID=HARSH_THEN_FAULT（或文档固定值）调用 verify.sh 须绿
    - 禁止仅靠空闲分区演示停滞；禁止以 Flink REST /watermarks 为唯一断言（可空）
    - UI Watermarks 列作为 README checklist 副证（human-check）
    - Makefile drill-watermark；失败非 0
  </behavior>
  <action>
    将 Wave 0 drill 骨架替换为可执行两阶段剧本，调用 gen 冻结/恢复能力与 verify.sh。交叉引用 docs/02-time-window。可选附录（杀 TM）仅文档提及不挡 exit 0。OrbStack 上脚本须 exit 0。README「验证/演练」节给出一键命令与预期观察。
  </action>
  <verify>
    <automated>bash -n projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh &amp;&amp; grep -qE 'frozen|freeze|stall' projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh &amp;&amp; grep -q verify.sh projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh &amp;&amp; grep -q drill-watermark projects/p03-vehicle-monitoring/Makefile &amp;&amp; grep -q drill_watermark_stall projects/p03-vehicle-monitoring/README.md</automated>
  </verify>
  <acceptance_criteria>
    - drill_watermark_stall.sh 含 stall 与 recover 两阶段逻辑（命令或函数名可检索）
    - 脚本调用 verify.sh 或显式 CH MATCH 查询作恢复断言
    - 脚本提及 frozen/freeze 或固定 eventTime，而非仅 sleep 等空闲分区
    - Makefile 含 drill-watermark；README 含演练入口
  </acceptance_criteria>
  <done>watermark 停滞演练可执行且恢复后 MATCH 可断言（VEH-06 / D-09/D-10/D-11）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loadtest → Kafka | 高压写入 lab 集群 |
| drill → Prometheus/CH | 读取指标与告警计数 |
| gen CLI args → JSON events | 用户可控 vin/rate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Denial of Service | loadtest/gen | mitigate | 默认 bootstrap localhost:9094；duration/rate 有上限或文档推荐值；禁止默认云端 broker |
| T-03-08 | Tampering | drill 恢复后 verify | mitigate | 继续走 verify.sh PATTERN_ID 白名单 + CH 权威；Kafka 不放行 |
| T-03-09 | Information Disclosure | baseline.md | accept | 仅含 lab 指标与并行度快照，无密钥 |
</threat_model>

<verification>
- bash -n loadtest.sh drill_watermark_stall.sh；Makefile 目标存在
- OrbStack：make loadtest → docs/baseline.md 有实测表；make drill-watermark → exit 0
- human-check：stall 阶段 Flink UI Watermarks 列停滞笔记/截图可选
</verification>

<success_criteria>
- VEH-06：两条演练入口可执行；baseline 含 OrbStack 数字；冻结 eventTime 停滞剧本可绿
- 无 k6/JMeter；无编造 SLA
</success_criteria>

<output>
Create `.planning/phases/03-p03/03-02-SUMMARY.md` when done
</output>
