---
phase: 03-p03
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/window/EventCountAggTest.java
  - projects/p03-vehicle-monitoring/scripts/verify_dashboard.sh
  - projects/p03-vehicle-monitoring/scripts/loadtest.sh
  - projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh
  - projects/p03-vehicle-monitoring/sql/clickhouse_window_metrics.sql
  - projects/p03-vehicle-monitoring/monitoring/dashboards/.gitkeep
  - docker/config/grafana/provisioning/datasources/clickhouse.yml
  - docker/config/grafana/provisioning/dashboards/dashboards.yml
  - docker/docker-compose.yml
  - README.md
autonomous: true
requirements:
  - VEH-05
  - VEH-06
  - VEH-07
user_setup: []

must_haves:
  truths:
    - "EventCountAggTest 可被 surefire 发现且当前 RED（EventCountAgg 尚未交付）— 建立窗口累加器反馈环"
    - "verify_dashboard.sh 在缺少 dashboard JSON 或 Grafana API 不健康时非 0 退出（D-15）"
    - "loadtest.sh 与 drill_watermark_stall.sh 可 bash -n，且当前以非 0 失败态骨架存在（D-11）"
    - "clickhouse_window_metrics.sql 与 Grafana ClickHouse DS / dashboards provider 骨架落仓；根 README 版本矩阵已登记 grafana-clickhouse-datasource（ENG-01）"
  artifacts:
    - path: "projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/window/EventCountAggTest.java"
      provides: "窗口累加器 RED 夹具（event/harsh/dtc 计数）"
      contains: "EventCountAgg"
    - path: "projects/p03-vehicle-monitoring/scripts/verify_dashboard.sh"
      provides: "大盘 JSON + Grafana API 门禁骨架（D-15）"
      contains: "p03-vehicle"
    - path: "projects/p03-vehicle-monitoring/scripts/loadtest.sh"
      provides: "压测入口骨架（D-06/D-11）"
      contains: "baseline"
    - path: "projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh"
      provides: "watermark 停滞演练入口骨架（D-09/D-10）"
      contains: "stall"
    - path: "projects/p03-vehicle-monitoring/sql/clickhouse_window_metrics.sql"
      provides: "vehicle_window_metrics DDL（D-02）"
      contains: "vehicle_window_metrics"
  key_links:
    - from: "EventCountAggTest"
      to: "EventCountAgg"
      via: "JUnit 断言 createAccumulator/add/merge"
      pattern: "EventCountAgg"
    - from: "verify_dashboard.sh"
      to: "Grafana /api/search + dashboard JSON path"
      via: "curl -sf + test -s"
      pattern: "api/search|p03-vehicle-overview"
    - from: "docker-compose grafana"
      to: "GF_INSTALL_PLUGINS + dashboards mount"
      via: "compose env/volumes stubs"
      pattern: "GF_INSTALL_PLUGINS|dashboards/p03"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 复现 Grafana 双数据源大盘、执行 watermark 停滞与压测 baseline、并打开架构/ADR/简历陈述页，**so that** p03 达到 PHASES P4 单项目完成态（可简历陈述、可一键复现、可压测演练）。

<objective>
Wave 0（Nyquist）：落地 VEH-05/06/07 失败态脚本/单测夹具与 DDL/Grafana provisioning 骨架，建立 RED→GREEN 反馈环。

Purpose: 03-VALIDATION.md 要求先有 verify_dashboard / loadtest / drill 骨架、EventCountAggTest、window metrics DDL 与 Grafana provider 钩子；否则后续垂直切片无法自动化门禁。
Output: RED 单测 + 三脚本骨架（非 0）+ SQL DDL 文件 + Grafana DS/provider YAML + compose 挂载/插件 env 钩子 + 根 README 插件登记；不实现 VehicleWindowMetricsJob / 完整 dashboard JSON 面板 / baseline 实测数字。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-p03/03-CONTEXT.md
@.planning/phases/03-p03/03-RESEARCH.md
@.planning/phases/03-p03/03-PATTERNS.md
@.planning/phases/03-p03/03-VALIDATION.md
@projects/p03-vehicle-monitoring/scripts/verify.sh
@projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/PatternActivationGateTest.java
@docker/config/grafana/provisioning/datasources/datasources.yml
@docker/docker-compose.yml
@README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED 夹具 — EventCountAggTest + verify_dashboard.sh</name>
  <files>projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/window/EventCountAggTest.java, projects/p03-vehicle-monitoring/scripts/verify_dashboard.sh</files>
  <read_first>
    - projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/PatternActivationGateTest.java（轻量 JUnit 无 MiniCluster 风格）
    - projects/p03-vehicle-monitoring/scripts/verify.sh（set -euo pipefail、FAIL 非 0、CH 权威纪律）
    - .planning/phases/03-p03/03-RESEARCH.md（Validation Architecture Wave 0、Grafana API 示例、EventCountAgg 行为）
    - .planning/phases/03-p03/03-CONTEXT.md（D-01/D-03/D-15）
    - .planning/phases/03-p03/03-VALIDATION.md（Wave 0 Requirements）
  </read_first>
  <behavior>
    - EventCountAggTest：对尚未存在的 EventCountAgg（或等价类名，须与后续 03-01 一致）断言 createAccumulator 零值；add(HEARTBEAT) 只增 eventCount；add(HARSH_ACCEL) 增 eventCount+harshCount；add(DTC) 增 eventCount+dtcCount；merge 相加（per D-02 / RESEARCH Pattern 3）
    - verify_dashboard.sh：set -euo pipefail；断言 monitoring/dashboards/p03-vehicle-overview.json 非空；curl Grafana /api/datasources（admin/flinklab）与 /api/search?query=p03；缺文件或 API 失败则 exit 1（per D-15）
    - 本任务不实现 EventCountAgg 生产类、不写完整 dashboard JSON（留给 03-01）
  </behavior>
  <action>
    新建 EventCountAggTest，引用尚未存在的 com.flywhl.flinklab.p03.window.EventCountAgg 符号，故意让 mvn -Dtest=EventCountAggTest 失败（RED）。新建 scripts/verify_dashboard.sh：默认 GRAFANA_URL=http://localhost:3000、凭据 admin/flinklab；先 test -s 仓库内 p03-vehicle-overview.json，再 curl -sf -u 查 datasources 与 search；当前无 JSON 时脚本必须 exit 1。禁止把测试写成永远通过的空壳。禁止修改 VehicleAlertJob。
  </action>
  <verify>
    <automated>bash -n projects/p03-vehicle-monitoring/scripts/verify_dashboard.sh &amp;&amp; cd projects/p03-vehicle-monitoring &amp;&amp; mvn -q -Dtest=EventCountAggTest test; ec=$?; test $ec -ne 0 &amp;&amp; (! bash scripts/verify_dashboard.sh)</automated>
  </verify>
  <acceptance_criteria>
    - EventCountAggTest.java 存在且含 EventCountAgg、HARSH_ACCEL、DTC 或 harshCount/dtcCount 断言
    - verify_dashboard.sh 含 set -euo pipefail、p03-vehicle-overview.json、api/datasources 或 api/search
    - bash -n verify_dashboard.sh 通过
    - mvn -Dtest=EventCountAggTest 退出码 ≠ 0（RED）
    - 在无 dashboard JSON 时 verify_dashboard.sh 退出码 ≠ 0
  </acceptance_criteria>
  <done>VEH-05 Nyquist RED：累加器单测与 verify_dashboard 门禁骨架就位（per D-15）。</done>
</task>

<task type="auto">
  <name>Task 2: RED 骨架 — loadtest.sh + drill_watermark_stall.sh</name>
  <files>projects/p03-vehicle-monitoring/scripts/loadtest.sh, projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh</files>
  <read_first>
    - projects/p03-vehicle-monitoring/scripts/verify.sh（失败非 0 契约）
    - .planning/phases/03-p03/03-CONTEXT.md（D-06/D-09/D-10/D-11）
    - .planning/phases/03-p03/03-RESEARCH.md（Pattern 5 frozen-eventTime；Pattern 6 Prom scrape；Wave 0 Gaps）
    - projects/p03-vehicle-monitoring/Makefile（verify-switch 等待环风格，供注释/后续对齐）
  </read_first>
  <behavior>
    - loadtest.sh：set -euo pipefail；声明将写入 docs/baseline.md；当前若无完整压测实现则明确 echo FAIL 并 exit 1（骨架允许检测依赖：Prometheus :9090 不可达也 exit 1）（per D-06/D-11）
    - drill_watermark_stall.sh：set -euo pipefail；注释或 echo 标明 stall→recover 两阶段与 frozen-eventTime（禁止仅 idle 分区）；当前未实现完整演练则 exit 1（per D-09/D-10）
    - 两脚本均可 bash -n；禁止引入 k6/JMeter（D-07）
  </behavior>
  <action>
    新建 loadtest.sh 与 drill_watermark_stall.sh 于 projects/p03-vehicle-monitoring/scripts/。骨架须可执行且默认非 0（例如缺少 --implemented 标志或直接 FAIL: Wave0 stub）。在文件头注释写明最终职责：loadtest 调 gen --rate/--duration、刮 Prometheus、写 baseline.md；drill 用冻结 eventTime HEARTBEAT ≥45s 再 recover 并用 PATTERN_ID=… verify.sh。禁止在本任务实现完整压测/演练逻辑与实测数字。禁止修改 VehicleAlertJob。
  </action>
  <verify>
    <automated>bash -n projects/p03-vehicle-monitoring/scripts/loadtest.sh &amp;&amp; bash -n projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh &amp;&amp; (! bash projects/p03-vehicle-monitoring/scripts/loadtest.sh) &amp;&amp; (! bash projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh)</automated>
  </verify>
  <acceptance_criteria>
    - 两脚本存在且含 set -euo pipefail
    - loadtest.sh 源码含 baseline 字样
    - drill_watermark_stall.sh 源码含 stall 或 watermark 或 frozen
    - bash -n 均通过；直接执行两脚本退出码均 ≠ 0
  </acceptance_criteria>
  <done>VEH-06 Nyquist RED：压测与 watermark 演练入口骨架就位（per D-11）。</done>
</task>

<task type="auto">
  <name>Task 3: 骨架 — window DDL + Grafana provisioning 钩子 + SSOT 登记</name>
  <files>projects/p03-vehicle-monitoring/sql/clickhouse_window_metrics.sql, projects/p03-vehicle-monitoring/monitoring/dashboards/.gitkeep, docker/config/grafana/provisioning/datasources/clickhouse.yml, docker/config/grafana/provisioning/dashboards/dashboards.yml, docker/docker-compose.yml, README.md</files>
  <read_first>
    - projects/p03-vehicle-monitoring/sql/clickhouse_alerts.sql（DDL 风格）
    - docker/docker-compose.yml（grafana 服务 volumes/env；p03-init 两次 POST 模式）
    - docker/config/grafana/provisioning/datasources/datasources.yml
    - .planning/phases/03-p03/03-RESEARCH.md（Pattern 1–2 YAML shape、DDL 列、compose 挂载）
    - .planning/phases/03-p03/03-CONTEXT.md（D-01/D-02/D-03）
    - README.md（版本矩阵表位置）
  </read_first>
  <behavior>
    - clickhouse_window_metrics.sql：CREATE TABLE IF NOT EXISTS flinklab.vehicle_window_metrics，列含 vin、window_start、window_end、event_count、harsh_count、dtc_count、ingest_time（per RESEARCH / D-02）
    - clickhouse.yml：type grafana-clickhouse-datasource，uid p03-clickhouse，HTTP host clickhouse port 8123，defaultDatabase flinklab（per D-01）
    - dashboards.yml：file provider path /var/lib/grafana/dashboards/p03，folder p03（per D-03 / RESEARCH Pattern 1）
    - compose grafana：增加 GF_INSTALL_PLUGINS=grafana-clickhouse-datasource；挂载 projects/.../monitoring/dashboards → /var/lib/grafana/dashboards/p03:ro；p03-init 增加第三 volume + wget --post-file window metrics DDL（与 alerts 分 POST）（per D-02/D-03）
    - 根 README 版本矩阵登记 grafana-clickhouse-datasource（via GF_INSTALL_PLUGINS）；禁止未登记先用
    - 本任务不写完整 p03-vehicle-overview.json 面板（仅 .gitkeep 或占位说明）；default make up 不加 --profile p03
  </behavior>
  <action>
    按 RESEARCH Pattern 1–2 与 PATTERNS.md 写入 DDL、clickhouse.yml、dashboards.yml；改 docker-compose.yml grafana 与 p03-init（第三 POST）。在 projects/p03-vehicle-monitoring/monitoring/dashboards/ 放 .gitkeep（勿伪造完整业务面板 JSON）。根 README 版本矩阵新增插件一行。禁止把 metrics DDL 并进 alerts 单文件多语句。禁止改 VehicleAlertJob CEP 图。compose config 须仍可通过 docker compose config 校验。
  </action>
  <verify>
    <automated>test -f projects/p03-vehicle-monitoring/sql/clickhouse_window_metrics.sql &amp;&amp; grep -q vehicle_window_metrics projects/p03-vehicle-monitoring/sql/clickhouse_window_metrics.sql &amp;&amp; grep -q grafana-clickhouse-datasource docker/config/grafana/provisioning/datasources/clickhouse.yml &amp;&amp; grep -q dashboards/p03 docker/config/grafana/provisioning/dashboards/dashboards.yml &amp;&amp; grep -q GF_INSTALL_PLUGINS docker/docker-compose.yml &amp;&amp; grep -q clickhouse_window_metrics.sql docker/docker-compose.yml &amp;&amp; grep -q grafana-clickhouse-datasource README.md &amp;&amp; cd docker &amp;&amp; docker compose config -q</automated>
  </verify>
  <acceptance_criteria>
    - SQL 文件含 CREATE TABLE 与 vehicle_window_metrics 及 event_count/harsh_count/dtc_count
    - clickhouse.yml 含 uid p03-clickhouse 与 port 8123
    - dashboards.yml 含 path /var/lib/grafana/dashboards/p03
    - docker-compose.yml 含 GF_INSTALL_PLUGINS 与 window metrics DDL 挂载/POST
    - 根 README 版本矩阵含 grafana-clickhouse-datasource
    - docker compose config -q 成功
  </acceptance_criteria>
  <done>VEH-05/ENG 骨架：DDL + Grafana 双 DS 钩子 + SSOT 登记就位（per D-01/D-02/D-03）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 宿主机 → Grafana API | verify_dashboard 使用 lab 默认 basic auth |
| Grafana → ClickHouse | 未来业务面板以配置用户执行 SQL |
| compose env → GF_INSTALL_PLUGINS | 首次启动从插件目录下载 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | clickhouse.yml secureJsonData | mitigate | 仅 lab 默认密码；与 docker/.env 对齐；文档注明勿用于生产；优先后续可用 ${CH_PASSWORD} |
| T-03-02 | Tampering | verify_dashboard / 脚本路径 | mitigate | 固定仓库相对路径与 localhost 默认；禁止接受任意远程 URL 作为默认 |
| T-03-03 | Denial of Service | loadtest 骨架 | accept | Wave 0 故意非 0；完整压测限制 localhost:9094（03-02） |
| T-03-SC | Tampering | GF_INSTALL_PLUGINS 插件 | mitigate | 仅官方 grafana-clickhouse-datasource；已在 README SSOT 登记；无 npm/pip 新包 |
</threat_model>

<verification>
- EventCountAggTest surefire ≠ 0；verify_dashboard / loadtest / drill 直接执行 ≠ 0
- bash -n 三脚本通过；compose config -q 通过
- DDL + Grafana YAML + README 插件行存在
</verification>

<success_criteria>
- Wave 0 Nyquist 夹具全部可运行且为失败态（或骨架非 0）
- provisioning/DDL 钩子落仓且不污染 default make up 的 profile 标志
- 不实现窗口作业主类、完整大盘面板、baseline 实测（留给 03-01/03-02）
</success_criteria>

<output>
Create `.planning/phases/03-p03/03-00-SUMMARY.md` when done
</output>
