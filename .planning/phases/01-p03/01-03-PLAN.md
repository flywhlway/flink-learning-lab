---
phase: 01-p03
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py
  - projects/p03-vehicle-monitoring/scripts/verify.sh
  - projects/p03-vehicle-monitoring/Makefile
  - projects/p03-vehicle-monitoring/README.md
  - docs/README.md
  - CHANGELOG.md
  - PHASES.md
autonomous: false
requirements:
  - VEH-02
user_setup:
  - service: orbstack-base
    why: "端到端 verify 需要 docker 基座已 make up，且已 make up-p03；作业需已 submit"
    env_vars: []
    dashboard_config:
      - task: "确保 OrbStack 上下文下 kafka/flink/clickhouse 健康"
        location: "cd docker && make up && make init && make up-p03"

must_haves:
  truths:
    - "造数注入 harsh→fault 可判定序列后，ClickHouse flinklab.vehicle_alerts 中 MATCH 行数 >= 1 或 Kafka vehicle.alerts 有消息"
    - "CEP 超时半成品经 Side Output 进入可观察通道（同表 alert_type=TIMEOUT 或同 topic）"
    - "verify.sh 在有数据时 exit 0；故意空库/清空后 exit ≠ 0"
    - "项目 README 八段式可按路径打开；docs/README 模块 15 指向本项目（ADR 完整内容延 Phase 3）"
  artifacts:
    - path: "projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py"
      provides: "可判定 HARSH_ACCEL→DTC 注入"
      contains: "HARSH_ACCEL"
    - path: "projects/p03-vehicle-monitoring/scripts/verify.sh"
      provides: "断言型验收"
      contains: "exit 1"
    - path: "projects/p03-vehicle-monitoring/README.md"
      provides: "八段式文档"
      min_lines: 80
      contains: "背景"
  key_links:
    - from: "gen_vehicle_events.py"
      to: "verify.sh"
      via: "可判定序列驱动断言"
      pattern: "HARSH_ACCEL|vehicle_alerts"
    - from: "make submit + make gen"
      to: "scripts/verify.sh"
      via: "OrbStack e2e 串行采样"
      pattern: "submit|gen|verify"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 用独立 p03 compose profile 一键拉起并复现「事件→Kafka→CEP→Side Output→ClickHouse/Kafka 告警」链路且用断言脚本验收，**so that** default `make up` 不受影响且告警可观察、验证失败非 0。

<objective>
垂直切片 VEH-02（验收半段）：造数 + submit/gen/wait/verify 端到端转绿，交付八段式 README 与会话收尾，并经人工确认告警可观察。

Purpose: e2e 与文档/人工验收独立采样，避免与快反馈 package 混在同一 task。
Output: gen_vehicle_events.py、转绿 verify、八段式 README、CHANGELOG/PHASES、人工 approved。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-p03/01-RESEARCH.md
@.planning/phases/01-p03/01-PATTERNS.md
@.planning/phases/01-p03/01-02-SUMMARY.md
@scripts/gen_events.py
@examples/e10-cep/README.md
@docs/README.md
@CHANGELOG.md
@PHASES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 造数脚本 + verify 正负例 + Makefile gen/verify</name>
  <files>projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py, projects/p03-vehicle-monitoring/scripts/verify.sh, projects/p03-vehicle-monitoring/Makefile</files>
  <read_first>
    - scripts/gen_events.py（uv script 头、localhost:9094）
    - projects/p03-vehicle-monitoring/scripts/verify.sh（Wave 0 失败态骨架）
    - projects/p03-vehicle-monitoring/Makefile（01-02 已实现的 package/submit）
    - .planning/phases/01-p03/01-RESEARCH.md（verify 骨架；RESOLVED：同表 alert_type）
  </read_first>
  <action>
    实现 gen_vehicle_events.py：uv + confluent-kafka；bootstrap localhost:9094；`--scenario match` 注入同 vin 的 HARSH_ACCEL(500)→DTC(500) 于 30s 内并夹 HEARTBEAT。更新 Makefile：实现 gen（调造数脚本）与 verify（调 scripts/verify.sh）。更新 verify.sh：MATCH count&gt;=1（查询 alert_type='MATCH' 或总 count≥1；可选 Kafka vehicle.alerts 非空），失败 exit 1；保留 set -euo pipefail。本任务 automated 必须串完整 e2e：在基座与 up-p03 已就绪前提下 package → submit → gen → 等待水位/CEP（建议 sleep 45–90s 或轮询 CH count）→ verify 期望 0；另测清空表后 verify 期望非 0。
  </action>
  <verify>
    <automated>cd projects/p03-vehicle-monitoring &amp;&amp; make package &amp;&amp; make submit &amp;&amp; make gen &amp;&amp; for i in 1 2 3 4 5 6; do bash scripts/verify.sh &amp;&amp; break || sleep 15; done &amp;&amp; bash scripts/verify.sh &amp;&amp; (cd ../../docker &amp;&amp; docker compose exec -T clickhouse clickhouse-client --user flinklab --password flinklab123 --query "TRUNCATE TABLE IF EXISTS flinklab.vehicle_alerts") &amp;&amp; (! bash scripts/verify.sh)</automated>
  </verify>
  <acceptance_criteria>
    - gen_vehicle_events.py 含 HARSH_ACCEL、DTC、localhost:9094
    - Makefile 含可执行 gen、verify 目标（非 exit 1 占位）
    - 造数并等待后 verify.sh 退出 0；清空 vehicle_alerts 后 verify.sh 退出非 0
    - verify.sh 含 vehicle_alerts 或 MATCH 断言与 exit 1
  </acceptance_criteria>
  <done>VEH-02 端到端可复现；正负例断言成立。</done>
</task>

<task type="auto">
  <name>Task 2: 八段式 README + docs/CHANGELOG/PHASES 收尾</name>
  <files>projects/p03-vehicle-monitoring/README.md, docs/README.md, CHANGELOG.md, PHASES.md</files>
  <read_first>
    - examples/e10-cep/README.md（八段式）
    - docs/README.md（模块 15）
    - CHANGELOG.md
    - PHASES.md
    - CLAUDE.md / 项目约束（八段式：背景→架构→代码→启动→验证→踩坑→最佳实践→面试题+参考）
  </read_first>
  <action>
    撰写 projects/p03-vehicle-monitoring/README.md，强制八段标题齐全：背景、架构、代码、启动、验证、踩坑、最佳实践、面试题（或「面试题+参考」合并节）。内容覆盖：事件→CEP→Side Output→CH/Kafka；`make up` + `make up-p03` + package/submit/gen/verify；verify 失败非 0；bootstrap 混用/无 within/watermark 停滞/CH flush 日志坑。docs/README.md 模块 15 注明 Phase 1 告警样板；完整 ADR（VEH-07）延 Phase 3，本 Phase 仅登记占位（per RESEARCH Open Questions RESOLVED）。CHANGELOG 未发布区记 p03/vehicle；PHASES.md 更新 P4/Phase 1 状态列。禁止违禁词。
  </action>
  <verify>
    <automated>python3 -c "from pathlib import Path; t=Path('projects/p03-vehicle-monitoring/README.md').read_text(); keys=['背景','架构','代码','启动','验证','踩坑','最佳实践','面试题']; missing=[k for k in keys if k not in t]; assert not missing, missing" &amp;&amp; grep -q 'p03\|vehicle' CHANGELOG.md &amp;&amp; grep -q 'p03-vehicle-monitoring\|模块 15\|15' docs/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README 同时含：背景、架构、代码、启动、验证、踩坑、最佳实践、面试题
    - docs/README 模块 15 指向 p03 或注明 Phase 1 样板 / ADR 延 Phase 3
    - CHANGELOG 未发布区含 p03 或 vehicle
    - PHASES.md 已反映本 Phase 进度
  </acceptance_criteria>
  <done>八段式文档与会话收尾完成。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 维护者确认告警可观察</name>
  <files></files>
  <read_first>
    - projects/p03-vehicle-monitoring/README.md
  </read_first>
  <action>
    暂停自动化：请维护者按 how-to-verify 确认 VEH-02 可观察告警与 verify 负例非 0。收到 approved 后标记本 plan 完成。
  </action>
  <what-built>
    p03 造数、verify 断言、八段式 README 与 CHANGELOG/PHASES 已落地；自动化 e2e（submit+gen+verify）应已转绿。
  </what-built>
  <how-to-verify>
    1. 按 README：`cd docker && make up && make up-p03`。
    2. `cd projects/p03-vehicle-monitoring && make package && make submit && make gen`。
    3. `make verify` 应成功。
    4. 目视 ClickHouse `SELECT * FROM flinklab.vehicle_alerts LIMIT 5` 或消费 vehicle.alerts 可见 MATCH。
    5. Side Output：确认代码路径存在；可选注入半成品观察 TIMEOUT。
    6. 清空表后 `make verify` 应非 0。
  </how-to-verify>
  <verify>
    <automated>test -f projects/p03-vehicle-monitoring/scripts/verify.sh &amp;&amp; grep -q 'exit 1' projects/p03-vehicle-monitoring/scripts/verify.sh</automated>
    <human-check>CH 或 Kafka 可见 MATCH 告警；空表跑 verify 非 0</human-check>
  </verify>
  <resume-signal>Type "approved" 或描述问题</resume-signal>
  <acceptance_criteria>
    - 用户确认 CH 或 Kafka 告警通道可观察 CEP 匹配输出
    - 用户确认 verify 失败场景非 0
  </acceptance_criteria>
  <done>Phase 1 成功标准人工确认。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gen_vehicle_events → Kafka EXTERNAL | 宿主机造数 |
| verify.sh → ClickHouse / Kafka | 断言查询 |
| 文档 → 口令引用 | 学习环境凭证出现在说明中 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | verify 查询 | mitigate | 固定表名 `flinklab.vehicle_alerts`；不拼接外部输入 |
| T-1-02 | Denial of Service | 造数流量 | accept | 学习 lab 可控 `--scenario`；非压测（压测属后续 Phase） |
| T-1-03 | Denial of Service | 轮询等待 | mitigate | e2e 有上限重试（verify 循环），避免无限挂起 |
| T-1-04 | Information Disclosure | README/CHANGELOG | mitigate | 仅文档引用 docker/.env 既有 lab 口令；不新增云厂商真实密钥 |
| T-1-05 | Tampering | Makefile gen/verify | mitigate | gen/verify 调用仓库内固定脚本路径；不接受任意用户脚本参数覆盖核心路径 |
| T-1-SC | Tampering | confluent-kafka via uv | mitigate | RESEARCH 已 [OK] Approved；脚本依赖声明 confluent-kafka>=2.5，与仓库 gen_events 一致 |
</threat_model>

<verification>
- OrbStack：package + submit + gen + wait + verify.sh 绿；空表负例非 0
- README 八段关键词齐全；CHANGELOG/PHASES/docs 更新
</verification>

<success_criteria>
- Success Criteria 2：造数后 CH/Kafka 可观察 CEP 匹配或旁路
- Success Criteria 3：verify 失败非 0（含断言）
- VEH-02 验收半段完整；不实现 Broadcast/Grafana/压测（Phase 2/3）
</success_criteria>

<output>
Create `.planning/phases/01-p03/01-03-SUMMARY.md` when done
</output>
