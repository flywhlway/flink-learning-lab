---
phase: 01-p03
plan: 02
type: execute
wave: 2
depends_on:
  - "01-00"
  - "01-01"
files_modified:
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/VehicleAlertJob.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/JobConfig.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/model/AlertEvent.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPattern.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/AlertPatternHandler.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/sink/ClickHouseAlertSink.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/ParseVehicleJson.java
  - projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPatternTest.java
  - projects/p03-vehicle-monitoring/Makefile
  - projects/p03-vehicle-monitoring/pom.xml
  - docker/Makefile
autonomous: true
requirements:
  - VEH-02
user_setup: []

must_haves:
  truths:
    - "HarshThenFaultPatternTest 全部 GREEN；Pattern 含 within(30s)"
    - "AlertPatternHandler 超时路径有单测：processTimedOutMatch 产出 alertType=TIMEOUT 的 AlertEvent（写在 HarshThenFaultPatternTest 或同包现有测试类，不强制新文件）"
    - "VehicleAlertJob 管线可 mvn package 产出可提交 jar（shade mainClass 正确）"
    - "Makefile submit 与 docker/Makefile submit-p03 仅复制固定 p03 jar 并 flink run 固定主类"
    - "source/parse/cep/sink 等有意义算子均设置 .uid(...)"
  artifacts:
    - path: "projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/VehicleAlertJob.java"
      provides: "Kafka→parse→WM→keyBy→CEP→双写+Side Output"
      exports: ["main"]
      contains: ".uid("
    - path: "projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/AlertPatternHandler.java"
      provides: "MATCH 主流 + TIMEOUT Side Output"
      contains: "TimedOutPartialMatchHandler"
    - path: "projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPatternTest.java"
      provides: "模式 within + Handler TIMEOUT 契约单测"
      contains: "TIMEOUT"
    - path: "projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/sink/ClickHouseAlertSink.java"
      provides: "HTTP SinkV2 写入 vehicle_alerts"
      contains: "vehicle_alerts"
  key_links:
    - from: "KafkaSource vehicle.events"
      to: "CEP HarshThenFaultPattern"
      via: "ParseVehicleJson + keyBy(vin)"
      pattern: "vehicle\\.events|HarshThenFaultPattern"
    - from: "AlertPatternHandler"
      to: "Kafka vehicle.alerts + ClickHouseAlertSink"
      via: "main stream + getSideOutput"
      pattern: "TimedOutPartialMatchHandler|ClickHouseAlertSink"
    - from: "Makefile submit"
      to: "docker/jobs + flink run"
      via: "固定 jar 通配与主类"
      pattern: "p03-vehicle-monitoring-.*\\.jar|VehicleAlertJob"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 用独立 p03 compose profile 一键拉起并复现「事件→Kafka→CEP→Side Output→ClickHouse/Kafka 告警」链路且用断言脚本验收，**so that** default `make up` 不受影响且告警可观察、验证失败非 0。

<objective>
垂直切片 VEH-02（作业半段）：完成 CEP 模式/Handler 单测转绿，并实现可打包、可提交的 VehicleAlertJob（含 Parse、Sink、uid、submit 接线）。

Purpose: 将快反馈（单测 + package）与慢 e2e（造数/verify）分离，满足 Nyquist 与 scope_sanity。
Output: 可提交 jar、GREEN 单测、Makefile submit / submit-p03；造数与 e2e verify 留给 01-03。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-p03/01-RESEARCH.md
@.planning/phases/01-p03/01-PATTERNS.md
@.planning/phases/01-p03/01-00-SUMMARY.md
@.planning/phases/01-p03/01-01-SUMMARY.md
@examples/e10-cep/src/main/java/com/flywhl/flinklab/e10/C5VehicleDtcPatternJob.java
@examples/e10-cep/src/main/java/com/flywhl/flinklab/e10/C3TimeoutSideOutputJob.java
@examples/e07-connectors/src/main/java/com/flywhl/flinklab/e07/C6ClickHouseHttpSinkJob.java
@examples/e01-hello-flink/src/main/java/com/flywhl/flinklab/e01/KafkaClickstreamWindowJob.java
@templates/job-datastream/src/main/java/com/flywhl/flinklab/template/JobTemplate.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CEP 模式 + Handler + 单测转绿</name>
  <files>projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/model/AlertEvent.java, projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPattern.java, projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/AlertPatternHandler.java, projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPatternTest.java</files>
  <read_first>
    - examples/e10-cep/src/main/java/com/flywhl/flinklab/e10/C5VehicleDtcPatternJob.java
    - examples/e10-cep/src/main/java/com/flywhl/flinklab/e10/C3TimeoutSideOutputJob.java（TimedOutPartialMatchHandler、OutputTag、processTimedOutMatch）
    - projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPatternTest.java
    - .planning/phases/01-p03/01-RESEARCH.md（Security：非法 signalType 过滤、within 强制；Open Questions RESOLVED：同表 alert_type）
  </read_first>
  <behavior>
    - HarshThenFaultPattern.build()：followedBy + within(30s)；harsh: HARSH_ACCEL &amp;&amp; value&gt;450；fault: DTC &amp;&amp; value&gt;480
    - AlertEvent 字段：vin、alertType（MATCH|TIMEOUT）、harshValue、faultValue、eventTime、message
    - AlertPatternHandler 实现 PatternProcessFunction&lt;VehicleEvent, AlertEvent&gt; 与 TimedOutPartialMatchHandler&lt;VehicleEvent&gt;；匹配输出 alertType=MATCH；超时 ctx.output(timeoutTag, alertType=TIMEOUT)
    - HarshThenFaultPatternTest 全部 GREEN，且含 Handler 超时单测：直接调用 processTimedOutMatch（或等价可测路径），断言产出 AlertEvent.alertType==TIMEOUT（不得仅依赖 e2e 观察 Side Output）
  </behavior>
  <action>
    完成 HarshThenFaultPattern（禁止省略 within）。实现 AlertEvent 与 AlertPatternHandler（对齐 e10-C3 结构，类型改为 VehicleEvent/AlertEvent；同表 alert_type 语义 per Open Questions RESOLVED）。更新单测直至 `mvn -q -Dtest=HarshThenFaultPatternTest test` 退出 0；**必须**覆盖 TIMEOUT：对 AlertPatternHandler.processTimedOutMatch 注入半成品 Map，断言输出 AlertEvent 的 alertType 为 TIMEOUT（可用 Mockito/测试用 Context 桩；测例写在 HarshThenFaultPatternTest 同文件或同包，不新增 files_modified 条目）。不在本任务提交 Flink 作业到集群，不写造数/README。
  </action>
  <verify>
    <automated>cd projects/p03-vehicle-monitoring &amp;&amp; mvn -q -Dtest=HarshThenFaultPatternTest test &amp;&amp; grep -q 'TIMEOUT' src/test/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPatternTest.java</automated>
  </verify>
  <acceptance_criteria>
    - mvn -Dtest=HarshThenFaultPatternTest test 退出码 0
    - HarshThenFaultPattern.java 源码含 within 与 Duration.ofSeconds(30)
    - AlertPatternHandler.java 实现 TimedOutPartialMatchHandler 且调用 ctx.output
    - HarshThenFaultPatternTest.java 含 TIMEOUT 断言（证明 Handler 超时路径，非仅 Pattern 谓词）
    - 源码中无禁止词 TODO/省略/略/自行实现/请参考官网
  </acceptance_criteria>
  <done>模式库单模式与超时旁路处理器可单测证明。</done>
</task>

<task type="auto">
  <name>Task 2: VehicleAlertJob + Parse/Sink + package/submit 接线</name>
  <files>projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/VehicleAlertJob.java, projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/JobConfig.java, projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/ParseVehicleJson.java, projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/sink/ClickHouseAlertSink.java, projects/p03-vehicle-monitoring/Makefile, projects/p03-vehicle-monitoring/pom.xml, docker/Makefile</files>
  <read_first>
    - templates/job-datastream/src/main/java/com/flywhl/flinklab/template/JobTemplate.java
    - templates/job-datastream/src/main/java/com/flywhl/flinklab/template/JobConfig.java
    - examples/e01-hello-flink/src/main/java/com/flywhl/flinklab/e01/KafkaClickstreamWindowJob.java（KafkaSource/Sink、ParseJson、watermark+idleness、.uid）
    - examples/e07-connectors/src/main/java/com/flywhl/flinklab/e07/C6ClickHouseHttpSinkJob.java（SinkV2；先记 size 再 clear；HTTP 非 2xx 抛错）
    - docker/Makefile（submit-e01）
    - .planning/phases/01-p03/01-PATTERNS.md（Shared Pattern：算子 uid + checkpoint）
  </read_first>
  <action>
    实现 JobConfig（默认 kafka-bootstrap=kafka:9092、events-topic=vehicle.events、alerts-topic=vehicle.alerts、clickhouse-url=http://clickhouse:8123/、group-id=p03-vehicle-alerts、checkpoint 15s）。ParseVehicleJson：ObjectMapper 在 open()；解析失败或 vin 含引号/非法 signalType（白名单 HARSH_ACCEL|DTC|HEARTBEAT）则丢弃（T-1-01/02/03）。VehicleAlertJob：KafkaSource earliest → parse → forBoundedOutOfOrderness(5s)+withIdleness(30s) → keyBy(vin) → CEP + AlertPatternHandler → 主流与 Side Output 双写 KafkaSink(vehicle.alerts) 与 ClickHouseAlertSink（同表 alert_type per RESOLVED Q3）。**必须**对 source/parse/CEP process/Kafka sink/ClickHouse sink 等有意义算子调用 `.uid(...)`（PATTERNS Shared Pattern「算子 uid + checkpoint」）；env.enableCheckpointing 显式配置。Sink 校验 vin/alertType（拒绝引号反斜杠），flush 先记 batchSize 再 clear，非 2xx 抛错。pom shade mainClass=com.flywhl.flinklab.p03.VehicleAlertJob。Makefile：实现 package 与 submit（submit 只复制 p03-vehicle-monitoring-*.jar 到 docker/jobs，flink run -c 固定主类，T-1-05；gen/verify 若仍为占位可保留 exit 1，由 01-03 实现）。docker/Makefile 增 submit-p03（薄封装，对齐 submit-e01）。本任务不做造数、不跑 OrbStack e2e verify、不写 README/CHANGELOG。
  </action>
  <verify>
    <automated>cd projects/p03-vehicle-monitoring &amp;&amp; mvn -q -Dtest=HarshThenFaultPatternTest test &amp;&amp; mvn -q clean package &amp;&amp; test -f target/p03-vehicle-monitoring-*.jar &amp;&amp; grep -q '\.uid(' src/main/java/com/flywhl/flinklab/p03/VehicleAlertJob.java &amp;&amp; grep -q 'submit' Makefile &amp;&amp; grep -q 'submit-p03' ../../docker/Makefile</automated>
  </verify>
  <acceptance_criteria>
    - HarshThenFaultPatternTest 退出 0；mvn clean package 成功且存在 p03-vehicle-monitoring-*.jar
    - VehicleAlertJob.java 含 KafkaSource、CEP.pattern、getSideOutput、sinkTo，且至少 3 处 .uid(
    - ClickHouseAlertSink 含引号拒绝逻辑且非 2xx 抛错
    - Makefile submit 仅使用 p03-vehicle-monitoring-*.jar 固定通配；docker/Makefile 含 submit-p03
    - 源码无违禁词 TODO/省略/略/自行实现/请参考官网
  </acceptance_criteria>
  <done>作业可编译打包；submit 接线就绪；单测 GREEN；uid/checkpoint 纪律落实。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka JSON → ParseVehicleJson | 不可信事件进入作业 |
| AlertEvent → ClickHouse HTTP INSERT | 字符串拼接 SQL |
| 维护者 → make submit | jar 路径进入容器 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | ClickHouseAlertSink | mitigate | vin/alertType 白名单字符（拒绝引号反斜杠）；仅插入已校验字段；表名列名常量 |
| T-1-02 | Denial of Service | CEP NFA | mitigate | 强制 within(30s)；Parse 过滤非法 signalType，避免脏事件进 keyed CEP |
| T-1-03 | Denial of Service | ParseVehicleJson | mitigate | 解析失败丢弃；字段集合固定（vin/signalType/value/eventTime）；不保留超大未知嵌套到状态 |
| T-1-04 | Information Disclosure | 配置 | mitigate | 仅使用 JobConfig / docker/.env 既有 lab 口令；不新增云厂商真实密钥 |
| T-1-05 | Tampering | Makefile submit | mitigate | 固定复制 `p03-vehicle-monitoring-*.jar` 到 docker/jobs；flink run -c 固定主类；不接受用户任意路径参数 |
| T-1-SC | Tampering | Maven 坐标 | accept | 仅 SSOT 已 Approved 坐标；无新 [ASSUMED] 包 |
</threat_model>

<verification>
- HarshThenFaultPatternTest 绿（&lt;30s），含 TIMEOUT Handler 断言
- mvn package 成功；VehicleAlertJob 含 .uid(
- Makefile submit / submit-p03 存在
</verification>

<success_criteria>
- VEH-02 作业半段完成：模式+Handler（含 TIMEOUT 单测）+Job+Sink+submit 可打包
- 不实现造数/e2e verify/八段式文档（留给 01-03）
- 不实现 Broadcast/Grafana/压测（Phase 2/3）
</success_criteria>

<output>
Create `.planning/phases/01-p03/01-02-SUMMARY.md` when done
</output>
