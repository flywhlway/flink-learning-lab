---
phase: 01-p03
plan: 02
type: execute
wave: 2
depends_on:
  - "01-00"
  - "01-01"
files_modified:
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/VehicleAlertJob.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/JobConfig.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/model/AlertEvent.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPattern.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/AlertPatternHandler.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/sink/ClickHouseAlertSink.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/ParseVehicleJson.java
  - projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py
  - projects/p03-vehicle-monitoring/scripts/verify.sh
  - projects/p03-vehicle-monitoring/Makefile
  - projects/p03-vehicle-monitoring/README.md
  - projects/p03-vehicle-monitoring/pom.xml
  - docker/Makefile
  - docs/README.md
  - CHANGELOG.md
  - PHASES.md
autonomous: false
requirements:
  - VEH-02
user_setup:
  - service: orbstack-base
    why: "端到端 verify 需要 docker 基座已 make up，且已 make up-p03"
    env_vars: []
    dashboard_config:
      - task: "确保 OrbStack 上下文下 kafka/flink/clickhouse 健康"
        location: "cd docker && make up && make init && make up-p03"

must_haves:
  truths:
    - "造数注入 harsh→fault 可判定序列后，ClickHouse flinklab.vehicle_alerts 中 MATCH 行数 >= 1 或 Kafka vehicle.alerts 有消息"
    - "CEP 超时半成品经 Side Output 进入可观察通道（同表 alert_type=TIMEOUT 或同 topic）"
    - "verify.sh 在有数据时 exit 0；故意空库/清空后 exit ≠ 0"
    - "项目 README 八段式可按路径打开；docs/README 模块 15 指向本项目"
  artifacts:
    - path: "projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/VehicleAlertJob.java"
      provides: "Kafka→parse→WM→keyBy→CEP→双写+Side Output"
      exports: ["main"]
    - path: "projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py"
      provides: "可判定 HARSH_ACCEL→DTC 注入"
      contains: "HARSH_ACCEL"
    - path: "projects/p03-vehicle-monitoring/scripts/verify.sh"
      provides: "断言型验收"
      contains: "exit 1"
    - path: "projects/p03-vehicle-monitoring/README.md"
      provides: "八段式最小文档"
      min_lines: 80
  key_links:
    - from: "KafkaSource vehicle.events"
      to: "CEP HarshThenFaultPattern"
      via: "ParseVehicleJson + keyBy(vin)"
      pattern: "vehicle\\.events|HarshThenFaultPattern"
    - from: "AlertPatternHandler"
      to: "Kafka vehicle.alerts + ClickHouseAlertSink"
      via: "main stream + getSideOutput"
      pattern: "TimedOutPartialMatchHandler|ClickHouseAlertSink"
    - from: "gen_vehicle_events.py"
      to: "verify.sh"
      via: "可判定序列驱动断言"
      pattern: "HARSH_ACCEL|vehicle_alerts"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 用独立 p03 compose profile 一键拉起并复现「事件→Kafka→CEP→Side Output→ClickHouse/Kafka 告警」链路且用断言脚本验收，**so that** default `make up` 不受影响且告警可观察、验证失败非 0。

<objective>
垂直切片 VEH-02：打通事件→Kafka→CEP→Side Output→ClickHouse/Kafka 告警的可复现链路，并使 verify.sh 对可观察输出做断言（含失败非 0）。

Purpose: Phase 1 核心验收；固化后续 p01/p02 可复制的「造数 + 提交 + 断言」样板。
Output: 可提交的 VehicleAlertJob、gen_vehicle_events.py、转绿的 verify、八段式 README、会话收尾 CHANGELOG/PHASES。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-p03/01-RESEARCH.md
@.planning/phases/01-p03/01-PATTERNS.md
@.planning/phases/01-p03/01-00-SUMMARY.md
@.planning/phases/01-p03/01-01-SUMMARY.md
@examples/e10-cep/src/main/java/com/flywhl/flinklab/e10/C5VehicleDtcPatternJob.java
@examples/e10-cep/src/main/java/com/flywhl/flinklab/e10/C3TimeoutSideOutputJob.java
@examples/e07-connectors/src/main/java/com/flywhl/flinklab/e07/C6ClickHouseHttpSinkJob.java
@examples/e01-hello-flink/src/main/java/com/flywhl/flinklab/e01/KafkaClickstreamWindowJob.java
@scripts/gen_events.py
@templates/job-datastream/src/main/java/com/flywhl/flinklab/template/JobTemplate.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CEP 模式 + Handler + 单测转绿</name>
  <files>projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/model/AlertEvent.java, projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPattern.java, projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/AlertPatternHandler.java, projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPatternTest.java</files>
  <read_first>
    - examples/e10-cep/src/main/java/com/flywhl/flinklab/e10/C5VehicleDtcPatternJob.java
    - examples/e10-cep/src/main/java/com/flywhl/flinklab/e10/C3TimeoutSideOutputJob.java（TimedOutPartialMatchHandler、OutputTag、processTimedOutMatch）
    - projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPatternTest.java
    - .planning/phases/01-p03/01-RESEARCH.md（Security：非法 signalType 过滤、within 强制）
  </read_first>
  <behavior>
    - HarshThenFaultPattern.build()：followedBy + within(30s)；harsh: HARSH_ACCEL &amp;&amp; value&gt;450；fault: DTC &amp;&amp; value&gt;480
    - AlertEvent 字段：vin、alertType（MATCH|TIMEOUT）、harshValue、faultValue、eventTime、message
    - AlertPatternHandler 实现 PatternProcessFunction&lt;VehicleEvent, AlertEvent&gt; 与 TimedOutPartialMatchHandler&lt;VehicleEvent&gt;；匹配输出 alertType=MATCH；超时 ctx.output(timeoutTag, alertType=TIMEOUT)
    - HarshThenFaultPatternTest 全部 GREEN
  </behavior>
  <action>
    完成 HarshThenFaultPattern（禁止省略 within）。实现 AlertEvent 与 AlertPatternHandler（对齐 e10-C3 结构，类型改为 VehicleEvent/AlertEvent）。更新单测直至 `mvn -q -Dtest=HarshThenFaultPatternTest test` 退出 0。不在本任务提交 Flink 作业到集群。
  </action>
  <verify>
    <automated>cd projects/p03-vehicle-monitoring &amp;&amp; mvn -q -Dtest=HarshThenFaultPatternTest test</automated>
  </verify>
  <acceptance_criteria>
    - mvn -Dtest=HarshThenFaultPatternTest test 退出码 0
    - HarshThenFaultPattern.java 源码含 within 与 Duration.ofSeconds(30)
    - AlertPatternHandler.java 实现 TimedOutPartialMatchHandler 且调用 ctx.output
    - 源码中无禁止词 TODO/省略/略/自行实现/请参考官网
  </acceptance_criteria>
  <done>模式库单模式与超时旁路处理器可单测证明。</done>
</task>

<task type="auto">
  <name>Task 2: 端到端作业 + 造数 + verify/文档转绿</name>
  <files>projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/VehicleAlertJob.java, projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/JobConfig.java, projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/ParseVehicleJson.java, projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/sink/ClickHouseAlertSink.java, projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py, projects/p03-vehicle-monitoring/scripts/verify.sh, projects/p03-vehicle-monitoring/Makefile, projects/p03-vehicle-monitoring/pom.xml, projects/p03-vehicle-monitoring/README.md, docker/Makefile, docs/README.md, CHANGELOG.md, PHASES.md</files>
  <read_first>
    - templates/job-datastream/src/main/java/com/flywhl/flinklab/template/JobTemplate.java
    - templates/job-datastream/src/main/java/com/flywhl/flinklab/template/JobConfig.java
    - examples/e01-hello-flink/src/main/java/com/flywhl/flinklab/e01/KafkaClickstreamWindowJob.java（KafkaSource/Sink、ParseJson、watermark+idleness）
    - examples/e07-connectors/src/main/java/com/flywhl/flinklab/e07/C6ClickHouseHttpSinkJob.java（SinkV2；先记 size 再 clear；HTTP 非 2xx 抛错）
    - scripts/gen_events.py（uv script 头、localhost:9094）
    - docker/Makefile（submit-e01）
    - examples/e10-cep/README.md（八段式）
    - docs/README.md（模块 15）
    - CHANGELOG.md
    - PHASES.md
  </read_first>
  <action>
    实现 JobConfig（默认 kafka-bootstrap=kafka:9092、events-topic=vehicle.events、alerts-topic=vehicle.alerts、clickhouse-url=http://clickhouse:8123/、group-id=p03-vehicle-alerts、checkpoint 15s）。ParseVehicleJson：ObjectMapper 在 open()；解析失败或 vin 含引号/非法 signalType（白名单 HARSH_ACCEL|DTC|HEARTBEAT）则丢弃（T-1-01/02/03）。VehicleAlertJob：KafkaSource earliest → parse → forBoundedOutOfOrderness(5s)+withIdleness(30s) → keyBy(vin) → CEP + AlertPatternHandler → 主流与 Side Output 双写 KafkaSink(vehicle.alerts) 与 ClickHouseAlertSink；Sink 校验 vin/alertType（拒绝引号反斜杠），flush 先记 batchSize 再 clear，非 2xx 抛错。shade mainClass=com.flywhl.flinklab.p03.VehicleAlertJob。gen_vehicle_events.py：uv + confluent-kafka；bootstrap localhost:9094；--scenario match 注入同 vin 的 HARSH_ACCEL(500)→DTC(500) 于 30s 内并夹 HEARTBEAT。Makefile：package/submit/gen/verify/test；submit 只复制 p03-vehicle-monitoring-*.jar 到 docker/jobs，flink run -c 固定主类（T-1-05）。docker/Makefile 增 submit-p03。更新 verify.sh：MATCH count&gt;=1（可选 Kafka 非空），失败 exit 1。README 八段式；docs/README 模块 15 注明 Phase 1 告警样板（VEH-07 完整 ADR 延 Phase 3）；CHANGELOG 未发布区 + PHASES P4 进行中。OrbStack：up-p03 → package → submit → gen → verify 期望 0；清空表后 verify 期望非 0。
  </action>
  <verify>
    <automated>cd projects/p03-vehicle-monitoring &amp;&amp; mvn -q clean package &amp;&amp; bash scripts/verify.sh &amp;&amp; python3 -c "import pathlib; t=pathlib.Path('README.md').read_text(); assert all(k in t for k in ['背景','架构','启动','验证'])"</automated>
  </verify>
  <acceptance_criteria>
    - VehicleAlertJob.java 含 KafkaSource、CEP.pattern、getSideOutput、sinkTo
    - ClickHouseAlertSink 含引号拒绝逻辑且非 2xx 抛错
    - gen_vehicle_events.py 含 HARSH_ACCEL、DTC、localhost:9094
    - submit 目标仅使用 p03-vehicle-monitoring-*.jar 固定通配
    - 造数后 verify.sh 退出 0；清空 vehicle_alerts 后 verify.sh 退出非 0
    - README 含背景/架构/启动/验证；CHANGELOG 未发布区含 p03 或 vehicle
    - projects/p03-vehicle-monitoring 无违禁词 TODO/省略/略/自行实现/请参考官网
  </acceptance_criteria>
  <done>VEH-02 端到端可复现；断言与文档样板固化。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 维护者确认告警可观察</name>
  <files></files>
  <read_first>
    - projects/p03-vehicle-monitoring/README.md
  </read_first>
  <action>
    暂停自动化：请维护者按 how-to-verify 确认 VEH-02 可观察告警与 verify 负例非 0。收到 approved 后标记本 plan 完成。
  </action>
  <what-built>
    p03 告警作业、造数、verify 断言、README 已落地；自动化 verify 应已转绿。
  </what-built>
  <how-to-verify>
    1. 按 README：`cd docker && make up && make up-p03`。
    2. `cd projects/p03-vehicle-monitoring && make package && make submit && make gen`。
    3. `make verify` 应成功。
    4. 目视 ClickHouse `SELECT * FROM flinklab.vehicle_alerts LIMIT 5` 或消费 vehicle.alerts 可见 MATCH。
    5. Side Output：确认代码路径存在；可选注入半成品观察 TIMEOUT。
  </how-to-verify>
  <verify>
    <automated>test -f projects/p03-vehicle-monitoring/scripts/verify.sh &amp;&amp; grep -q 'exit 1' projects/p03-vehicle-monitoring/scripts/verify.sh</automated>
    <human-check>CH 或 Kafka 可见 MATCH 告警；空表跑 verify 非 0</human-check>
  </verify>
  <resume-signal>Type "approved" 或描述问题</resume-signal>
  <acceptance_criteria>
    - 用户确认 CH 或 Kafka 告警通道可观察 CEP 匹配输出
    - 用户确认 verify 失败场景非 0
  </acceptance_criteria>
  <done>Phase 1 成功标准人工确认。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka JSON → ParseVehicleJson | 不可信事件进入作业 |
| AlertEvent → ClickHouse HTTP INSERT | 字符串拼接 SQL |
| 维护者 → make submit | jar 路径进入容器 |
| gen_vehicle_events → Kafka EXTERNAL | 宿主机造数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | ClickHouseAlertSink | mitigate | vin/alertType 白名单字符（拒绝引号反斜杠）；仅插入已校验字段；表名列名常量 |
| T-1-02 | Denial of Service | CEP NFA | mitigate | 强制 within(30s)；Parse 过滤非法 signalType，避免脏事件进 keyed CEP |
| T-1-03 | Denial of Service | ParseVehicleJson | mitigate | 解析失败丢弃；字段集合固定（vin/signalType/value/eventTime）；不保留超大未知嵌套到状态 |
| T-1-04 | Information Disclosure | 配置/文档 | mitigate | 仅文档引用 docker/.env 既有 lab 口令；不新增云厂商真实密钥 |
| T-1-05 | Tampering | Makefile submit | mitigate | 固定复制 `p03-vehicle-monitoring-*.jar` 到 docker/jobs；flink run -c 固定主类；不接受用户任意路径参数 |
| T-1-SC | Tampering | confluent-kafka via uv | mitigate | RESEARCH 已 [OK] Approved；脚本依赖声明 confluent-kafka>=2.5，与仓库 gen_events 一致 |
</threat_model>

<verification>
- HarshThenFaultPatternTest 绿
- mvn package 成功
- OrbStack：submit + gen 后 verify.sh 绿；空表负例非 0
- README 八段式；CHANGELOG/PHASES 更新
</verification>

<success_criteria>
- Success Criteria 2：造数后 CH/Kafka 可观察 CEP 匹配或旁路
- Success Criteria 3：verify 失败非 0（含断言）
- VEH-02 完整覆盖；不实现 Broadcast/Grafana/压测（Phase 2/3）
</success_criteria>

<output>
Create `.planning/phases/01-p03/01-02-SUMMARY.md` when done
</output>
