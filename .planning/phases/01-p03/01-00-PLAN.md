---
phase: 01-p03
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - projects/p03-vehicle-monitoring/pom.xml
  - projects/p03-vehicle-monitoring/Makefile
  - projects/p03-vehicle-monitoring/scripts/verify.sh
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPattern.java
  - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/model/VehicleEvent.java
  - projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPatternTest.java
autonomous: true
requirements:
  - VEH-02
user_setup: []

  must_haves:
  truths:
    - "mvn -Dtest=HarshThenFaultPatternTest 在实现前为 RED（断言失败或编译失败均可，但测试类与 surefire 已接线）"
    - "verify.sh 以 ClickHouse flinklab.vehicle_alerts count 为唯一放行条件；无 CH 告警行时非 0；Kafka 不可单独放行"
    - "独立 pom 声明 flink 2.2.1、flink-cep、kafka connector 5.0.0-2.2、junit 5.10.2、surefire"
  artifacts:
    - path: "projects/p03-vehicle-monitoring/pom.xml"
      provides: "独立 Maven 模块 + surefire + shade mainClass 占位"
      contains: "maven-surefire-plugin"
    - path: "projects/p03-vehicle-monitoring/scripts/verify.sh"
      provides: "VEH-02 断言骨架（失败非 0）"
      contains: "set -euo pipefail"
    - path: "projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPatternTest.java"
      provides: "模式条件 / within 语义单测"
      contains: "HarshThenFaultPattern"
  key_links:
    - from: "HarshThenFaultPatternTest"
      to: "HarshThenFaultPattern.build()"
      via: "JUnit 调用 Pattern API 条件"
      pattern: "HarshThenFaultPattern"
    - from: "verify.sh"
      to: "flinklab.vehicle_alerts"
      via: "clickhouse-client count 为唯一放行条件（Kafka 不可单独放行）"
      pattern: "vehicle_alerts|count"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 用独立 p03 compose profile 一键拉起并复现「事件→Kafka→CEP→Side Output→ClickHouse/Kafka 告警」链路且用断言脚本验收，**so that** default `make up` 不受影响且告警可观察、验证失败非 0。

<objective>
Wave 0（Nyquist）：落地 p03 项目骨架与失败态验收夹具，使后续垂直切片有可重复的 RED→GREEN 反馈环。

Purpose: VALIDATION.md 要求 Wave 0 先有 `HarshThenFaultPatternTest`、`verify.sh`（失败非 0）、surefire；否则后续 e2e 无法自动化门禁。
Output: `projects/p03-vehicle-monitoring` 最小骨架；单测与 verify 均可运行且当前为失败态（或单测对未完成行为失败）。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-p03/01-RESEARCH.md
@.planning/phases/01-p03/01-PATTERNS.md
@.planning/phases/01-p03/01-VALIDATION.md
@templates/job-datastream/pom.xml
@templates/job-datastream/src/main/java/com/flywhl/flinklab/template/JobConfig.java
@examples/e10-cep/src/main/java/com/flywhl/flinklab/e10/C5VehicleDtcPatternJob.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 脚手架 pom + 失败态 HarshThenFaultPatternTest</name>
  <files>projects/p03-vehicle-monitoring/pom.xml, projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/model/VehicleEvent.java, projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPattern.java, projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/HarshThenFaultPatternTest.java</files>
  <read_first>
    - templates/job-datastream/pom.xml（独立模块、flink 2.2.1、kafka 5.0.0-2.2、junit、shade、可选 -Plocal）
    - examples/e10-cep/pom.xml（flink-cep 依赖写法）
    - examples/e10-cep/src/main/java/com/flywhl/flinklab/e10/C5VehicleDtcPatternJob.java（harsh→fault + within(30s)）
    - .planning/phases/01-p03/01-VALIDATION.md（Wave 0 单测命令）
  </read_first>
  <behavior>
    - VehicleEvent 字段：vin (String)、signalType (String)、value (double)、eventTime (long)
    - HarshThenFaultPattern.build() 返回 Pattern：begin("harsh") where signalType=="HARSH_ACCEL" && value&gt;450，followedBy("fault") where signalType=="DTC" && value&gt;480，within(Duration.ofSeconds(30)) — 本任务可先写测试期望，实现可故意缺 within 或阈值错误使测试 RED
    - HarshThenFaultPatternTest：至少 2 个用例 — (1) 合法 harsh+fault 条件谓词为 true；(2) Pattern 上存在 within 30s（通过反射或 Pattern API 可观察属性断言；不得仅 assertTrue(true)）
    - pom：groupId com.flywhl.flinklab，artifactId p03-vehicle-monitoring，version 0.1.0；显式 flink-cep:${flink.version}；jackson-databind 2.17.2；maven-surefire-plugin 3.x 绑定 test；shade mainClass 暂设 com.flywhl.flinklab.p03.VehicleAlertJob（类可下波创建）
  </behavior>
  <action>
    从 templates/job-datastream 复制独立 pom 到 projects/p03-vehicle-monitoring/pom.xml，按 RESEARCH 改 artifact 与依赖（含 flink-cep、jackson-databind），添加 maven-surefire-plugin。创建最小 VehicleEvent 与 HarshThenFaultPattern（可先不完整以保持 RED）。编写 HarshThenFaultPatternTest 覆盖 HARSH_ACCEL/DTC 阈值与 within(30s) 语义（对齐 e10-C5，信号名改为 HARSH_ACCEL/DTC）。禁止把模块挂入 examples/ 父工程。禁止引入未在 SSOT 登记的新版本坐标。
  </action>
  <verify>
    <automated>cd projects/p03-vehicle-monitoring &amp;&amp; mvn -q -Dtest=HarshThenFaultPatternTest test; test_exit=$?; if [ $test_exit -eq 0 ]; then echo "EXPECTED RED in Wave 0 if pattern incomplete — if GREEN, ensure tests are non-trivial"; fi; test -f pom.xml &amp;&amp; grep -q 'maven-surefire-plugin' pom.xml &amp;&amp; grep -q 'flink-cep' pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - 文件 projects/p03-vehicle-monitoring/pom.xml 存在且含 flink.version=2.2.1、flink-cep、maven-surefire-plugin、artifactId p03-vehicle-monitoring
    - HarshThenFaultPatternTest.java 存在且可被 surefire 发现（类名 HarshThenFaultPatternTest）
    - 测试断言引用 HARSH_ACCEL、DTC、within 或 Duration.ofSeconds(30) 之一（grep 源码可见）
    - 模块未出现在 examples/pom.xml 的 modules 列表中
  </acceptance_criteria>
  <done>Wave 0 单测夹具与 pom 接线完成；模式测试可运行（允许 RED）。</done>
</task>

<task type="auto">
  <name>Task 2: 失败态 verify.sh + 项目 Makefile 桩</name>
  <files>projects/p03-vehicle-monitoring/scripts/verify.sh, projects/p03-vehicle-monitoring/Makefile</files>
  <read_first>
    - .planning/phases/01-p03/01-RESEARCH.md（verify.sh 断言骨架、CH 用户 flinklab/flinklab123）
    - scripts/qa_check.sh（失败非 0 风格）
    - docker/init.sh（clickhouse-client 调用方式）
    - .planning/phases/01-p03/01-VALIDATION.md（空库跑 verify 期望 exit ≠0）
  </read_first>
  <action>
    创建 projects/p03-vehicle-monitoring/scripts/verify.sh：shebang bash；set -euo pipefail；从仓库根或脚本相对路径定位 docker/docker-compose.yml；**ClickHouse 为唯一权威放行出口**：查询 `SELECT count() FROM flinklab.vehicle_alerts`（用户 flinklab、密码 flinklab123）；若表不存在或 count&lt;1 则 echo FAIL 到 stderr 并 exit 1。Kafka `vehicle.alerts` 仅允许作为可选诊断日志（打印消费条数或 offset），**不得**在 CH count&lt;1 时因 Kafka 有消息而 exit 0。禁止仅 `echo OK; exit 0`。创建 Makefile 含目标：package（mvn -q clean package）、verify（调用 scripts/verify.sh）、test（mvn -q test）；submit/gen 可先 echo「未实现」并以 exit 1 占位（下波实现）。本任务不要求集群已有数据——空环境跑 verify 必须非 0。
  </action>
  <verify>
    <automated>bash -n projects/p03-vehicle-monitoring/scripts/verify.sh &amp;&amp; (cd projects/p03-vehicle-monitoring &amp;&amp; bash scripts/verify.sh); ec=$?; test $ec -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - verify.sh 含 set -euo pipefail
    - verify.sh 含 vehicle_alerts；CH count 为唯一放行条件（Kafka 不可单独放行）
    - 在无告警数据（或 CH 未起/表不存在）时执行 scripts/verify.sh 退出码 ≠ 0
    - Makefile 含 package、verify、test 目标
  </acceptance_criteria>
  <done>验证脚本失败态可证明；Makefile 入口存在。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 造数/作业 → ClickHouse HTTP | 告警字段进入 INSERT SQL 字符串 |
| 宿主机脚本 → docker exec | 验证脚本拼查询与 topic 名 |
| Maven shade jar → flink run | 提交路径与 artifact 名 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | ClickHouseAlertSink（后续实现） | mitigate | Wave 0 不实现 Sink；本计划在 verify 查询中只使用固定表名 `flinklab.vehicle_alerts`，禁止拼接外部输入 |
| T-1-02 | Denial of Service | JSON / CEP 状态 | mitigate | 单测锁定 within(30s)；后续 Parse 过滤非法 signalType（本 Wave 在测试期望中写明） |
| T-1-03 | Denial of Service | 超大 JSON | mitigate | 本 Wave 不接 Kafka；下波 ParseVehicleJson 限制字段白名单 |
| T-1-04 | Information Disclosure | secrets | mitigate | 使用 docker/.env 既有 flinklab/flinklab123；verify.sh 不新增真实密钥文件；禁止提交 .env 副本到 projects/ |
| T-1-05 | Tampering | jar submit 路径 | mitigate | Makefile package 产出固定 artifactId `p03-vehicle-monitoring-0.1.0*.jar`；禁止接受任意用户路径参数 |
| T-1-SC | Tampering | Maven/PyPI 安装 | accept | 仅使用 SSOT 已 Approved 坐标（RESEARCH Package Legitimacy）；无新 [ASSUMED] 包 |
</threat_model>

<verification>
- `grep maven-surefire-plugin projects/p03-vehicle-monitoring/pom.xml`
- `bash -n projects/p03-vehicle-monitoring/scripts/verify.sh`
- 空数据执行 verify.sh → exit ≠ 0
</verification>

<success_criteria>
- Wave 0 夹具齐全：surefire、HarshThenFaultPatternTest、verify.sh（失败非 0）
- 不触碰 docker-compose profile（留给 01-01）
- 不实现完整 VehicleAlertJob 管线（留给 01-02）；造数/e2e/文档留给 01-03
</success_criteria>

<output>
Create `.planning/phases/01-p03/01-00-SUMMARY.md` when done
</output>
