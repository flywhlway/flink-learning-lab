---
phase: 01-p03
plan: 01
type: execute
wave: 1
depends_on:
  - "01-00"
files_modified:
  - docker/docker-compose.yml
  - docker/Makefile
  - projects/p03-vehicle-monitoring/sql/clickhouse_alerts.sql
  - projects/p03-vehicle-monitoring/scripts/smoke_profile.sh
autonomous: false
requirements:
  - VEH-01
user_setup: []

must_haves:
  truths:
    - "维护者执行 docker compose --profile p03 up 后，vehicle.events 与 vehicle.alerts topic 存在，且 flinklab.vehicle_alerts 表存在"
    - "default make up / docker compose up 不启动 p03-init 容器"
    - "docker compose config -q 与 docker compose --profile p03 config -q 均成功"
  artifacts:
    - path: "docker/docker-compose.yml"
      provides: "p03-init 服务 profiles: [p03]"
      contains: "profiles: [\"p03\"]"
    - path: "projects/p03-vehicle-monitoring/sql/clickhouse_alerts.sql"
      provides: "vehicle_alerts DDL（含 alert_type）"
      contains: "vehicle_alerts"
    - path: "docker/Makefile"
      provides: "up-p03 目标，不修改 up 目标语义"
      contains: "up-p03"
  key_links:
    - from: "p03-init"
      to: "kafka + clickhouse"
      via: "depends_on + entrypoint 建 topic/DDL"
      pattern: "p03-init"
    - from: "up-p03"
      to: "docker compose --profile p03"
      via: "Makefile 目标"
      pattern: "profile p03"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 用独立 p03 compose profile 一键拉起并复现「事件→Kafka→CEP→Side Output→ClickHouse/Kafka 告警」链路且用断言脚本验收，**so that** default `make up` 不受影响且告警可观察、验证失败非 0。

<objective>
垂直切片 VEH-01：交付独立 compose profile `p03` 的一键初始化（topic + CH 表），并证明 default `make up` 不被拖入 p03-init。

Purpose: 工程样板的隔离纪律；后续告警作业依赖这些 topic/表。
Output: `p03-init` 服务、`clickhouse_alerts.sql`、`make up-p03`、profile 冒烟脚本；人工确认 default make up 无 p03-init。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-p03/01-RESEARCH.md
@.planning/phases/01-p03/01-PATTERNS.md
@.planning/phases/01-p03/01-00-SUMMARY.md
@docker/docker-compose.yml
@docker/Makefile
@docker/init.sh
@docker/config/clickhouse/init/01-init.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: vehicle_alerts DDL + 失败态 profile 冒烟脚本</name>
  <files>projects/p03-vehicle-monitoring/sql/clickhouse_alerts.sql, projects/p03-vehicle-monitoring/scripts/smoke_profile.sh</files>
  <read_first>
    - docker/config/clickhouse/init/01-init.sql（MergeTree 风格）
    - .planning/phases/01-p03/01-RESEARCH.md（表名 vehicle_alerts、alert_type MATCH/TIMEOUT）
    - docker/docker-compose.yml（ai profile 隔离写法）
    - projects/p03-vehicle-monitoring/scripts/verify.sh（路径约定）
  </read_first>
  <action>
    创建 projects/p03-vehicle-monitoring/sql/clickhouse_alerts.sql：CREATE TABLE IF NOT EXISTS flinklab.vehicle_alerts，列至少含 vin String、alert_type String（取值 MATCH/TIMEOUT）、signal_summary String、harsh_value Float64、fault_value Float64、event_time DateTime64(3) 或 Int64、ingest_time DateTime64(3) DEFAULT now64(3)；ENGINE=MergeTree ORDER BY (event_time, vin)。创建 scripts/smoke_profile.sh：set -euo pipefail；在 docker/ 目录执行 `docker compose config -q` 与 `docker compose --profile p03 config -q`；用 `docker compose config`（无 profile）管道检查输出中 p03-init 不得出现在未标注 profiles 的默认服务集合——实现方式：断言 `docker compose config --services`（无 profile）列表不含 p03-init，且 `docker compose --profile p03 config --services` 含 p03-init；任一失败 exit 1。本任务先写冒烟脚本（可先失败因 compose 尚未加服务）。
  </action>
  <verify>
    <automated>test -f projects/p03-vehicle-monitoring/sql/clickhouse_alerts.sql &amp;&amp; grep -q 'vehicle_alerts' projects/p03-vehicle-monitoring/sql/clickhouse_alerts.sql &amp;&amp; bash -n projects/p03-vehicle-monitoring/scripts/smoke_profile.sh</automated>
  </verify>
  <acceptance_criteria>
    - clickhouse_alerts.sql 含 CREATE TABLE IF NOT EXISTS flinklab.vehicle_alerts
    - DDL 含 alert_type 与 vin 列
    - smoke_profile.sh 含 `docker compose config` 与 `--profile p03` 以及字符串 p03-init
    - smoke_profile.sh 使用 set -euo pipefail
  </acceptance_criteria>
  <done>DDL 与 profile 冒烟夹具就绪。</done>
</task>

<task type="auto">
  <name>Task 2: compose p03-init + make up-p03（冒烟转绿）</name>
  <files>docker/docker-compose.yml, docker/Makefile</files>
  <read_first>
    - docker/docker-compose.yml（milvus-* profiles: ["ai"] 段落后结构、kafka/clickhouse 服务名）
    - docker/Makefile（up、up-ai 目标）
    - docker/init.sh（create_topic 幂等参数：--if-not-exists、partitions 4）
    - projects/p03-vehicle-monitoring/sql/clickhouse_alerts.sql
  </read_first>
  <action>
    在 docker/docker-compose.yml 末尾 volumes 之前新增服务 p03-init：profiles: ["p03"]；container_name: fll-p03-init；restart: "no"；depends_on kafka 与 clickhouse（condition: service_healthy 若已有 healthcheck，否则 service_started）；image 使用与 kafka 同系的 apache/kafka:${TAG} 或 bitnami 皆不可——必须复用 compose 中已有 kafka 镜像变量以便 arm64 一致，entrypoint 用 shell 一次性：等待后对 INTERNAL bootstrap kafka:9092 执行 kafka-topics.sh --create --if-not-exists --topic vehicle.events --partitions 4 --replication-factor 1，同样创建 vehicle.alerts；再用 clickhouse-client 或 curl HTTP 对 clickhouse:8123 执行挂载的 clickhouse_alerts.sql（volume 挂载 ../projects/p03-vehicle-monitoring/sql/clickhouse_alerts.sql）。若单容器难以同时带 kafka 与 ch 客户端：允许 p03-init 使用 clickhouse/clickhouse-server:24.8 镜像跑 SQL，另用 docker compose 同服务内 wget/curl 调 kafka 外部 API——优先方案：服务 network 加入默认网络，image 选 curlimages/curl 或已有工具镜像不可新增未登记镜像；**Claude's Discretion**：采用与 RESEARCH 一致的 one-shot：image 使用 apache/kafka（与 kafka 服务相同 ${KAFKA_IMAGE}），通过 network 访问 clickhouse HTTP `POST` 原始 SQL 文件内容创建表（clickhouse HTTP 可用）。禁止把 p03 topic/DDL 写入 docker/init.sh 默认路径。在 docker/Makefile 增加 up-p03：`docker compose --profile p03 up -d`（或显式 up p03-init），.PHONY 更新；禁止修改 up 目标使其带 --profile p03。跑通 smoke_profile.sh 转绿。
  </action>
  <verify>
    <automated>cd docker &amp;&amp; docker compose config -q &amp;&amp; docker compose --profile p03 config -q &amp;&amp; docker compose config --services | grep -v -q '^p03-init$' ; docker compose --profile p03 config --services | grep -q '^p03-init$' &amp;&amp; bash ../projects/p03-vehicle-monitoring/scripts/smoke_profile.sh</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml 中存在服务名 p03-init 且同行或缩进块含 profiles: ["p03"]
    - docker compose config --services（无 profile）输出不包含 p03-init
    - docker compose --profile p03 config --services 输出包含 p03-init
    - Makefile 含 up-p03，且 up 目标行不含 --profile p03
    - smoke_profile.sh 退出码 0
  </acceptance_criteria>
  <done>VEH-01 自动化部分完成：profile 隔离可配置验证。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 确认 default make up 不拉起 p03-init</name>
  <files></files>
  <read_first>
    - docker/Makefile
    - .planning/phases/01-p03/01-VALIDATION.md（Manual-Only Verifications）
  </read_first>
  <action>
    暂停自动化：请维护者按 how-to-verify 在 OrbStack 上确认 VEH-01（default make up 无 p03-init；profile p03 后 topic/表存在）。收到 approved 后继续下一 plan。
  </action>
  <what-built>
    p03-init 已标 profiles: ["p03"]；up-p03 与 smoke_profile.sh 已落地。
  </what-built>
  <how-to-verify>
    1. `cd docker && make down`（若安全）后 `make up`，或在已 up 环境执行 `docker compose ps -a`。
    2. 确认容器列表中无 `fll-p03-init` / 无正在运行的 p03-init。
    3. 执行 `make up-p03` 或 `docker compose --profile p03 up -d`。
    4. 确认 topic：`docker compose exec kafka /opt/kafka/bin/kafka-topics.sh --bootstrap-server localhost:9092 --list`（路径按实际镜像调整）含 vehicle.events 与 vehicle.alerts。
    5. 确认表：`docker compose exec clickhouse clickhouse-client --user flinklab --password flinklab123 --query "EXISTS TABLE flinklab.vehicle_alerts"` 返回 1。
  </how-to-verify>
  <verify>
    <automated>cd docker &amp;&amp; docker compose config --services | grep -vxq p03-init &amp;&amp; docker compose --profile p03 config --services | grep -qx p03-init</automated>
    <human-check>default make up 后无 p03-init；up-p03 后 vehicle.events/vehicle.alerts 与 vehicle_alerts 表存在</human-check>
  </verify>
  <resume-signal>Type "approved" 或描述偏差</resume-signal>
  <acceptance_criteria>
    - 用户确认 default make up 后无 p03-init 容器
    - 用户确认 --profile p03 后 topic 与表存在
  </acceptance_criteria>
  <done>VEH-01 人工验收通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| p03-init → Kafka admin | 创建 topic（固定名称） |
| p03-init → ClickHouse HTTP/SQL | 执行挂载 DDL 文件 |
| 维护者主机 → compose | profile 开关 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | clickhouse_alerts.sql / init | mitigate | DDL 为仓库静态文件；init 不拼接外部字符串；表名固定 flinklab.vehicle_alerts |
| T-1-02 | Denial of Service | CEP 状态 | accept | 本切片无 CEP；下波强制 within |
| T-1-03 | Denial of Service | 超大 JSON | accept | 本切片无事件解析 |
| T-1-04 | Information Disclosure | CH 凭证 | mitigate | 复用 CH_USER/CH_PASSWORD 环境变量；不把密码写入新 git 文件明文副本（compose 用 ${CH_PASSWORD}） |
| T-1-05 | Tampering | jar 路径 | accept | 本切片不提交 jar |
| T-1-SC | Tampering | 新容器镜像 | mitigate | p03-init 复用已有 KAFKA_IMAGE 或 CLICKHOUSE_IMAGE；禁止引入未登记第三方镜像；若必须 curl 镜像则先写入根 README 版本矩阵（ENG-01）再使用 |
</threat_model>

<verification>
- smoke_profile.sh 退出 0
- docker compose config 双路径 -q 通过
- 人工 checkpoint：make up 无 p03-init；up-p03 后 topic/表存在
</verification>

<success_criteria>
- VEH-01 满足：独立 profile 一键初始化；default make up 不受影响
- 不实现 Flink 作业与造数（留给 01-02）
</success_criteria>

<output>
Create `.planning/phases/01-p03/01-01-SUMMARY.md` when done
</output>
