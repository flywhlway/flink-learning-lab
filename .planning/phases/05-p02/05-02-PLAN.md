---
phase: 05-p02
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/RealtimeRecoJob.java
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/feature/SessionFeatureFunction.java
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/feature/RedisFeatureWriter.java
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/catalog/CatalogLoader.java
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/score/RuleScorer.java
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/score/TopKScoreFunction.java
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/sink/ClickHouseRecoSink.java
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/model/FeatureSnapshot.java
  - projects/p02-realtime-reco/src/test/java/com/flywhl/flinklab/p02/score/RuleScorerTest.java
  - projects/p02-realtime-reco/src/test/java/com/flywhl/flinklab/p02/feature/SessionFeatureFunctionTest.java
  - projects/p02-realtime-reco/scripts/verify.sh
  - projects/p02-realtime-reco/Makefile
autonomous: true
requirements:
  - RECO-02
user_setup: []

must_haves:
  truths:
    - "双通道可观察：Keyed State 累积会话/近窗特征，且 Redis 以 feature:{userId}:* 被写入（at-least-once 攒批，D-01/D-02/D-03）"
    - "规则加权 Top-K（默认 K=5）产出 RecoResult；正常路径 feature_source=REDIS，Redis 读失败回落 STATE_ONLY 且作业不 FAIL（D-04/D-12 预备）"
    - "结果双写 Kafka reco.results + ClickHouse flinklab.reco_results；make match / verify.sh 以 CH count≥1 唯一放行（D-06/D-10）"
    - "RuleScorerTest 与 SessionFeatureFunctionTest GREEN"
  artifacts:
    - path: "projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/feature/SessionFeatureFunction.java"
      provides: "Keyed State 通道一（D-01）"
      contains: "MapState"
    - path: "projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/feature/RedisFeatureWriter.java"
      provides: "jedis Pipeline + CheckpointedFunction 通道二（D-02/D-03）"
      contains: "CheckpointedFunction"
    - path: "projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/score/RuleScorer.java"
      provides: "可解释加权打分纯函数（D-04）"
      contains: "score"
    - path: "projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/catalog/CatalogLoader.java"
      provides: "PG reco_items open() 加载（D-05）"
      contains: "reco_items"
    - path: "projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/sink/ClickHouseRecoSink.java"
      provides: "HTTP SinkV2 权威出口（D-10）"
      contains: "reco_results"
    - path: "projects/p02-realtime-reco/scripts/verify.sh"
      provides: "CH 权威 verify 绿路径（D-10）"
      contains: "reco_results"
  key_links:
    - from: "SessionFeatureFunction"
      to: "FeatureSnapshot"
      via: "keyBy(userId) MapState 累积后随流下发"
      pattern: "FeatureSnapshot|MapState"
    - from: "RedisFeatureWriter"
      to: "feature:{userId}:"
      via: "jedis Pipeline SET；失败 catch 不抛死作业"
      pattern: "feature:|pipelined|CheckpointedFunction"
    - from: "TopKScoreFunction"
      to: "RuleScorer + CatalogLoader"
      via: "读 Redis 优先，失败 STATE_ONLY；emit Top-K RecoResult"
      pattern: "STATE_ONLY|REDIS|RuleScorer"
    - from: "RealtimeRecoJob"
      to: "Kafka reco.results + ClickHouseRecoSink"
      via: "union/旁路双写同一 RecoResult 流"
      pattern: "reco.results|ClickHouseRecoSink"
    - from: "verify.sh"
      to: "flinklab.reco_results"
      via: "count()>=1 放行；Kafka/Redis 仅 diag"
      pattern: "count\\(\\)|reco_results"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 用独立 p02 compose profile 一键拉起「行为流 → 双通道在线特征 → 规则 Top-K」实时推荐闭环并完成压测/Redis 降级演练/文档包，**so that** p02 达到与 p03/p01 同等的单项目完成态且可简历陈述。

<objective>
V2 闭环垂直切片（RECO-02）：双通道特征 + PG catalog + 规则 Top-K + Kafka/CH 双写 + CH 权威 verify 绿。

Purpose: 交付可观察推荐结果，使 make match 在 OrbStack 上以 ClickHouse 放行；压测/演练/文档留给 05-03。
Output: SessionFeatureFunction + RedisFeatureWriter + CatalogLoader + RuleScorer/TopK + ClickHouseRecoSink + 接好的 RealtimeRecoJob + verify.sh 实现态；不填 baseline 实测、不写 ADR/RESUME 终稿。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-p02/05-CONTEXT.md
@.planning/phases/05-p02/05-RESEARCH.md
@.planning/phases/05-p02/05-PATTERNS.md
@.planning/phases/05-p02/05-01-SUMMARY.md
@examples/e12-06-streaming-feature/
@examples/e07-connectors/src/main/java/com/flywhl/flinklab/e07/C7RedisBatchWriteJob.java
@projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/sink/ClickHouseLogSink.java
@projects/p01-log-ai-platform/scripts/verify.sh
@projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/VehicleAlertJob.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 双通道特征 — SessionFeature + RedisFeatureWriter</name>
  <files>projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/model/FeatureSnapshot.java, projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/feature/SessionFeatureFunction.java, projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/feature/RedisFeatureWriter.java, projects/p02-realtime-reco/src/test/java/com/flywhl/flinklab/p02/feature/SessionFeatureFunctionTest.java</files>
  <read_first>
    - examples/e12-06-streaming-feature/（窗口/MapState → Redis 教学；注意勿抄省略 Operator State 的简化写路径）
    - examples/e07-connectors/src/main/java/com/flywhl/flinklab/e07/C7RedisBatchWriteJob.java（jedis Pipeline + CheckpointedFunction 生产基点）
    - .planning/phases/05-p02/05-CONTEXT.md（D-01、D-02、D-03）
    - .planning/phases/05-p02/05-RESEARCH.md（Redis key：feature:{userId}:click_30s / cat_affinity / last_ts；Pitfall 1/2/6）
    - .planning/phases/05-p02/05-PATTERNS.md（SessionFeatureFunction / RedisFeatureWriter Apply）
  </read_first>
  <behavior>
    - FeatureSnapshot：随流携带 userId、clickCount/categoryAffinity/lastEventTs 等可序列化字段，供打分与降级（per D-01）
    - SessionFeatureFunction：KeyedProcessFunction 或 window+ProcessWindowFunction；MapState（或 ValueState）按 userId 累积；输出携带 FeatureSnapshot 的中间记录；单测覆盖计数/类目亲和更新（避免强依赖 MiniCluster，可用包内纯逻辑辅助）（per D-01）
    - RedisFeatureWriter：RichMap/Process + CheckpointedFunction；jedis 5.2.0 Pipeline 攒批 SET key=feature:{userId}:*；checkpoint 刷尾巴；写失败 catch + metric，不下抛打挂作业；语义 at-least-once，文档注释禁止声称 exactly-once（per D-02/D-03）
    - Redis key 仅由已校验 userId + 固定后缀拼接（T-05-02）
  </behavior>
  <action>
    实现 FeatureSnapshot、SessionFeatureFunction、RedisFeatureWriter 与 SessionFeatureFunctionTest。强制复用 e07-C7 CheckpointedFunction 模式，禁止把 e12-06 教学简化版当生产写路径。本任务可不接线主作业（Task 3 接线）。
  </action>
  <verify>
    <automated>cd projects/p02-realtime-reco &amp;&amp; mvn -q -Dtest=SessionFeatureFunctionTest test &amp;&amp; grep -q CheckpointedFunction src/main/java/com/flywhl/flinklab/p02/feature/RedisFeatureWriter.java &amp;&amp; grep -q 'feature:' src/main/java/com/flywhl/flinklab/p02/feature/RedisFeatureWriter.java</automated>
  </verify>
  <acceptance_criteria>
    - SessionFeatureFunction 含 MapState 或 ValueState；输出含 FeatureSnapshot
    - RedisFeatureWriter 实现 CheckpointedFunction + jedis Pipeline；含 try/catch 降级
    - SessionFeatureFunctionTest 退出码 0
    - 源码/注释标明 at-least-once（非 exactly-once）
  </acceptance_criteria>
  <done>RECO-02：双通道特征算子可单测/可降级写（per D-01/D-02/D-03）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Catalog + RuleScorer Top-K GREEN</name>
  <files>projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/catalog/CatalogLoader.java, projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/score/RuleScorer.java, projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/score/TopKScoreFunction.java, projects/p02-realtime-reco/src/test/java/com/flywhl/flinklab/p02/score/RuleScorerTest.java</files>
  <read_first>
    - examples/e11-async-io/（CachedEnrich.open JDBC 形态参考；本 Phase 用 open 全表加载而非每条 Async PG）
    - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/rule/RuleTagger.java（纯函数打分/标签风格）
    - .planning/phases/05-p02/05-CONTEXT.md（D-04、D-05、D-06）
    - .planning/phases/05-p02/05-RESEARCH.md（权重 VIEW=1 CLICK=3 CART=5 BUY=10；类目 ×1.5；近因半衰期 30min；K=5；读 Redis 失败 → STATE_ONLY）
  </read_first>
  <behavior>
    - CatalogLoader.load(jdbcUrl)：DriverManager 读 reco_items → Map&lt;String,ItemCatalog&gt;；供算子 open() 调用一次（per D-05）
    - RuleScorer：纯函数；输入 FeatureSnapshot + catalog + 可选 Redis 对齐特征 → List&lt;RecoResult&gt; Top-K；权重与类目/近因因子可单测（per D-04）
    - TopKScoreFunction：Rich 算子；open 加载 catalog；打分时优先 Redis 点查（短超时），失败或空 → 用随流 FeatureSnapshot，featureSource=STATE_ONLY；成功 featureSource=REDIS；reason 可解释；禁止默认抛异常重启作业（per D-01/D-12）
    - RuleScorerTest 从 RED → GREEN
  </behavior>
  <action>
    实现 CatalogLoader、RuleScorer、TopKScoreFunction，使 RuleScorerTest 变绿。禁止引入 Milvus/ANN/LLM/外部模型服务（per D-04）。禁止把 catalog 做成每条 Async PG 查询（Discretion：open 全表）。
  </action>
  <verify>
    <automated>cd projects/p02-realtime-reco &amp;&amp; mvn -q -Dtest=RuleScorerTest test</automated>
  </verify>
  <acceptance_criteria>
    - RuleScorerTest 退出码 0；断言含 Top-K 排序与权重方向（BUY&gt;VIEW 或等价）
    - CatalogLoader 查询 reco_items
    - TopKScoreFunction 含 STATE_ONLY 与 REDIS 分支字面量
    - 源码无 milvus/ollama/lettuce
  </acceptance_criteria>
  <done>RECO-02：可解释规则召回/打分闭环纯逻辑绿（per D-04/D-05）。</done>
</task>

<task type="auto">
  <name>Task 3: 接线双写 + verify.sh 绿 — make match</name>
  <files>projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/RealtimeRecoJob.java, projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/sink/ClickHouseRecoSink.java, projects/p02-realtime-reco/scripts/verify.sh, projects/p02-realtime-reco/Makefile</files>
  <read_first>
    - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/sink/ClickHouseLogSink.java（SinkV2 批量 INSERT；字段消毒）
    - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/VehicleAlertJob.java（Kafka+CH 双写形态）
    - projects/p01-log-ai-platform/scripts/verify.sh（CH 权威；轮询；diag_*）
    - projects/p01-log-ai-platform/Makefile（match = submit→gen→verify 轮询）
    - .planning/phases/05-p02/05-CONTEXT.md（D-06、D-10）
  </read_first>
  <behavior>
    - ClickHouseRecoSink：表/列名常量；user_id/item_id/reason/feature_source 拒引号反斜杠；非 2xx 抛 IOException（T-05-01）
    - RealtimeRecoJob 完整图：Parse → keyBy → SessionFeature → RedisFeatureWriter → TopKScore → 分支 KafkaSink(reco.results) + ClickHouseRecoSink；全部 uid 前缀 p02-
    - verify.sh：实现态（移除 Wave0 必败门闩）；CH SELECT count() FROM flinklab.reco_results 为唯一 exit 0；Kafka/redis-cli 仅 diag_ 打印；FEATURE_SOURCE 若设则白名单拼 SQL
    - Makefile match：truncate（可选）→ submit → gen scenario → 轮询 verify（VERIFY_WAIT_SEC）；在 OrbStack 上跑通
  </behavior>
  <action>
    接线生产作业图与 Sink，实现 verify.sh 绿路径，并在 OrbStack 执行 make match（前置：cd docker &amp;&amp; make up &amp;&amp; make up-p02，确保 redis/postgres Running）。禁止以 Kafka 有消息或 Redis KEYS 单独放行。禁止假装 Redis exactly-once。
  </action>
  <verify>
    <automated>cd projects/p02-realtime-reco &amp;&amp; mvn -q test &amp;&amp; mvn -q -DskipTests package &amp;&amp; grep -q reco_results scripts/verify.sh &amp;&amp; (! grep -E 'redis-cli.*exit 0|KEYS .*exit 0' scripts/verify.sh) &amp;&amp; make match</automated>
  </verify>
  <acceptance_criteria>
    - mvn test 全绿；jar 可打包
    - RealtimeRecoJob 含 SessionFeature、RedisFeatureWriter、TopK、Kafka results、ClickHouseRecoSink
    - verify.sh 以 reco_results count 放行；无 Redis/Kafka 单独 exit 0
    - make match 在 OrbStack 退出 0（CH 有行）
  </acceptance_criteria>
  <done>RECO-02：特征+打分闭环结果可在 CH/Kafka 观察且 CH 权威 verify 绿（per D-01–D-06/D-10）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BehaviorEvent → Redis key / CH INSERT | 流数据进入外存与分析库 |
| TopKScoreFunction → Redis GET | 外依赖读路径 |
| verify.sh → ClickHouse HTTP | 验收脚本拼 SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | ClickHouseRecoSink / verify.sh | mitigate | Sink 字段消毒；表名列名常量；verify FEATURE_SOURCE 白名单 |
| T-05-02 | Tampering | RedisFeatureWriter key | mitigate | key=feature:{validatedUserId}:{suffix}；禁止原始 JSON 当 key |
| T-05-03 | Denial of Service | Redis 不可用 | mitigate | 写/读失败 catch；STATE_ONLY 打分；作业不 FAIL（演练在 05-03 断言） |
| T-05-06 | Information Disclosure | JDBC/Redis 连接串 | accept | lab 默认口令；不提交云密钥 |
| T-05-SC | Tampering | jedis/pg JDBC | mitigate | 仅 SSOT 5.2.0 / 42.7.4 Approved 坐标 |
</threat_model>

<verification>
- SessionFeatureFunctionTest + RuleScorerTest + 全量 mvn test 绿
- make match 在 OrbStack 以 CH 放行
- 双通道代码路径存在且 Redis 写为 CheckpointedFunction
</verification>

<success_criteria>
- RECO-02 闭环可端到端观察：行为 → 双通道特征 → Top-K → CH/Kafka
- 压测 baseline、Redis 演练脚本绿、文档包不在本 plan（05-03）
</success_criteria>

<output>
Create `.planning/phases/05-p02/05-02-SUMMARY.md` when done
</output>
