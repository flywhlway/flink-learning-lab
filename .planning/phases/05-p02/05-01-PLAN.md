---
phase: 05-p02
plan: 01
type: execute
wave: 1
depends_on:
  - "05-00"
files_modified:
  - projects/p02-realtime-reco/pom.xml
  - projects/p02-realtime-reco/Makefile
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/RealtimeRecoJob.java
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/JobConfig.java
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/ParseBehaviorJson.java
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/model/BehaviorEvent.java
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/model/RecoResult.java
  - projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/model/ItemCatalog.java
  - projects/p02-realtime-reco/src/test/java/com/flywhl/flinklab/p02/ParseBehaviorJsonTest.java
  - projects/p02-realtime-reco/scripts/gen_reco_events.py
  - scripts/smoke_p02_profile.sh
autonomous: true
requirements:
  - RECO-01
user_setup: []

must_haves:
  truths:
    - "维护者可 make up-p02 后看到 reco.events/reco.results 与 flinklab.reco_results 已初始化，且 default make up 不被 --profile p02 污染（D-08）"
    - "mvn clean package 产出可 shade 的 p02 jar；RealtimeRecoJob 可 Kafka→ParseBehaviorJson 透传并 submit 不崩"
    - "ParseBehaviorJsonTest GREEN：合法行为事件保留、脏数据丢弃（D-09）"
    - "gen_reco_events.py 可向 reco.events 写入可判定 scenario（至少含 userId/itemId/eventType/ts）"
  artifacts:
    - path: "projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/RealtimeRecoJob.java"
      provides: "作业主类 Kafka→Parse 透传骨架（D-07）"
      exports: ["main"]
    - path: "projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/JobConfig.java"
      provides: "手写 --key 解析；默认 topics/redis/pg/top-k（D-07）"
      contains: "from"
    - path: "projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/ParseBehaviorJson.java"
      provides: "行为 JSON 解析（D-09）"
      contains: "eventType"
    - path: "projects/p02-realtime-reco/Makefile"
      provides: "package/submit/gen/test 入口"
      contains: "submit"
    - path: "projects/p02-realtime-reco/scripts/gen_reco_events.py"
      provides: "合成行为造数（D-09）"
      contains: "reco.events"
    - path: "scripts/smoke_p02_profile.sh"
      provides: "profile 隔离冒烟"
      contains: "p02-init"
  key_links:
    - from: "RealtimeRecoJob"
      to: "reco.events"
      via: "KafkaSource + ParseBehaviorJson.uid(p02-*)"
      pattern: "reco.events|p02-parse"
    - from: "JobConfig"
      to: "eventsTopic/resultsTopic"
      via: "默认 reco.events / reco.results"
      pattern: "reco\\.events|reco\\.results"
    - from: "smoke_p02_profile.sh"
      to: "compose --profile p02"
      via: "config --services 有无 profile 差集"
      pattern: "p02-init|profile p02"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 用独立 p02 compose profile 一键拉起「行为流 → 双通道在线特征 → 规则 Top-K」实时推荐闭环并完成压测/Redis 降级演练/文档包，**so that** p02 达到与 p03/p01 同等的单项目完成态且可简历陈述。

<objective>
V1 启动面垂直切片（RECO-01）：独立模块可打包提交 + Parse 透传作业 + gen 造数 + profile 冒烟，证明行为流可接入。

Purpose: 先让维护者能一键起 p02 profile、打包提交作业、向 reco.events 灌合成行为；特征/打分/CH 绿留给 05-02。
Output: RealtimeRecoJob（Parse 透传）+ JobConfig/模型 + ParseBehaviorJson GREEN + Makefile + gen_reco_events.py + smoke_p02_profile；不实现 SessionFeature/RedisWriter/RuleScorer 接线 / verify 绿。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-p02/05-CONTEXT.md
@.planning/phases/05-p02/05-RESEARCH.md
@.planning/phases/05-p02/05-PATTERNS.md
@.planning/phases/05-p02/05-00-SUMMARY.md
@projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/LogAiJob.java
@projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/JobConfig.java
@projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/ParseLogJson.java
@projects/p01-log-ai-platform/Makefile
@projects/p01-log-ai-platform/scripts/gen_log_events.py
@scripts/smoke_p01_profile.sh
@docker/Makefile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 模型 + JobConfig + ParseBehaviorJson GREEN</name>
  <files>projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/model/BehaviorEvent.java, projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/model/RecoResult.java, projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/model/ItemCatalog.java, projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/JobConfig.java, projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/ParseBehaviorJson.java, projects/p02-realtime-reco/src/test/java/com/flywhl/flinklab/p02/ParseBehaviorJsonTest.java, projects/p02-realtime-reco/pom.xml</files>
  <read_first>
    - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/JobConfig.java（手写 --key 解析；默认 kafka/clickhouse）
    - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/ParseLogJson.java（脏数据丢弃；拒引号反斜杠）
    - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/model/LogEvent.java（POJO 约定）
    - .planning/phases/05-p02/05-PATTERNS.md（JobConfig / ParseBehaviorJson 映射）
    - .planning/phases/05-p02/05-CONTEXT.md（D-07、D-09）
    - .planning/phases/05-p02/05-RESEARCH.md（Discretion：eventType 大写枚举；topics；top-k=5；redis/pg 主机默认）
  </read_first>
  <behavior>
    - BehaviorEvent：userId、itemId、eventType（VIEW|CLICK|CART|BUY）、eventTimeMs（per D-09）
    - RecoResult：userId、itemId、score、eventTimeMs、reason、featureSource、featureSnapshot（per D-06）
    - ItemCatalog：itemId、category、title、baseWeight（per D-05）
    - JobConfig.from(args)：手写解析（Flink 2.2 无 ParameterTool）；默认 kafkaBootstrap=kafka:9092、eventsTopic=reco.events、resultsTopic=reco.results、clickhouseUrl、redisHost=redis、redisPort=6379、postgresJdbcUrl、topK=5、jobName=p02-realtime-reco（per RESEARCH Discretion）
    - ParseBehaviorJson：FlatMap/Process 风格对齐 p01；非法 JSON/缺字段/危险字符 → 丢弃不抛；合法 → BehaviorEvent
    - ParseBehaviorJsonTest 从 RED → GREEN
  </behavior>
  <action>
    实现三个 model POJO、JobConfig、ParseBehaviorJson，使 Wave 0 的 ParseBehaviorJsonTest 变绿。userId/itemId 校验字符集（字母数字下划线连字符）以阻断 Redis key / CH 注入（T-05-01/T-05-02）。禁止引入 lettuce。禁止在本任务实现特征/打分/Sink。
  </action>
  <verify>
    <automated>cd projects/p02-realtime-reco &amp;&amp; mvn -q -Dtest=ParseBehaviorJsonTest test</automated>
  </verify>
  <acceptance_criteria>
    - ParseBehaviorJson.java / JobConfig.java / 三 model 文件存在
    - JobConfig 默认含 reco.events 与 reco.results 与 topK
    - ParseBehaviorJsonTest 退出码 0
    - 解析路径拒绝含引号或反斜杠的 userId
  </acceptance_criteria>
  <done>RECO-01：行为契约可解析且单测绿（per D-09）。</done>
</task>

<task type="auto">
  <name>Task 2: RealtimeRecoJob 透传 + Makefile + gen_reco_events.py</name>
  <files>projects/p02-realtime-reco/src/main/java/com/flywhl/flinklab/p02/RealtimeRecoJob.java, projects/p02-realtime-reco/Makefile, projects/p02-realtime-reco/scripts/gen_reco_events.py, projects/p02-realtime-reco/pom.xml</files>
  <read_first>
    - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/LogAiJob.java（KafkaSource + parse + uid + checkpoint）
    - projects/p01-log-ai-platform/Makefile（package/submit/gen/verify 目标形态）
    - projects/p01-log-ai-platform/scripts/gen_log_events.py（uv + confluent-kafka + scenario）
    - .planning/phases/05-p02/05-PATTERNS.md（RealtimeRecoJob / Makefile Apply）
    - .planning/phases/05-p02/05-CONTEXT.md（D-07、D-09）
  </read_first>
  <behavior>
    - RealtimeRecoJob.main：JobConfig → env.enableCheckpointing → KafkaSource(reco.events) → ParseBehaviorJson.uid(p02-parse-behavior) → 暂用 print/sink 占位或 side-effect-free map（尚可不写 CH）；算子 uid 前缀 p02-；job name p02-realtime-reco
    - Makefile：package / test / submit / gen / verify / match / loadtest / drill-redis / truncate-results；submit 复制 jar 到 docker/jobs 并 flink run -c RealtimeRecoJob
    - gen_reco_events.py：--scenario feature-score（或等价名）写出可判定序列（同 user 多 eventType + 已知 itemId）；支持 --rate/--duration 供后续压测；写 Kafka topic reco.events；依赖与 p01 一致（uv + confluent-kafka）
  </behavior>
  <action>
    落地透传作业与项目 Makefile、造数脚本。本切片允许尚无 CH/Kafka 结果双写（05-02 接上）。禁止把打分/Redis 逻辑塞进本任务膨胀。submit 必须固定主类与 jar 通配，禁止 interactive flink run。
  </action>
  <verify>
    <automated>cd projects/p02-realtime-reco &amp;&amp; mvn -q -DskipTests package &amp;&amp; test -f Makefile &amp;&amp; grep -q RealtimeRecoJob Makefile &amp;&amp; grep -q 'reco.events' scripts/gen_reco_events.py &amp;&amp; python3 -m py_compile scripts/gen_reco_events.py</automated>
  </verify>
  <acceptance_criteria>
    - target/p02-realtime-reco-*.jar（非 original）存在
    - Makefile 含 package、submit、gen、verify、drill-redis
    - gen_reco_events.py 可 py_compile；含 scenario 与 reco.events
    - RealtimeRecoJob 含 KafkaSource 与 ParseBehaviorJson 与 uid p02-
  </acceptance_criteria>
  <done>RECO-01：行为流作业可打包提交 + 造数入口就位（per D-07/D-09）。</done>
</task>

<task type="auto">
  <name>Task 3: profile 冒烟 — smoke_p02_profile + up-p02 可达性检查</name>
  <files>scripts/smoke_p02_profile.sh, docker/Makefile</files>
  <read_first>
    - scripts/smoke_p01_profile.sh（config --services 有无 profile 差集断言）
    - docker/Makefile（up-p02 目标；确认 PG seed 步骤）
    - .planning/phases/05-p02/05-CONTEXT.md（D-08）
  </read_first>
  <behavior>
    - smoke_p02_profile.sh：set -euo pipefail；断言 docker compose config --services（无 profile）不含 p02-init；带 --profile p02 含 p02-init；可选断言 default up 目标行无 --profile p02
    - 若 OrbStack 可用：cd docker &amp;&amp; make up-p02 后检查 topic 列表含 reco.events（kafka-topics）或至少 p02-init 容器 exit 0；PG 表 reco_items 行数 ≥50。若基座未 up，脚本应清晰 FAIL 并提示 make up，不假绿
  </behavior>
  <action>
    新增 scripts/smoke_p02_profile.sh（对照 smoke_p01_profile）。必要时微调 up-p02 使 PG seed 幂等可重跑。禁止改 default up 加 profile。本任务不要求 CH 有推荐行（那是 05-02）。
  </action>
  <verify>
    <automated>bash -n scripts/smoke_p02_profile.sh &amp;&amp; bash scripts/smoke_p02_profile.sh</automated>
  </verify>
  <acceptance_criteria>
    - smoke_p02_profile.sh 退出 0
    - 脚本断言无 profile 服务集合不含 p02-init、有 profile 含 p02-init
    - up-p02 目标仍不污染 default up
  </acceptance_criteria>
  <done>RECO-01：独立 compose profile 一键钩子可冒烟验证（per D-08）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka 行为 JSON → ParseBehaviorJson | 不可信合成/外部消息 |
| JobConfig CLI → 作业 | 参数边界 |
| smoke/gen 脚本 → Kafka/PG | 本地 lab 网络 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | ParseBehaviorJson | mitigate | 必填字段校验；userId/itemId 字符集白名单；拒引号反斜杠 |
| T-05-04 | Denial of Service | JobConfig topK / gen --rate | mitigate | topK 设上限（如 ≤50）；造数脚本 MAX_RATE 封顶（对齐 p01） |
| T-05-06 | Information Disclosure | JobConfig 默认口令 | accept | 复用 lab 默认；不写入真实云密钥 |
| T-05-SC | Tampering | uv/confluent-kafka | mitigate | 复用 p01 已验证造数依赖路径；不新增未审计包 |
</threat_model>

<verification>
- ParseBehaviorJsonTest GREEN；mvn package 产出 jar
- Makefile submit/gen 目标存在；gen 可 py_compile
- smoke_p02_profile.sh 绿；default up 无 p02 profile
</verification>

<success_criteria>
- 维护者具备 RECO-01 启动面：profile + 可提交透传作业 + 行为造数
- 特征双通道、Top-K 双写、verify 绿不在本 plan（05-02）
</success_criteria>

<output>
Create `.planning/phases/05-p02/05-01-SUMMARY.md` when done
</output>
