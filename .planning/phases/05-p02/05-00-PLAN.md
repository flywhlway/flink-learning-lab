---
phase: 05-p02
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - README.md
  - projects/p02-realtime-reco/pom.xml
  - projects/p02-realtime-reco/src/test/java/com/flywhl/flinklab/p02/ParseBehaviorJsonTest.java
  - projects/p02-realtime-reco/src/test/java/com/flywhl/flinklab/p02/score/RuleScorerTest.java
  - projects/p02-realtime-reco/scripts/verify.sh
  - projects/p02-realtime-reco/scripts/drill_redis_degrade.sh
  - projects/p02-realtime-reco/scripts/loadtest.sh
  - projects/p02-realtime-reco/sql/clickhouse_reco_results.sql
  - projects/p02-realtime-reco/sql/postgres_reco_items.sql
  - docker/docker-compose.yml
  - docker/Makefile
autonomous: true
requirements:
  - RECO-01
  - RECO-02
  - RECO-03
user_setup: []

must_haves:
  truths:
    - "根 README 版本矩阵已登记 jedis 5.2.0（D-02）；p02 独立 pom 可被 surefire 发现且 ParseBehaviorJsonTest/RuleScorerTest 当前 RED"
    - "verify.sh / drill_redis_degrade.sh / loadtest.sh 可 bash -n，且当前默认非 0（失败态骨架）"
    - "clickhouse_reco_results.sql + postgres_reco_items.sql 与 docker p02-init / make up-p02 钩子落仓；default make up 不加 --profile p02（D-08）"
  artifacts:
    - path: "README.md"
      provides: "jedis 5.2.0 SSOT 矩阵行（D-02）"
      contains: "jedis"
    - path: "projects/p02-realtime-reco/pom.xml"
      provides: "独立模块 surefire 脚手架（D-07）"
      contains: "p02-realtime-reco"
    - path: "projects/p02-realtime-reco/src/test/java/com/flywhl/flinklab/p02/ParseBehaviorJsonTest.java"
      provides: "行为 JSON 解析契约 RED 夹具（RECO-01 / D-09）"
      contains: "ParseBehaviorJson"
    - path: "projects/p02-realtime-reco/src/test/java/com/flywhl/flinklab/p02/score/RuleScorerTest.java"
      provides: "规则 Top-K RED 夹具（RECO-02 / D-04）"
      contains: "RuleScorer"
    - path: "projects/p02-realtime-reco/scripts/verify.sh"
      provides: "CH 权威出口失败态骨架（D-10）"
      contains: "reco_results"
    - path: "projects/p02-realtime-reco/sql/clickhouse_reco_results.sql"
      provides: "权威表 DDL（D-06/D-10）"
      contains: "flinklab.reco_results"
    - path: "docker/docker-compose.yml"
      provides: "p02-init profile（D-08）"
      contains: "p02-init"
  key_links:
    - from: "ParseBehaviorJsonTest"
      to: "ParseBehaviorJson"
      via: "JUnit 断言合法事件保留与脏数据丢弃"
      pattern: "ParseBehaviorJson"
    - from: "RuleScorerTest"
      to: "RuleScorer"
      via: "JUnit 断言加权 Top-K 排序"
      pattern: "RuleScorer|Top"
    - from: "verify.sh"
      to: "flinklab.reco_results"
      via: "CH count 权威；Kafka/Redis 仅 diag"
      pattern: "reco_results"
    - from: "docker Makefile up-p02"
      to: "p02-init profile"
      via: "docker compose --profile p02 up -d p02-init"
      pattern: "up-p02|profile p02"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 用独立 p02 compose profile 一键拉起「行为流 → 双通道在线特征 → 规则 Top-K」实时推荐闭环并完成压测/Redis 降级演练/文档包，**so that** p02 达到与 p03/p01 同等的单项目完成态且可简历陈述。

<objective>
Wave 0（Nyquist）：落地 RECO-01–03 失败态单测/脚本夹具、jedis SSOT、DDL 与 compose 钩子，建立 RED→GREEN 反馈环。

Purpose: 05-VALIDATION.md 要求先有 Parse/RuleScorer 单测、verify/drill/loadtest 骨架、reco_results + reco_items DDL 与 p02-init 钩子；否则后续垂直切片无法自动化门禁。
Output: 根 README jedis 行 + 最小独立 pom + RED 两测 + 三脚本骨架（非 0）+ 双 SQL DDL + docker p02-init / up-p02；不实现 RealtimeRecoJob / RedisWriter / CatalogLoader / baseline 实测。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-p02/05-CONTEXT.md
@.planning/phases/05-p02/05-RESEARCH.md
@.planning/phases/05-p02/05-PATTERNS.md
@.planning/phases/05-p02/05-VALIDATION.md
@projects/p01-log-ai-platform/pom.xml
@projects/p01-log-ai-platform/scripts/verify.sh
@projects/p01-log-ai-platform/scripts/loadtest.sh
@projects/p01-log-ai-platform/scripts/drill_ai_degrade.sh
@docker/docker-compose.yml
@docker/Makefile
@README.md
@examples/pom.xml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSOT jedis + 最小 pom + Parse/RuleScorer RED 夹具</name>
  <files>README.md, projects/p02-realtime-reco/pom.xml, projects/p02-realtime-reco/src/test/java/com/flywhl/flinklab/p02/ParseBehaviorJsonTest.java, projects/p02-realtime-reco/src/test/java/com/flywhl/flinklab/p02/score/RuleScorerTest.java</files>
  <read_first>
    - README.md（版本矩阵表结构；确认尚无 jedis 行）
    - examples/pom.xml（jedis.version=5.2.0；postgresql 42.7.4）
    - projects/p01-log-ai-platform/pom.xml（独立模块 + surefire + jackson/junit；勿挂 examples 父工程）
    - projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/ParseLogJsonTest.java（轻量 JUnit 无 MiniCluster）
    - .planning/phases/05-p02/05-RESEARCH.md（Discretion：eventType VIEW/CLICK/CART/BUY；Top-K=5；权重公式）
    - .planning/phases/05-p02/05-CONTEXT.md（D-02、D-04、D-07、D-09）
  </read_first>
  <behavior>
    - README 版本矩阵新增一行：jedis | 5.2.0 | Redis 客户端（p02 / e07 / e12）（per D-02）
    - pom：artifactId=p02-realtime-reco；flink 2.2.1 / kafka connector 5.0.0-2.2 / jackson 2.17.2 / junit 5.10.2 / jedis 5.2.0 / postgresql 42.7.4；shade mainClass 可先写 com.flywhl.flinklab.p02.RealtimeRecoJob；禁止 lettuce、jedis 6.x、Milvus、flink-agents（per D-02/D-04/D-07）
    - ParseBehaviorJsonTest：引用尚未存在的 ParseBehaviorJson；断言合法 JSON 产出 userId/itemId/eventType/eventTime；缺字段或 userId/itemId 含引号/反斜杠被丢弃；eventType 仅 VIEW|CLICK|CART|BUY（per D-09 / T-05-01）
    - RuleScorerTest：引用尚未存在的 RuleScorer；断言给定 FeatureSnapshot + 小 catalog 时 Top-K=5 按加权分降序；BUY 权重高于 VIEW；类目匹配加分（per D-04 / RESEARCH Discretion）
    - 本任务不实现生产类；单测必须 RED
  </behavior>
  <action>
    在根 README 版本矩阵登记 jedis 5.2.0（先于 pom 依赖，per D-02）。新建 projects/p02-realtime-reco 最小 pom（复制 p01 属性区，追加 jedis/postgresql 坐标，数值抄 examples/pom.xml）。新建两份 JUnit 测试引用尚未存在的符号，故意让 mvn -Dtest=ParseBehaviorJsonTest,RuleScorerTest 失败（RED）。禁止把测试写成永远通过的空壳。禁止引入未登记 Maven 坐标（Package Legitimacy：仅 SSOT Approved）。
  </action>
  <verify>
    <automated>grep -v '^#' README.md | grep -qi jedis &amp;&amp; grep -q '5\.2\.0' README.md &amp;&amp; test -f projects/p02-realtime-reco/pom.xml &amp;&amp; cd projects/p02-realtime-reco &amp;&amp; mvn -q -Dtest=ParseBehaviorJsonTest,RuleScorerTest test; ec=$?; test $ec -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - README.md 版本矩阵含 jedis 与 5.2.0
    - pom.xml 含 artifactId p02-realtime-reco、flink.version 2.2.1、jedis.version 5.2.0；grep 不含 lettuce 与 flink-agents
    - 两测试文件存在且分别含 ParseBehaviorJson、RuleScorer（或 Top-K/score）断言关键字
    - mvn -Dtest=ParseBehaviorJsonTest,RuleScorerTest 退出码 ≠ 0（RED）
  </acceptance_criteria>
  <done>RECO-* Nyquist RED：jedis SSOT + 解析/打分单测夹具就位（per D-02/D-07）。</done>
</task>

<task type="auto">
  <name>Task 2: RED 骨架 — verify / drill-redis / loadtest 三脚本 + 双 DDL</name>
  <files>projects/p02-realtime-reco/scripts/verify.sh, projects/p02-realtime-reco/scripts/drill_redis_degrade.sh, projects/p02-realtime-reco/scripts/loadtest.sh, projects/p02-realtime-reco/sql/clickhouse_reco_results.sql, projects/p02-realtime-reco/sql/postgres_reco_items.sql</files>
  <read_first>
    - projects/p01-log-ai-platform/scripts/verify.sh（set -euo pipefail、CH 权威、白名单拼 SQL、Kafka 仅 diag）
    - projects/p01-log-ai-platform/scripts/loadtest.sh（--implemented 门闩失败态模式）
    - projects/p01-log-ai-platform/scripts/drill_ai_degrade.sh（外依赖降级演练骨架）
    - projects/p01-log-ai-platform/sql/clickhouse_log_results.sql（单语句 CREATE）
    - .planning/phases/05-p02/05-CONTEXT.md（D-06、D-10、D-11、D-12）
    - .planning/phases/05-p02/05-RESEARCH.md（reco_results / reco_items 列；feature_source REDIS|STATE_ONLY）
  </read_first>
  <behavior>
    - 三脚本均 set -euo pipefail；可 bash -n
    - verify.sh：声明 CH flinklab.reco_results；默认断言 count()&gt;=1；可选 FEATURE_SOURCE 白名单（REDIS|STATE_ONLY）；当前无作业/空表时非 0；禁止 Kafka 或 redis-cli KEYS 单独 exit 0（per D-10）
    - drill_redis_degrade.sh：声明 stop/pause fll-redis 后作业凭 Keyed State 仍产出 CH 行（feature_source=STATE_ONLY 或等价）；骨架默认 exit 1（per D-12）
    - loadtest.sh：声明 --rate/--duration → docs/baseline.md；骨架默认 exit 1（per D-11）；禁止 k6/JMeter
    - clickhouse_reco_results.sql：单语句 CREATE TABLE IF NOT EXISTS flinklab.reco_results，列含 user_id/item_id/score/event_time/reason/feature_source/feature_snapshot/ingest_time（per D-06）
    - postgres_reco_items.sql：CREATE TABLE reco_items(item_id PK, category, title, base_weight) + 至少 50 行种子 INSERT（per D-05）
  </behavior>
  <action>
    在 projects/p02-realtime-reco/scripts/ 新建三脚本；在 sql/ 写入两份 DDL。骨架须可执行且默认非 0（例如缺少 --implemented 或直接 FAIL: Wave0 stub）。文件头注释写明最终职责与权威出口（CH，非 Kafka/Redis）。verify.sh 须含 reco_results 字样及 feature_source 白名单意图。postgres SQL 可为多语句（由 psql -f 执行，非 CH HTTP）。禁止实现完整 E2E 绿路径与实测 baseline 数字。
  </action>
  <verify>
    <automated>bash -n projects/p02-realtime-reco/scripts/verify.sh &amp;&amp; bash -n projects/p02-realtime-reco/scripts/drill_redis_degrade.sh &amp;&amp; bash -n projects/p02-realtime-reco/scripts/loadtest.sh &amp;&amp; (! bash projects/p02-realtime-reco/scripts/verify.sh) &amp;&amp; (! bash projects/p02-realtime-reco/scripts/drill_redis_degrade.sh) &amp;&amp; (! bash projects/p02-realtime-reco/scripts/loadtest.sh) &amp;&amp; grep -q flinklab.reco_results projects/p02-realtime-reco/sql/clickhouse_reco_results.sql &amp;&amp; grep -q reco_items projects/p02-realtime-reco/sql/postgres_reco_items.sql</automated>
  </verify>
  <acceptance_criteria>
    - 三脚本存在且含 set -euo pipefail
    - verify.sh 含 reco_results；drill 含 redis 或 STATE_ONLY；loadtest 含 baseline
    - bash -n 均通过；直接执行三脚本退出码均 ≠ 0
    - CH SQL 单 CREATE 含 reco_results；PG SQL 含 reco_items 与 INSERT
  </acceptance_criteria>
  <done>RECO-01–03 Nyquist RED：验收/演练/压测入口与 DDL 骨架就位（per D-05/D-10/D-12）。</done>
</task>

<task type="auto">
  <name>Task 3: 骨架 — p02-init + make up-p02（不污染 default up）</name>
  <files>docker/docker-compose.yml, docker/Makefile</files>
  <read_first>
    - docker/docker-compose.yml（p01-init：profiles、KAFKA_IMAGE、wget --post-file 分 POST、topic 创建）
    - docker/Makefile（up-p01 显式 up p01-init；default up 不加 profile）
    - .planning/phases/05-p02/05-CONTEXT.md（D-08、D-09、D-10）
    - .planning/phases/05-p02/05-RESEARCH.md（topics reco.events / reco.results；p02-init 镜像 ${KAFKA_IMAGE}；PG seed 经 Makefile psql）
  </read_first>
  <behavior>
    - p02-init：profiles: [p02]；image ${KAFKA_IMAGE}；幂等创建 reco.events 与 reco.results（partitions≥1）；wget --post-file 挂载 clickhouse_reco_results.sql（单语句）；不新建 redis/postgres 服务（per D-08）
    - docker/Makefile：增加 up-p02 → docker compose --profile p02 up -d p02-init，并在目标内（或紧随）对 fll-postgres 执行 psql -f postgres_reco_items.sql 种子（per RESEARCH Discretion）；.PHONY 更新；禁止改 default up 目标加 --profile p02（per D-08）
  </behavior>
  <action>
    在 docker-compose.yml 追加 p02-init（对照 p01-init，改名与路径到 projects/p02-realtime-reco）。Makefile 增加 up-p02：先确保 redis/postgres 可达时可提示，再 up p02-init，再 docker compose exec -T postgres psql 种子 reco_items。禁止把 CH DDL 多语句塞进一次 POST。禁止引入未登记镜像。compose config 须仍可通过。
  </action>
  <verify>
    <automated>grep -q p02-init docker/docker-compose.yml &amp;&amp; grep -q 'reco.events' docker/docker-compose.yml &amp;&amp; grep -q 'reco.results' docker/docker-compose.yml &amp;&amp; grep -q up-p02 docker/Makefile &amp;&amp; grep -q 'profile p02' docker/Makefile &amp;&amp; (! grep -E '^up:.*profile p02' docker/Makefile) &amp;&amp; cd docker &amp;&amp; docker compose config -q &amp;&amp; docker compose --profile p02 config -q</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml 含 p02-init、reco.events、reco.results、clickhouse_reco_results.sql
    - Makefile 含 up-p02 且 default up 目标行不含 --profile p02
    - up-p02 路径含 postgres/psql 或 postgres_reco_items 种子意图
    - docker compose config -q 与 --profile p02 config -q 成功
  </acceptance_criteria>
  <done>RECO-01 骨架：p02 profile 钩子就位且不污染 default up（per D-08）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 造数/脚本 → ClickHouse HTTP | verify 拼 SQL；init POST DDL |
| 仓库 → Maven Central | pom 依赖解析（jedis 须先 SSOT） |
| compose profile → 基座网络 | p02-init 仅本地 lab 网络 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | verify.sh / 行为 JSON | mitigate | FEATURE_SOURCE 白名单后再拼 SQL；Parse 拒引号/反斜杠 userId（抄 p01 RULE_LABEL） |
| T-05-02 | Tampering | Redis key 构造 | mitigate | Wave 0 仅文档约定 feature:{userId}:*；实现期 userId 字符集校验（05-02） |
| T-05-05 | Tampering | p02 pom 依赖 | mitigate | 仅 SSOT Approved：jedis 5.2.0 / postgresql 42.7.4；禁止 lettuce/jedis6/Agents |
| T-05-06 | Information Disclosure | compose/CH/Redis 凭据 | accept | 复用 lab 演示口令；不新增云 API key；文档注明非生产 |
| T-05-SC | Tampering | Maven installs | mitigate | jedis/pg 已在 Package Legitimacy Approved；无 [SLOP]/[SUS]；先登记 README 再写 pom |
</threat_model>

<verification>
- README 含 jedis 5.2.0；两单测 surefire ≠ 0；三脚本直接执行 ≠ 0
- bash -n 三脚本通过；compose config -q 通过
- DDL + p02-init + up-p02 存在；default up 无 p02 profile
</verification>

<success_criteria>
- Wave 0 Nyquist 夹具全部可运行且为失败态
- p02 目录树与 compose 钩子落仓且不污染 default make up
- 不实现作业主类、完整 verify 绿、双通道特征、baseline（留给 05-01+）
</success_criteria>

<output>
Create `.planning/phases/05-p02/05-00-SUMMARY.md` when done
</output>
