---
phase: 06-p5
plan: 01
type: execute
wave: 1
depends_on:
  - "06-00"
files_modified:
  - benchmark/README.md
  - benchmark/baseline.md
  - benchmark/Makefile
  - benchmark/scripts/run_matrix.sh
  - benchmark/scripts/collect_metrics.py
  - docs/13-performance/README.md
  - docs/README.md
autonomous: true
requirements:
  - PROD-01
user_setup: []

must_haves:
  truths:
    - "维护者可在 OrbStack arm64 上执行 make -C benchmark matrix（或等价目标）跑完 D-01 裁剪必跑单元格并生成非空 benchmark/baseline.md（D-01/D-02/D-03）"
    - "baseline.md 含环境快照、负载定义、矩阵指标表与结论；热身 30–60s 偏差已相对「理想 3 分钟」声明；无 TBD/假数；20k/ForSt 若未跑须 SKIPPED+原因"
    - "docs 模块 13 完成态指向 benchmark/ 权威路径（D-13）"
  artifacts:
    - path: "benchmark/baseline.md"
      provides: "仓库级压测权威报告（PROD-01 / D-03）"
      contains: "OrbStack"
    - path: "benchmark/scripts/run_matrix.sh"
      provides: "裁剪矩阵编排（D-01）"
      contains: "HashMap|RocksDB|eps"
    - path: "benchmark/scripts/collect_metrics.py"
      provides: "Prometheus 指标刮取"
      contains: "prometheus|query"
    - path: "benchmark/Makefile"
      provides: "matrix/baseline 一键入口"
      contains: "matrix"
    - path: "benchmark/README.md"
      provides: "裁剪矩阵轴说明（替换全笛卡尔积诱惑）"
      contains: "e01-J2|p03|1k"
    - path: "docs/13-performance/README.md"
      provides: "模块 13 完成态正文（D-13）"
      contains: "benchmark"
  key_links:
    - from: "run_matrix.sh"
      to: "gen_events.py / gen_vehicle_events.py"
      via: "按作业轴调用既有 Python 造数，无 k6"
      pattern: "gen_events|gen_vehicle|eps|rate"
    - from: "run_matrix.sh"
      to: "benchmark/baseline.md"
      via: "实测行写入权威报告"
      pattern: "baseline\\.md"
    - from: "docs/13-performance"
      to: "benchmark/baseline.md"
      via: "完成态互链"
      pattern: "baseline"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 在 OrbStack arm64 上跑通压测矩阵、Operator Blue/Green、单一 Argo GitOps/CI，并交付完整规范/题库/三块 Grafana 看板，**so that** P5 生产化层可简历陈述且每项均可观察复现。

<objective>
V1 压测矩阵垂直切片（PROD-01）：可运行裁剪矩阵 harness，并在 OrbStack 产出仓库级 `benchmark/baseline.md`。

Purpose: D-14 第 (1) 波——先交付可复现压测证据；PROD-01 不依赖 K8s Operator。
Output: run_matrix/collect_metrics/Makefile GREEN + 实测 baseline.md + README 裁剪说明 + docs 模块 13 完成态；不装 Operator、不交 Argo、不扩 interview。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-p5/06-00-SUMMARY.md
@.planning/phases/06-p5/06-CONTEXT.md
@.planning/phases/06-p5/06-RESEARCH.md
@.planning/phases/06-p5/06-PATTERNS.md
@benchmark/README.md
@projects/p03-vehicle-monitoring/scripts/loadtest.sh
@projects/p03-vehicle-monitoring/docs/baseline.md
@scripts/gen_events.py
@examples/e01-datastream
@examples/e10-cep
@monitoring/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现裁剪矩阵 harness（compose Flink + Python 造数 + Prom 刮取）</name>
  <files>benchmark/scripts/run_matrix.sh, benchmark/scripts/collect_metrics.py, benchmark/Makefile, benchmark/README.md</files>
  <read_first>
    - .planning/phases/06-p5/06-CONTEXT.md（D-01/D-02/D-03 矩阵轴与驱动锁定）
    - .planning/phases/06-p5/06-RESEARCH.md（Pattern 1 最小必跑集；Open Questions RESOLVED：PROD-01 默认 compose Flink）
    - .planning/phases/06-p5/06-VALIDATION.md（PROD-01 验收命令）
    - .planning/phases/06-p5/06-PATTERNS.md（loadtest/collect_metrics/Makefile 模式）
    - projects/p03-vehicle-monitoring/scripts/loadtest.sh（完整 warmup/measure/scrape/写报告）
    - scripts/gen_events.py（`--eps` / `--bootstrap`）
    - projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py（`--rate`/`--duration`）
    - examples/e10-cep/README.md（锁定 C5VehicleDtcPatternJob 作为 e10 轴主类，per RESEARCH A3）
    - monitoring/README.md（值班五指标名，禁止臆造 PromQL）
    - benchmark/scripts/run_matrix.sh（06-00 失败态骨架）
  </read_first>
  <action>
    将 06-00 骨架升级为可跑 harness（per D-01/D-02）：
    1) 作业轴：e01-J2 窗口聚合、e10 CEP（固定 `C5VehicleDtcPatternJob`）、p03 `VehicleAlertJob`；复用既有 submit/package 路径与 Python gen，禁止 k6/JMeter。e10 造数锁定复用 `scripts/gen_events.py`（或 e10 README 已文档化的等价命令）投递到 e10 作业消费的 topic；若 topic/格式不兼容，仅允许最小补丁脚本 `benchmark/scripts/gen_e10_events.py`（不得引入新镜像）。
    2) 必跑单元格：覆盖 RESEARCH Pattern 1 表中 #1–#7 与 1–2 行 checkpoint 对照（非对齐或 10s）；20k eps / ForSt 为 stretch——不稳则跳过并在报告标 SKIPPED+原因，禁止填假数。
    3) State：HashMap + RocksDB 增量；Checkpoint 主路径对齐+30s。
    4) 热身默认 30–60s（可用 `WARMUP_SEC`），须在后续 baseline 声明相对理想 3 分钟偏差。
    5) `collect_metrics.py` 从 Prometheus 刮取值班五指标（名以 `:9249`/Prom 实际 series 为准）；输出 key=value 供 shell 拼表。
    6) `Makefile`：`matrix`（全量必跑）、`dry-run`（低 EPS 冒烟，如 EPS=100 单单元格）；失败非 0。
    7) 回填 `benchmark/README.md`：写明 CONTEXT 裁剪轴，删除「e05/倾斜全轴必跑」误导。
    部署形态锁定 compose Flink（非 K8s），与现有 `make up` / submit 一致。
  </action>
  <verify>
    <automated>bash -n benchmark/scripts/run_matrix.sh &amp;&amp; make -C benchmark dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `make -C benchmark dry-run` exit 0（低负载冒烟至少完成 1 个单元格的提交/造数/刮取路径）
    - `rg -n "k6|JMeter" benchmark/` 无匹配
    - `rg -n "e01-J2|C5VehicleDtcPatternJob|VehicleAlertJob" benchmark/scripts/run_matrix.sh` 均命中
    - `rg -n "WARMUP|warmup" benchmark/scripts/run_matrix.sh` 命中且默认热身在 30–60 秒区间或可由环境变量覆盖到该区间
    - `benchmark/README.md` 明确 1k/5k 必跑、20k stretch、作业轴含 p03（不再把 e05 倾斜列为必跑）
  </acceptance_criteria>
  <done>矩阵 harness 可 dry-run 绿；README 裁剪轴与 D-01 一致</done>
</task>

<task type="auto">
  <name>Task 2: OrbStack 实测全量必跑矩阵并写入 baseline.md + 模块 13 完成态</name>
  <files>benchmark/baseline.md, docs/13-performance/README.md, docs/README.md</files>
  <read_first>
    - projects/p03-vehicle-monitoring/docs/baseline.md（四段结构模板）
    - projects/p01-log-ai-platform/docs/baseline.md（交叉引用句式）
    - benchmark/README.md（报告模板）
    - docs/README.md（模块 15 完成态句式）
    - docs/13-performance/README.md（06-00 骨架）
    - .planning/phases/06-p5/06-CONTEXT.md（D-03 权威路径；项目级 baseline 不替代仓库级）
  </read_first>
  <action>
    1) 在 OrbStack arm64、compose 基座已 up 的前提下执行 `make -C benchmark matrix`（或文档固定的全量入口），生成/覆盖 `benchmark/baseline.md`。
    2) baseline 必须含：采集 UTC 时间、Darwin arm64、Flink/TM/slots、热身偏差声明、矩阵表（Job/eps/State/Checkpoint/指标列）、复跑命令、SKIPPED 单元格原因（若有）。数字仅本次实测。
    3) 在 p03/p01/p02 项目级 `docs/baseline.md` 顶部或 README 增加指向 `benchmark/baseline.md` 的交叉引用一句（若改项目文件，仅加互链一句，不改其数字）——优先只改 `benchmark/README` + 模块 13 说明「项目级不替代仓库级」（D-03）；若为保持文件所有权干净，本任务可不改 projects/*。
    4) 将 `docs/13-performance/README.md` 与 `docs/README.md` 模块 13 升级为完成态 ✅，权威路径指向 `benchmark/` 与 `benchmark/baseline.md`（D-13）。
    5) 解除 run_matrix 的失败门闩（`--implemented` 默认真或删除门闩），使 `make -C benchmark matrix` 在成功跑完后 exit 0。
  </action>
  <verify>
    <automated>test -s benchmark/baseline.md &amp;&amp; rg -n "TODO|TBD|FIXME|请参考官网" benchmark/baseline.md; test $? -ne 0 || test -z "$(rg -n 'TODO|TBD|FIXME|请参考官网' benchmark/baseline.md || true)" &amp;&amp; rg -n "采集|环境|指标|1k|5k|HashMap|RocksDB" benchmark/baseline.md | head -20 &amp;&amp; rg -n "baseline.md|完成" docs/README.md docs/13-performance/README.md | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -s benchmark/baseline.md` 为真；文件含 UTC 采集时间与 arm64/OrbStack 环境句
    - `rg -n 'TODO|TBD|FIXME|未实测数字' benchmark/baseline.md` 无匹配
    - baseline 矩阵表至少覆盖 D-01 必跑：三作业 ×（1k 与 5k 出现）×（HashMap 与 RocksDB 增量均出现）× 对齐 30s 主路径，外加 ≥1 行 checkpoint 对照
    - `make -C benchmark matrix` 在已 up 环境 exit 0（或脚本末尾明确复跑命令且本次执行日志证明已跑通——以 exit 0 为准）
    - `docs/README.md` 模块 13 为完成态并链接 `benchmark/baseline.md`
  </acceptance_criteria>
  <done>PROD-01：仓库级 baseline.md 含 OrbStack 实测矩阵；模块 13 完成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 造数客户端 → Kafka | 压测流量进入演示总线 |
| harness → Prometheus | 只读查询指标写入报告 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | baseline.md 数字 | mitigate | 仅脚本刮取写入；禁止手填未跑单元格；SKIPPED 必写原因 |
| T-06-01-02 | Denial of Service | 高 eps 压测 | accept | 学习工程本机；5k 必跑、20k stretch；失败非 0 不硬灌 |
| T-06-01-03 | Information Disclosure | baseline 环境快照 | accept | 无生产 secret；仅本机演示拓扑 |
| T-06-01-SC | Tampering | 无新包管理器依赖 | accept | 复用既有 Python/uv；无 npm/pip 新包 |
</threat_model>

<verification>
PROD-01 完成：`make -C benchmark matrix` 绿且 `benchmark/baseline.md` 非空无占位假数；模块 13 完成态可检索。
</verification>

<success_criteria>
- 裁剪矩阵可一键跑
- 权威 baseline 仅含实测
- docs 13 完成
</success_criteria>

<output>
Create `.planning/phases/06-p5/06-01-SUMMARY.md` when done
</output>
