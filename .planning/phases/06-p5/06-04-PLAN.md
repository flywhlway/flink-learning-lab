---
phase: 06-p5
plan: 04
type: execute
wave: 3
depends_on:
  - "06-03"
files_modified:
  - monitoring/platform-overview.json
  - monitoring/job-deepdive.json
  - monitoring/ai-cost.json
  - monitoring/README.md
  - docker/config/grafana/provisioning/dashboards/dashboards.yml
  - docker/docker-compose.yml
  - interview/README.md
  - scripts/count_interview.py
  - best-practice/README.md
  - docs/14-production/README.md
  - docs/README.md
  - README.md
  - PHASES.md
  - CHANGELOG.md
autonomous: true
requirements:
  - PROD-04
user_setup: []

must_haves:
  truths:
    - "monitoring/ 恰好 3 个可导入 Grafana dashboard JSON（平台总览/作业深潜/AI 专项），经 provisioning 或文档一键导入可在 Grafana 打开（D-10）"
    - "interview/ 题量 ≥150，Level 分层保留，每题含完整参考答案或等价完整考点推导（D-11）"
    - "best-practice/ 扩展为完整规范体系并覆盖 D-12 所列主题，与 production/ 互链（D-12）"
    - "docs 模块 14 完成态登记；根 README 目录状态与 PHASES/CHANGELOG 反映 P5 完成（D-13 + 会话收尾）"
  artifacts:
    - path: "monitoring/platform-overview.json"
      provides: "平台总览看板（D-10）"
      contains: "panels"
    - path: "monitoring/job-deepdive.json"
      provides: "作业深潜看板（D-10）"
      contains: "panels"
    - path: "monitoring/ai-cost.json"
      provides: "AI 专项看板（D-10）"
      contains: "panels"
    - path: "interview/README.md"
      provides: "≥150 题索引与答案（D-11）"
      contains: "L1"
    - path: "scripts/count_interview.py"
      provides: "题量门禁 ≥150"
      contains: "150"
    - path: "best-practice/README.md"
      provides: "完整规范索引（D-12）"
      contains: "CI-CD|GitOps|降级|uid"
    - path: "docs/14-production/README.md"
      provides: "模块 14 完成态（D-13）"
      contains: "production"
  key_links:
    - from: "dashboards.yml / compose volume"
      to: "monitoring/*.json"
      via: "Grafana file provider 挂载"
      pattern: "monitoring|dashboards"
    - from: "ai-cost.json"
      to: "p01 实际指标名"
      via: "先 :9249/Prom 核实再写 expr（禁臆造）"
      pattern: "guardrail|budget|ai_"
    - from: "best-practice"
      to: "production/docs"
      via: "规范正文↔落地清单互链"
      pattern: "production/"
    - from: "count_interview.py"
      to: "interview/**"
      via: "计数 ≥150 exit 0"
      pattern: "150"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 在 OrbStack arm64 上跑通压测矩阵、Operator Blue/Green、单一 Argo GitOps/CI，并交付完整规范/题库/三块 Grafana 看板，**so that** P5 生产化层可简历陈述且每项均可观察复现。

<objective>
V4 规范/题库/看板垂直切片（PROD-04）：三块 Grafana JSON 可导入 + interview≥150 完整答案 + best-practice 体系 + docs 14/收尾。

Purpose: D-14 第 (4) 波——内容与可观测资产收官；Loki/OTel 不挡门禁（Deferred）。
Output: 恰好 3 JSON + provisioning、interview 计数绿、best-practice 完整互链、模块 14/PHASES/CHANGELOG/README 状态更新。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-p5/06-03-SUMMARY.md
@.planning/phases/06-p5/06-CONTEXT.md
@.planning/phases/06-p5/06-RESEARCH.md
@.planning/phases/06-p5/06-PATTERNS.md
@monitoring/README.md
@projects/p03-vehicle-monitoring/monitoring/dashboards/p03-vehicle-overview.json
@projects/p03-vehicle-monitoring/scripts/verify_dashboard.sh
@docker/config/grafana/provisioning/dashboards/dashboards.yml
@docker/docker-compose.yml
@interview/README.md
@best-practice/README.md
@projects/p01-log-ai-platform/docs/DEGRADE-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 恰好三块 Grafana JSON + provisioning 可打开</name>
  <files>monitoring/platform-overview.json, monitoring/job-deepdive.json, monitoring/ai-cost.json, monitoring/README.md, docker/config/grafana/provisioning/dashboards/dashboards.yml, docker/docker-compose.yml</files>
  <read_first>
    - .planning/phases/06-p5/06-CONTEXT.md（D-10：恰好 3 块；Loki/OTel 非硬验收）
    - .planning/phases/06-p5/06-PATTERNS.md（p03 dashboard JSON 结构、datasource UID、compose volume）
    - projects/p03-vehicle-monitoring/monitoring/dashboards/p03-vehicle-overview.json
    - projects/p03-vehicle-monitoring/scripts/verify_dashboard.sh
    - docker/config/grafana/provisioning/dashboards/dashboards.yml
    - docker/config/grafana/provisioning/datasources/（Prometheus/ClickHouse uid）
    - monitoring/README.md（值班五指标）
    - projects/p01-log-ai-platform 中 Guardrail/BudgetGate 指标注册名（先 grep `:9249` 或源码 counter 名）
  </read_first>
  <action>
    1) 在 `monitoring/` 创建恰好三个 JSON：`platform-overview.json`、`job-deepdive.json`、`ai-cost.json`（文件名可微调但必须仍是恰好 3 个 `monitoring/*.json`）。
    2) 复用已有 datasource uid（Prometheus `PBFA97CFB590B2093` 或 provisioning 实际 uid；ClickHouse `p03-clickhouse` 如适用）；面板覆盖值班五指标（平台/作业）与 AI 专项（token/成本类或等价业务指标：`budget_trips`/`guardrail_blocks`/`ai_*`——以实际暴露名为准，禁止臆造全名）。
    3) 扩展 `dashboards.yml` provider + `docker-compose.yml` Grafana volume，将 `monitoring/` 挂入可加载路径；或提供文档化的 Grafana HTTP API 一键导入命令（二选一，须可验证打开）。
    4) 更新 `monitoring/README.md`：三块看板说明 + 打开路径；明确 Loki/OTel 为可选增强非本 Phase 硬门禁。
    5) 可用 `verify_dashboard.sh` 模式写最小校验（非空 JSON、search API 可见标题）——失败非 0。
  </action>
  <verify>
    <automated>test $(ls monitoring/*.json 2&gt;/dev/null | wc -l | tr -d ' ') -eq 3 &amp;&amp; python3 -c "import json,glob; [json.load(open(p)) for p in glob.glob('monitoring/*.json')]" &amp;&amp; rg -n "platform|job|ai|看板" monitoring/README.md | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `ls monitoring/*.json | wc -l` 等于 3
    - 三个 JSON 均可 `json.load`；各含非空 `panels`
    - Grafana provisioning 或文档导入路径存在：`rg -n "monitoring" docker/config/grafana/provisioning/dashboards/dashboards.yml docker/docker-compose.yml` 命中挂载/provider，或 `monitoring/README.md` 含可执行 curl 导入命令
    - 在 Grafana（基座 up）`/api/search` 能查到三块标题之一（用 curl + admin/flinklab 或 verify 脚本证明）
    - `rg -n "TODO|请参考官网" monitoring/*.json monitoring/README.md` 无匹配
  </acceptance_criteria>
  <done>三块看板可导入/可打开（D-10）</done>
</task>

<task type="auto">
  <name>Task 2: interview ≥150 完整答案 + count_interview 绿</name>
  <files>interview/README.md, scripts/count_interview.py</files>
  <read_first>
    - .planning/phases/06-p5/06-CONTEXT.md（D-11）
    - interview/README.md（首批 30 题 Level 分层）
    - scripts/count_interview.py（06-00 门禁）
    - docs/README.md（教材章节锚点可用）
    - .planning/phases/06-p5/06-PATTERNS.md（interview 扩展纪律）
  </read_first>
  <action>
    1) 将题库扩至 ≥150 题，保持 L1–L8+ 分层（可拆 `interview/L*/` 文件，Discretion）；保留并升级原 30 题答案完整度。
    2) 每题必须附完整参考答案或等价完整考点推导（可链回 `docs/`/`ai/` 章节）；禁止只留空题干或「考点骨架」了事（D-11）。
    3) 确保 `python3 scripts/count_interview.py` 计数逻辑与实际题号格式一致，≥150 时 exit 0。
    4) 更新 `interview/README.md` 导航与使用方式（先自答再看答案）。
    5) 内容禁止违禁词与空洞占位。
  </action>
  <verify>
    <automated>python3 scripts/count_interview.py &amp;&amp; python3 scripts/count_interview.py | tee /tmp/interview-count.txt</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/count_interview.py` exit 0
    - 计数输出 ≥150
    - 抽样：原 L1 第 1 题与新增题各至少一题在文件中含「答案」或等价完整推导段落（非仅括号考点）
    - `rg -n "TODO|请参考官网|完整推导回教材" interview/` 不得以「完整推导回教材」作为唯一答案代替正文（允许链回章节，但本题须有答案正文）
  </acceptance_criteria>
  <done>interview ≥150 且门禁绿（D-11）</done>
</task>

<task type="auto">
  <name>Task 3: best-practice 完整体系 + docs 14/README/PHASES/CHANGELOG 收尾</name>
  <files>best-practice/README.md, docs/14-production/README.md, docs/README.md, README.md, PHASES.md, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/06-p5/06-CONTEXT.md（D-12/D-13）
    - best-practice/README.md（首批 12 条体例：规则+理由+反例）
    - production/docs/gitops-cicd.md（互链落点）
    - production/docs/bluegreen-sop.md
    - docs/README.md（模块 14 与完成态句式）
    - README.md（目录状态列 production/benchmark/monitoring）
    - PHASES.md（P5 验收口径）
    - CHANGELOG.md（未发布区格式）
    - projects/p01-log-ai-platform/docs/DEGRADE-CHECKLIST.md（AI 降级互链）
  </read_first>
  <action>
    1) 扩展 `best-practice/` 为完整规范体系（可多文件）：至少覆盖 D-12——架构/目录命名、uid/savepoint、checkpoint/Kafka 事务、状态与 TTL、反压基线、日志/异常、CI-CD/GitOps 检查清单、AI 降级；每条保持「规则+理由+反例」；与 `production/docs/*` **双向互链**（规范在 best-practice，落地清单在 production）。
    2) `docs/14-production/README.md` + `docs/README.md` 模块 14 → 完成态 ✅，指向 `production/`、`monitoring/`、`best-practice/` 权威路径（D-13）。
    3) 根 README 目录表：`production/`/`benchmark/`/`monitoring/`/`best-practice/`/`interview/` 从蓝图改为完成态表述；版本矩阵已含本 Phase 组件则勿漂移。
    4) 更新 `PHASES.md` P5 状态为完成/可验证；`CHANGELOG.md` 未发布区记录 P5 生产化交付要点。
    5) 跑 `bash scripts/qa_check.sh`（允许本 Phase 已知范围；若仅因 Phase 7 计量失败须在 SUMMARY 注明，但违禁词/断链不得新引入）。
  </action>
  <verify>
    <automated>rg -n "uid|savepoint|checkpoint|TTL|反压|GitOps|降级|CI" best-practice/README.md best-practice/*.md 2&gt;/dev/null | head -30 &amp;&amp; rg -n "模块 14|完成" docs/README.md docs/14-production/README.md | head -10 &amp;&amp; rg -n "production/|benchmark/|monitoring/" README.md | head -15 &amp;&amp; python3 scripts/count_interview.py &amp;&amp; test $(ls monitoring/*.json | wc -l | tr -d ' ') -eq 3</automated>
  </verify>
  <acceptance_criteria>
    - best-practice 索引或分章覆盖 D-12 全部主题关键词（uid/savepoint、checkpoint/Kafka、TTL、反压、日志/异常、CI-CD/GitOps、AI 降级、架构/目录命名）
    - `rg -n "production/" best-practice/` 与 `rg -n "best-practice" production/` 均有互链命中
    - docs 模块 14 完成态链接有效
    - README 目录表不再将 production/benchmark/monitoring 标为「仅蓝图/未落地」
    - PHASES.md 与 CHANGELOG 未发布区含 P5/Phase 6 相关完成表述
    - `python3 scripts/count_interview.py` exit 0 且 monitoring JSON 数为 3（回归）
  </acceptance_criteria>
  <done>PROD-04 与会话收尾完成；P5 内容资产可陈述</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grafana UI ← dashboard JSON | 看板定义进入可观测面 |
| 文档内容 → 学习者 | 题库/规范被当作权威指导 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | 恶意 dashboard JSON | mitigate | 仅本仓评审；复用已有 datasource uid；不对外部 URL apply |
| T-06-04-02 | Spoofing | 臆造 PromQL 误导值班 | mitigate | AI/平台面板查询先核实 :9249/Prom series |
| T-06-04-03 | Information Disclosure | Grafana 默认口令 | accept | 本机演示口令已有；文档不新增云密钥 |
| T-06-04-SC | Tampering | 无新包安装 | accept | 纯 JSON/Markdown 内容；无 npm/pip |
</threat_model>

<verification>
PROD-04：3 JSON 可打开；interview count≥150；best-practice 体系+互链；docs 14/README/PHASES/CHANGELOG 收尾。
</verification>

<success_criteria>
- D-10/D-11/D-12/D-13 全部可观察满足
- 与 06-01..03 产物共同构成 Phase 6 成功标准
</success_criteria>

<output>
Create `.planning/phases/06-p5/06-04-SUMMARY.md` when done
</output>
