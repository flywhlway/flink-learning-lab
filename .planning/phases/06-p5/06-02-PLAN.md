---
phase: 06-p5
plan: 02
type: execute
wave: 1
depends_on:
  - "06-00"
files_modified:
  - production/README.md
  - production/scripts/install-operator.sh
  - production/scripts/run-bluegreen-drill.sh
  - production/scripts/probe_kafka_from_k8s.sh
  - production/docs/operator-install.md
  - production/docs/bluegreen-sop.md
  - production/docs/bluegreen-timeline.md
  - production/charts/p03-vehicle-alert/Chart.yaml
  - production/charts/p03-vehicle-alert/values.yaml
  - production/charts/p03-vehicle-alert/templates/flink-bluegreen.yaml
  - production/docker/p03-k8s-image/Dockerfile
autonomous: true
requirements:
  - PROD-02
user_setup: []

must_haves:
  truths:
    - "OrbStack K8s 上 Helm 安装的 Flink Kubernetes Operator 1.15.0 可用，CRD flinkbluegreendeployments 存在（D-04）"
    - "p03 VehicleAlertJob 以 FlinkBlueGreenDeployment 部署并可观察 ACTIVE_BLUE↔ACTIVE_GREEN（或等价 ACTIVE_*）迁移（D-05）"
    - "production/scripts/run-bluegreen-drill.sh exit 0 且写出含 kubectl 事件/CR 状态/子 FlinkDeployment 片段的 production/docs/bluegreen-timeline.md（D-06）"
  artifacts:
    - path: "production/scripts/install-operator.sh"
      provides: "Operator 1.15.0 Helm 安装（D-04）"
      contains: "1.15.0"
    - path: "production/charts/p03-vehicle-alert/templates/flink-bluegreen.yaml"
      provides: "FlinkBlueGreenDeployment 包装 VehicleAlertJob（D-05）"
      contains: "FlinkBlueGreenDeployment"
    - path: "production/scripts/run-bluegreen-drill.sh"
      provides: "可观察 BG 演练入口（D-06）"
      contains: "bluegreen-timeline"
    - path: "production/docs/bluegreen-timeline.md"
      provides: "脚本化时间线证据（D-06）"
      contains: "FlinkBlueGreenDeployment|FlinkDeployment|Events"
    - path: "production/docs/operator-install.md"
      provides: "可复现安装 SOP（D-09）"
      contains: "helm|orb"
    - path: "production/docs/bluegreen-sop.md"
      provides: "Blue/Green 操作与回滚步骤（D-09）"
      contains: "TRANSITION|rollback|回滚"
  key_links:
    - from: "flink-bluegreen.yaml"
      to: "VehicleAlertJob"
      via: "entryClass + jarURI 对齐 p03 Makefile"
      pattern: "VehicleAlertJob|p03-vehicle"
    - from: "run-bluegreen-drill.sh"
      to: "bluegreen-timeline.md"
      via: "演练采集 tee/写入证据"
      pattern: "bluegreen-timeline"
    - from: "values / Job args"
      to: "compose Kafka 宿主机地址"
      via: "禁止 kafka:9092；探针锁定 bootstrap（RESEARCH Pattern 3）"
      pattern: "bootstrap|host.docker.internal|9094"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 在 OrbStack arm64 上跑通压测矩阵、Operator Blue/Green、单一 Argo GitOps/CI，并交付完整规范/题库/三块 Grafana 看板，**so that** P5 生产化层可简历陈述且每项均可观察复现。

<objective>
V2 Operator Blue/Green 垂直切片（PROD-02）：Helm 装 Operator 1.15 + p03 FlinkBlueGreenDeployment 可观察演练时间线。

Purpose: D-14 第 (2) 波——硬门禁是真实 BG 状态机时间线，不是 upgradeMode 散文；可与 06-01 并行（不共享文件）。
Output: install-operator + chart + drill 绿 + timeline/SOP/install 文档；不装 Argo、不写 GHA、不扩题库。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-p5/06-00-SUMMARY.md
@.planning/phases/06-p5/06-CONTEXT.md
@.planning/phases/06-p5/06-RESEARCH.md
@.planning/phases/06-p5/06-PATTERNS.md
@production/README.md
@projects/p03-vehicle-monitoring/Makefile
@projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helm 安装 Operator 1.15 + K8s↔Kafka 探针 + p03 FlinkBlueGreenDeployment chart</name>
  <files>production/scripts/install-operator.sh, production/scripts/probe_kafka_from_k8s.sh, production/charts/p03-vehicle-alert/Chart.yaml, production/charts/p03-vehicle-alert/values.yaml, production/charts/p03-vehicle-alert/templates/flink-bluegreen.yaml, production/docker/p03-k8s-image/Dockerfile, production/docs/operator-install.md</files>
  <read_first>
    - .planning/phases/06-p5/06-RESEARCH.md（Helm 安装块、Pattern 2 FlinkBlueGreenDeployment YAML、Pattern 3 混合拓扑、A1 网络、webhook.create=false）
    - .planning/phases/06-p5/06-CONTEXT.md（D-04/D-05）
    - .planning/phases/06-p5/06-PATTERNS.md（install-operator / chart 模式）
    - projects/p03-vehicle-monitoring/Makefile（主类 `com.flywhl.flinklab.p03.VehicleAlertJob`、jar 命名）
    - README.md（已登记的 chart 坐标，禁止漂移）
    - production/scripts/check_env.sh（门禁前置）
  </read_first>
  <action>
    1) `production/scripts/install-operator.sh`：调用/依赖 `check_env.sh`；`helm repo add` 使用 README SSOT 的 downloads.apache.org 1.15.0 坐标；`helm install`（或 upgrade --install）`flink-kubernetes-operator`，`--set webhook.create=false`，`--set image.tag=1.15.0`，namespace `flink-operator`；断言 `kubectl get crd flinkbluegreendeployments.flink.apache.org` 成功。禁止手写 Deployment 冒充 Operator。
    2) `probe_kafka_from_k8s.sh`：用临时 Pod/`nc` 实测 K8s→宿主机 Kafka（及文档化 MinIO 若作业需要）；锁定一种 bootstrap 写法写入 `operator-install.md` 与 chart `values.yaml`（禁止 `kafka:9092`）。
    3) 构建本地 Application 镜像（Dockerfile 基于 `flink:2.2.1-java21`，拷贝 p03 shade jar）：tag **非** `:latest`（如 `flinklab/p03-vehicle-alert:dev`），`imagePullPolicy: IfNotPresent`（OrbStack 纪律）。
    4) Helm chart `production/charts/p03-vehicle-alert`：`kind: FlinkBlueGreenDeployment`，`flinkVersion: v2_2`，`job.entryClass`/`jarURI` 对齐 p03；`upgradeMode: savepoint`；作业参数含宿主机 bootstrap 与必要 topic。Session/Autoscaler 仅可在文档附录提及，不进硬门禁（D-05）。
    5) `operator-install.md`：从 `orb start k8s` → helm install → 探针 → `helm install` chart 的逐步可跟命令，无「请参考官网」。
  </action>
  <verify>
    <automated>bash production/scripts/check_env.sh &amp;&amp; bash production/scripts/install-operator.sh &amp;&amp; kubectl get crd flinkbluegreendeployments.flink.apache.org &amp;&amp; test -f production/charts/p03-vehicle-alert/templates/flink-bluegreen.yaml &amp;&amp; rg -n "FlinkBlueGreenDeployment|VehicleAlertJob|v2_2|IfNotPresent" production/charts/p03-vehicle-alert/templates/flink-bluegreen.yaml production/charts/p03-vehicle-alert/values.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `kubectl get crd flinkbluegreendeployments.flink.apache.org` 成功
    - `kubectl get pods -n flink-operator`（或 install 脚本实际 namespace）显示 operator 相关 Pod Running
    - chart 模板含 `kind: FlinkBlueGreenDeployment` 与 `VehicleAlertJob`；镜像 tag 不含 `latest`
    - `rg -n "kafka:9092" production/charts production/docs/operator-install.md` 无匹配
    - `production/docs/operator-install.md` 含可复制 helm/kubectl 命令且无违禁词
    - `bash production/scripts/probe_kafka_from_k8s.sh` exit 0（或在 operator-install 中记录已验证的 bootstrap 且探针脚本 exit 0）
  </acceptance_criteria>
  <done>Operator 1.15 已装；p03 BG chart 与网络探针就绪</done>
</task>

<task type="auto">
  <name>Task 2: 脚本化 Blue/Green 演练产出时间线证据 + SOP</name>
  <files>production/scripts/run-bluegreen-drill.sh, production/docs/bluegreen-timeline.md, production/docs/bluegreen-sop.md, production/README.md</files>
  <read_first>
    - .planning/phases/06-p5/06-CONTEXT.md（D-06 时间线必备字段）
    - .planning/phases/06-p5/06-RESEARCH.md（BG 状态机 ACTIVE_* / TRANSITIONING_*；时间线骨架；Pitfall 2 勿把 upgradeMode 当 BG）
    - projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh（阶段标签与失败非 0）
    - production/scripts/run-bluegreen-drill.sh（06-00 RED 骨架）
    - production/charts/p03-vehicle-alert/values.yaml（触发 TRANSITION 的字段：image tag 或 flinkConfiguration）
  </read_first>
  <action>
    1) 升级 `run-bluegreen-drill.sh`：确保 chart 已部署且作业 RUNNING；采集阶段写入 `production/docs/bluegreen-timeline.md`（UTC、`FlinkBlueGreenDeployment` status yaml 片段、子 `FlinkDeployment` wide、events tail）；触发一次真实 TRANSITION（改 template 需迁移字段）；轮询至对侧 `ACTIVE_*`；超时 FAIL 非 0。
    2) 时间线必须可复现命令驱动，禁止「仅截图散文」（D-06）。
    3) `bluegreen-sop.md`：部署→观察→切换→回滚（abort/回切）逐步命令；标明硬门禁是 BG CR 状态机，三种 upgradeMode 仅附录对照。
    4) 回填 `production/README.md` 为可复现总入口（安装/演练/文档链接），仍声明 GitOps 细节在下一波，但不留 TODO/请参考官网。
  </action>
  <verify>
    <automated>bash production/scripts/run-bluegreen-drill.sh &amp;&amp; test -s production/docs/bluegreen-timeline.md &amp;&amp; rg -n "ACTIVE_|FlinkDeployment|Events|UTC" production/docs/bluegreen-timeline.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `bash production/scripts/run-bluegreen-drill.sh` exit 0
    - `production/docs/bluegreen-timeline.md` 非空，且同时出现：UTC 时间戳、`FlinkBlueGreenDeployment`（或 status）、`FlinkDeployment`、Events/事件 片段
    - 时间线或 SOP 中可见至少一次状态迁移（如 TRANSITIONING_* 或 ACTIVE_BLUE→ACTIVE_GREEN / 反向）
    - `rg -n "TODO|请参考官网|省略" production/docs/bluegreen-sop.md production/docs/bluegreen-timeline.md production/README.md` 无匹配
    - Autoscaler/Session 若出现仅在附录且不作为 exit 0 条件
  </acceptance_criteria>
  <done>PROD-02：可观察 Blue/Green 时间线与 SOP 齐备</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helm → K8s API | Operator/chart 安装改变集群状态 |
| K8s Flink → compose Kafka/MinIO | 混合拓扑跨引擎访问 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Elevation of Privilege | Operator cluster RBAC | mitigate | 使用官方 chart 默认 SA；演练限 flink/flink-operator namespace |
| T-06-02-02 | Tampering | 误用 upgradeMode 冒充 BG | mitigate | 硬门禁要求 FlinkBlueGreenDeployment 状态机 + 时间线脚本 |
| T-06-02-03 | Denial of Service | 演练拖垮本机 | accept | 单作业 p03；资源 requests 学习工程级 |
| T-06-02-04 | Information Disclosure | timeline 日志 | accept | 本地演示事件，无云凭证 |
| T-06-02-SC | Tampering | Helm chart 供应链 | mitigate | 仅 Apache 1.15.0 发行坐标（SSOT）；webhook 关闭免自签证书手搓 |
</threat_model>

<verification>
PROD-02：Operator CRD 存在；drill 绿；timeline 含事件与子 Deployment 状态。
</verification>

<success_criteria>
- Operator 1.15.0 可观察
- p03 BG 演练可复现
- D-06 时间线文件为脚本产物
</success_criteria>

<output>
Create `.planning/phases/06-p5/06-02-SUMMARY.md` when done
</output>
