---
phase: 06-p5
plan: 03
type: execute
wave: 2
depends_on:
  - "06-02"
files_modified:
  - production/argocd/application-p03.yaml
  - production/scripts/install-argocd.sh
  - production/scripts/verify-argocd-sync.sh
  - production/docs/gitops-cicd.md
  - production/README.md
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - PROD-03
user_setup:
  - service: git-remote
    why: "Argo Application 需要可拉取的 repoURL；私有库可能需凭据"
    env_vars: []
    dashboard_config:
      - task: "确认 git remote get-url origin 可用；若 private，按 gitops-cicd.md 配置 Argo repo 凭据或改用可访问 fork"
        location: "本机 git remote / Argo CD Settings → Repositories"

must_haves:
  truths:
    - "OrbStack K8s 上 Argo CD 已安装，且 Application 指向本仓库 production manifests/chart 路径并可观察到 Synced/Healthy（或文档化等价判据）（D-07）"
    - "production/scripts/verify-argocd-sync.sh exit 0（D-07/D-09）"
    - "GitHub Actions workflow 存在且执行编译与 scripts/qa_check.sh（或明确子集）；多架构 buildx 推送非硬门禁（D-08）"
    - "production/docs/gitops-cicd.md 含 Operator 安装、BG、Argo sync、回滚的可跟清单，无 Flux、无「请参考官网」（D-09）"
  artifacts:
    - path: "production/argocd/application-p03.yaml"
      provides: "Argo Application CR（D-07）"
      contains: "kind: Application"
    - path: "production/scripts/install-argocd.sh"
      provides: "Argo CD Helm 安装入口"
      contains: "argo-cd|argocd"
    - path: "production/scripts/verify-argocd-sync.sh"
      provides: "Sync/Health 失败非 0 门禁（D-07）"
      contains: "Synced|Healthy|sync"
    - path: "production/docs/gitops-cicd.md"
      provides: "单一 GitOps+CI 可复现 SOP（D-08/D-09）"
      contains: "Argo|GitHub Actions|回滚"
    - path: ".github/workflows/ci.yml"
      provides: "CI 编译 + qa_check（D-08）"
      contains: "qa_check"
  key_links:
    - from: "application-p03.yaml"
      to: "production/charts/p03-vehicle-alert"
      via: "source.path 指向 chart"
      pattern: "p03-vehicle-alert|production/"
    - from: "verify-argocd-sync.sh"
      to: "Application status"
      via: "kubectl/argocd 读取 sync/health"
      pattern: "Application|Synced"
    - from: "ci.yml"
      to: "scripts/qa_check.sh"
      via: "workflow step 调用"
      pattern: "qa_check"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 在 OrbStack arm64 上跑通压测矩阵、Operator Blue/Green、单一 Argo GitOps/CI，并交付完整规范/题库/三块 Grafana 看板，**so that** P5 生产化层可简历陈述且每项均可观察复现。

<objective>
V3 GitOps/CI 垂直切片（PROD-03）：Argo CD 同步 p03 BG manifests + GitHub Actions 门禁文档化可复现。

Purpose: D-14 第 (3) 波——单一 GitOps（仅 Argo）接上 06-02 已交付的 chart；CI 不替代 OrbStack 实测。
Output: install-argocd + Application + verify-argocd-sync 绿 + ci.yml + gitops-cicd.md；禁止 Flux；不以多架构推送为硬门禁。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-p5/06-02-SUMMARY.md
@.planning/phases/06-p5/06-CONTEXT.md
@.planning/phases/06-p5/06-RESEARCH.md
@.planning/phases/06-p5/06-PATTERNS.md
@production/charts/p03-vehicle-alert
@scripts/qa_check.sh
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 Argo CD + Application 指向 p03 chart + sync 门禁绿</name>
  <files>production/scripts/install-argocd.sh, production/argocd/application-p03.yaml, production/scripts/verify-argocd-sync.sh</files>
  <read_first>
    - .planning/phases/06-p5/06-CONTEXT.md（D-07/D-09；禁止 Flux）
    - .planning/phases/06-p5/06-RESEARCH.md（Argo Helm 安装、Application 示意、Pitfall 5 Git 源）
    - README.md（Argo chart SSOT 行）
    - production/charts/p03-vehicle-alert/Chart.yaml（path/name）
    - production/scripts/verify-argocd-sync.sh（06-00 失败态）
    - production/docs/operator-install.md（集群上下文约定）
  </read_first>
  <action>
    1) `install-argocd.sh`：Helm 安装 `argo/argo-cd` 到 `argocd` namespace（版本对齐 README SSOT）；等待 server 可用；文档化获取 initial admin 密码命令（从 K8s secret 读，禁止把密码写入仓库）。
    2) `application-p03.yaml`：`kind: Application`，`source.path: production/charts/p03-vehicle-alert`（或仓库内等价 path），`repoURL` 用 `git remote get-url origin` 可替换占位并在 SOP 写清填实步骤；destination in-cluster + namespace（如 `flink`）；允许首次 `kubectl apply -f` bootstrap，但后续变更须走 Git→sync 可观察（RESEARCH Pattern 4）。
    3) 执行 sync（CLI 或 UI 步骤脚本化）；升级 `verify-argocd-sync.sh`：无 CR / 非 Synced / 非 Healthy（按学习工程可接受的判据）则 FAIL；成功打印 `ok sync=… health=…`。
    4) 禁止任何 Flux 清单或双 GitOps 对照深讲（D-07）。
  </action>
  <verify>
    <automated>bash production/scripts/install-argocd.sh &amp;&amp; bash production/scripts/verify-argocd-sync.sh &amp;&amp; rg -n "kind: Application|p03-vehicle-alert" production/argocd/application-p03.yaml &amp;&amp; rg -n "Flux" production/argocd production/scripts/install-argocd.sh production/scripts/verify-argocd-sync.sh; test $? -ne 0 || test -z "$(rg -n Flux production/argocd production/scripts/install-argocd.sh production/scripts/verify-argocd-sync.sh || true)"</automated>
  </verify>
  <acceptance_criteria>
    - `bash production/scripts/verify-argocd-sync.sh` exit 0，输出含 sync/health 成功判据
    - `kubectl get application -n argocd`（或脚本实际 ns）可见 p03 相关 Application
    - `production/argocd/application-p03.yaml` 含 `kind: Application` 且 path 指向 `production/charts/p03-vehicle-alert`
    - `rg -n "Flux|flux" production/argocd production/scripts/install-argocd.sh production/scripts/verify-argocd-sync.sh` 无匹配
    - 仓库无明文 Argo admin 密码文件
  </acceptance_criteria>
  <done>Argo CD 同步 p03 chart 可验证</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions CI + gitops-cicd 可复现清单 + production README 收口</name>
  <files>.github/workflows/ci.yml, production/docs/gitops-cicd.md, production/README.md</files>
  <read_first>
    - .planning/phases/06-p5/06-CONTEXT.md（D-08/D-09）
    - .planning/phases/06-p5/06-PATTERNS.md（ci.yml 对齐 qa_check 语义）
    - scripts/qa_check.sh（门禁步骤）
    - production/docs/bluegreen-sop.md（回滚互链）
    - production/README.md（总入口）
    - projects/p01-log-ai-platform/docs/DEGRADE-CHECKLIST.md（清单体例）
  </read_first>
  <action>
    1) 新增 `.github/workflows/ci.yml`（文件名 Discretion）：触发 push/PR；步骤至少包含 JDK 21 下 Maven 编译（examples 与/或相关模块，范围写清）+ `bash scripts/qa_check.sh`（或文档声明的子集且子集须覆盖违禁词/断链核心）；不以 `buildx` 多架构推送为 required job（D-08）。
    2) `gitops-cicd.md`：端到端清单——Operator 安装、示例 FlinkBlueGreenDeployment、Blue/Green SOP 链接、Argo Application、sync、回滚；镜像/chart 发布步骤文档化（可本机 tag，不必真推远端 registry）；本机演示以 arm64/OrbStack 为准。
    3) 更新 `production/README.md` 链接到 gitops-cicd / install / BG / Argo verify；声明唯一 GitOps=Argo CD。
    4) 全文禁止 TODO/省略/请参考官网/Flux 深讲。
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml &amp;&amp; rg -n "qa_check" .github/workflows/ci.yml &amp;&amp; rg -n "Argo|回滚|GitHub Actions" production/docs/gitops-cicd.md | head -15 &amp;&amp; rg -n "Flux" production/docs/gitops-cicd.md production/README.md; test $? -ne 0 || test -z "$(rg -n 'Flux|请参考官网|TODO' production/docs/gitops-cicd.md production/README.md || true)"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` 存在且含调用 `scripts/qa_check.sh` 的步骤
    - `production/docs/gitops-cicd.md` 同时覆盖：Operator、Blue/Green、Argo sync、回滚、CI；无 Flux；无违禁词
    - `production/README.md` 含指向 `gitops-cicd.md` 与 `verify-argocd-sync.sh` 的链接
    - 多架构 buildx 若出现仅为可选 SOP 段落，且不标为 PROD-03 硬门禁
  </acceptance_criteria>
  <done>PROD-03：单一 Argo GitOps + CI 路径可按文档复现</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git remote → Argo CD | Desired state 拉取与 sync |
| GitHub Actions → 仓库检出 | CI 编译与 qa_check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | Argo 误 sync | mitigate | Application 限定 path/namespace；默认手动/显式 sync；verify 脚本门禁 |
| T-06-03-02 | Spoofing | 假 GitOps（仅 kubectl） | mitigate | 必须存在 Application CR + verify-argocd-sync 绿 |
| T-06-03-03 | Information Disclosure | Argo initial admin | mitigate | 密码仅从 K8s secret 读取；禁止提交 secret |
| T-06-03-04 | Elevation of Privilege | CI 凭证 | mitigate | GHA 不要求推集群；无强制 registry 账号 |
| T-06-03-SC | Tampering | argo-helm chart | mitigate | 使用 argoproj 官方 helm repo（SSOT）；禁 PyPI argocd |
</threat_model>

<verification>
PROD-03：verify-argocd-sync 绿；ci.yml 可解析；gitops-cicd 可跟且无 Flux。
</verification>

<success_criteria>
- 单一 Argo 路径可观察
- CI 门禁落仓
- production 清单完整
</success_criteria>

<output>
Create `.planning/phases/06-p5/06-03-SUMMARY.md` when done
</output>
