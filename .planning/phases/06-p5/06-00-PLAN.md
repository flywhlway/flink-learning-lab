---
phase: 06-p5
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - README.md
  - docs/README.md
  - docs/13-performance/README.md
  - docs/14-production/README.md
  - benchmark/Makefile
  - benchmark/scripts/run_matrix.sh
  - benchmark/scripts/collect_metrics.py
  - production/scripts/check_env.sh
  - production/scripts/run-bluegreen-drill.sh
  - production/scripts/verify-argocd-sync.sh
  - scripts/count_interview.py
  - monitoring/.gitkeep
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-04
user_setup:
  - service: homebrew-helm
    why: "RESEARCH 确认本机 Helm CLI 缺失；Operator/Argo 官方安装路径依赖 helm"
    env_vars: []
    dashboard_config:
      - task: "若 brew install helm 因权限失败，在宿主机手动安装 Helm 3.x/4.x 并确保 command -v helm 成功"
        location: "本机终端 / Homebrew"

must_haves:
  truths:
    - "根 README 版本矩阵已登记 Flink Operator Helm chart 1.15.0 与 Argo CD chart 坐标（D-04/D-07）；docs 模块 13/14 已登记非空链接（D-13）"
    - "Helm 可用且 OrbStack K8s nodes Ready（production/scripts/check_env.sh exit 0）"
    - "PROD-01/02/03/04 门禁脚本处于可运行失败态：matrix dry-run / BG drill / Argo verify / interview count 在完成态前非 0"
  artifacts:
    - path: "README.md"
      provides: "Operator Helm / Argo CD chart SSOT 行（D-04/D-07）"
      contains: "flink-kubernetes-operator"
    - path: "docs/README.md"
      provides: "模块 13/14 登记（D-13）"
      contains: "模块 13"
    - path: "benchmark/scripts/run_matrix.sh"
      provides: "矩阵 harness 失败态骨架（PROD-01 / D-01）"
      contains: "set -euo pipefail"
    - path: "production/scripts/check_env.sh"
      provides: "Helm + kubectl Ready 环境门禁（D-04）"
      contains: "helm"
    - path: "production/scripts/run-bluegreen-drill.sh"
      provides: "Blue/Green 演练失败态骨架（PROD-02 / D-06）"
      contains: "flinkbluegreendeployment"
    - path: "production/scripts/verify-argocd-sync.sh"
      provides: "Argo sync 失败态骨架（PROD-03 / D-07）"
      contains: "argocd"
    - path: "scripts/count_interview.py"
      provides: "interview 题量计数门禁（PROD-04 / D-11）"
      contains: "150"
  key_links:
    - from: "check_env.sh"
      to: "helm + kubectl get nodes"
      via: "command -v helm 与 Ready 节点门禁"
      pattern: "helm|Ready"
    - from: "run_matrix.sh"
      to: "benchmark/baseline.md"
      via: "未实现完整矩阵时 FAIL 非 0（--implemented 或等价门闩）"
      pattern: "baseline|--implemented|FAIL"
    - from: "count_interview.py"
      to: "interview/**"
      via: "题量 <150 时 exit 1"
      pattern: "150|interview"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 在 OrbStack arm64 上跑通压测矩阵、Operator Blue/Green、单一 Argo GitOps/CI，并交付完整规范/题库/三块 Grafana 看板，**so that** P5 生产化层可简历陈述且每项均可观察复现。

<objective>
Wave 0（Nyquist）：登记 SSOT/docs 编号，安装 Helm 门禁，并建立 PROD-01–04 失败态 harness，形成 RED→GREEN 反馈环。

Purpose: 按 D-14 波次与 RESEARCH Validation Architecture，后续垂直切片必须先有可自动化失败的脚本与版本登记；否则无法证明「未完成」与「完成」。
Output: README 矩阵新行 + docs 13/14 登记骨架 + check_env.sh 绿 + 四类门禁脚本失败态；不实现完整矩阵实测、不装 Operator、不扩满 150 题、不交三块可用 dashboard JSON。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-p5/06-CONTEXT.md
@.planning/phases/06-p5/06-RESEARCH.md
@.planning/phases/06-p5/06-PATTERNS.md
@README.md
@docs/README.md
@benchmark/README.md
@production/README.md
@scripts/qa_check.sh
@projects/p03-vehicle-monitoring/scripts/loadtest.sh
@projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh
@projects/p03-vehicle-monitoring/scripts/verify.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSOT 登记 + docs 13/14 编号骨架 + Helm/K8s 环境门禁</name>
  <files>README.md, docs/README.md, docs/13-performance/README.md, docs/14-production/README.md, production/scripts/check_env.sh</files>
  <read_first>
    - README.md（版本矩阵表结构；已有 Operator 1.15.0 引擎行；确认 Helm chart / Argo chart 是否已登记）
    - docs/README.md（模块 11/12/15 完成态句式；模块 13/14 当前空洞行）
    - docs/11-ecosystem/README.md（模块 README 体例）
    - .planning/phases/06-p5/06-CONTEXT.md（D-04、D-07、D-13、D-14）
    - .planning/phases/06-p5/06-RESEARCH.md（Helm install 坐标、Package Legitimacy、Environment Availability）
    - .planning/phases/06-p5/06-VALIDATION.md（Nyquist 门禁与 Wave 0 Requirements）
    - production/README.md（蓝图范围，勿删目标形态句）
  </read_first>
  <action>
    1) 在根 README 版本矩阵新增（或补全）行，至少覆盖：Flink Kubernetes Operator Helm chart `1.15.0`（repo `https://downloads.apache.org/flink/flink-kubernetes-operator-1.15.0/`）、Argo CD Helm chart（`argo/argo-cd` 稳定版，版本号以 `helm search repo` 可解析值为准并写死到矩阵）、可选注明 webhook 关闭时不强制 cert-manager（per D-04/D-07；禁止 PyPI helm/argocd）。
    2) 创建 `docs/13-performance/README.md` 与 `docs/14-production/README.md` 最小完成态骨架（背景一句 + 权威路径指向 `benchmark/` / `production/`），并在 `docs/README.md` 将模块 13/14 改为带链接的登记句（可标「进行中」；禁止编号空洞，per D-13）。
    3) 新建 `production/scripts/check_env.sh`：`set -euo pipefail`；若无 helm 则尝试 `brew install helm`（失败则 FAIL 提示手动安装）；`kubectl config use-context orbstack`（或文档化当前 context）；`kubectl get nodes` 必须含 Ready；成功打印 `ok helm=… k8s=Ready`。禁止引入 k6/JMeter/Flux。
  </action>
  <verify>
    <automated>bash production/scripts/check_env.sh &amp;&amp; rg -n "flink-kubernetes-operator|argo-cd|Argo CD" README.md | head -5 &amp;&amp; rg -n "模块 13|模块 14" docs/README.md | head -5 &amp;&amp; test -f docs/13-performance/README.md &amp;&amp; test -f docs/14-production/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash production/scripts/check_env.sh` exit 0，stdout 含 `ok` 与 `Ready`
    - `command -v helm` 成功
    - `rg -n "1\\.15\\.0" README.md` 命中 Operator 相关矩阵行；Argo chart 坐标出现在 README 版本矩阵
    - `docs/README.md` 模块 13/14 行含相对链接指向 `docs/13-performance/` 与 `docs/14-production/`（或等价完成态路径）
    - 无违禁词：`rg -n 'TODO|FIXME|请参考官网|自行实现' docs/13-performance/README.md docs/14-production/README.md production/scripts/check_env.sh` 无匹配（排除注释中的禁令声明）
  </acceptance_criteria>
  <done>SSOT + docs 13/14 登记完成；Helm/K8s 环境门禁脚本 exit 0</done>
</task>

<task type="auto">
  <name>Task 2: PROD-01–04 失败态 harness（matrix / BG / Argo / interview 计数）</name>
  <files>benchmark/Makefile, benchmark/scripts/run_matrix.sh, benchmark/scripts/collect_metrics.py, production/scripts/run-bluegreen-drill.sh, production/scripts/verify-argocd-sync.sh, scripts/count_interview.py, monitoring/.gitkeep</files>
  <read_first>
    - projects/p03-vehicle-monitoring/scripts/loadtest.sh（warmup→measure→scrape→baseline 四段）
    - projects/p03-vehicle-monitoring/scripts/drill_watermark_stall.sh（两阶段演练 + FAIL 非 0）
    - projects/p03-vehicle-monitoring/scripts/verify.sh（白名单 + 失败非 0）
    - scripts/qa_check.sh（案例计数段）
    - .planning/phases/06-p5/06-PATTERNS.md（各脚本 Pattern Assignments）
    - .planning/phases/06-p5/06-RESEARCH.md（Validation Architecture Wave 0 Gaps、D-01 最小必跑集）
    - .planning/phases/06-p5/06-VALIDATION.md（Per-Task Verification Map / Wave 0 Requirements）
    - interview/README.md（当前约 30 题骨架）
  </read_first>
  <action>
    按 PATTERNS 纪律创建失败态（默认非 0，禁止假绿）：
    1) `benchmark/scripts/run_matrix.sh` + `collect_metrics.py` + `benchmark/Makefile`：支持 `dry-run`/`EPS` 环境变量；默认或未设 `--implemented` 时打印 FAIL 并 exit 1；声明将写入 `benchmark/baseline.md`（文件可尚未生成）。禁止 k6（D-02）。
    2) `production/scripts/run-bluegreen-drill.sh`：检查 CRD `flinkbluegreendeployments.flink.apache.org`；不存在则 FAIL（RED）；存在时骨架可采集 kubectl 状态但本 Wave 不要求演练成功。
    3) `production/scripts/verify-argocd-sync.sh`：无 Application / 未 Synced 时 FAIL。
    4) `scripts/count_interview.py`：扫描 `interview/**/*.md` 题号（如行首 `^\d+\.`）；计数 &lt;150 则 exit 1 并打印当前数量；≥150 才 exit 0。
    5) 确保 `monitoring/` 目录存在（可用 `.gitkeep`）；本 Wave 不提交 3 个完整 dashboard JSON。
    所有脚本 `bash -n` 通过；`set -euo pipefail`。
  </action>
  <verify>
    <automated>bash -n benchmark/scripts/run_matrix.sh production/scripts/run-bluegreen-drill.sh production/scripts/verify-argocd-sync.sh &amp;&amp; (bash benchmark/scripts/run_matrix.sh; test $? -ne 0) &amp;&amp; (bash production/scripts/run-bluegreen-drill.sh; test $? -ne 0) &amp;&amp; (bash production/scripts/verify-argocd-sync.sh; test $? -ne 0) &amp;&amp; (python3 scripts/count_interview.py; test $? -ne 0)</automated>
  </verify>
  <acceptance_criteria>
    - 四个门禁在当前仓库状态下均非 0：`run_matrix.sh`、`run-bluegreen-drill.sh`、`verify-argocd-sync.sh`、`count_interview.py`
    - `bash -n` 对三个 bash 脚本均通过
    - `benchmark/Makefile` 含 `matrix` 或 `dry-run` 目标且调用 `run_matrix.sh`
    - `rg -n "k6|JMeter|Flux" benchmark/scripts production/scripts` 无匹配（D-02/D-07）
    - `python3 scripts/count_interview.py` stderr/stdout 打印当前题量且 &lt;150
  </acceptance_criteria>
  <done>四类 RED harness 可运行且默认失败；Makefile/计数器就位</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 宿主机 CLI → OrbStack K8s API | check_env / 后续 helm install 越过本机到集群控制面 |
| 仓库文档 → 执行者 | SSOT 坐标若被篡改会导致装错 chart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00-01 | Spoofing | Helm/Argo 安装源 | mitigate | 仅 Homebrew helm + Apache/argo-helm 官方 repo；禁止 PyPI 伪 CLI（Package Legitimacy） |
| T-06-00-02 | Tampering | README 版本矩阵 | mitigate | 先登记再使用；check_env 不拉取未登记镜像 |
| T-06-00-03 | Information Disclosure | check_env 输出 | accept | 仅打印工具版本与 Ready，无 secret |
| T-06-00-SC | Tampering | brew/helm 安装 | mitigate | 使用官方 formula/chart；无 [SUS] 包；失败显式非 0 |
</threat_model>

<verification>
Wave 0 完成当且仅当：check_env 绿；四门禁脚本失败态；SSOT 与 docs 13/14 登记可检索。
</verification>

<success_criteria>
- D-04/D-07/D-13 登记可 grep
- Helm + K8s Ready
- PROD-01–04 RED 反馈环建立
</success_criteria>

<output>
Create `.planning/phases/06-p5/06-00-SUMMARY.md` when done
</output>
