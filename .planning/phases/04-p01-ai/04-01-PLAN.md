---
phase: 04-p01-ai
plan: 01
type: execute
wave: 1
depends_on:
  - "04-00"
files_modified:
  - projects/p01-log-ai-platform/pom.xml
  - projects/p01-log-ai-platform/Makefile
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/LogAiJob.java
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/JobConfig.java
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/model/LogEvent.java
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/model/LogResult.java
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/ParseLogJson.java
  - projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/ParseLogJsonTest.java
  - projects/p01-log-ai-platform/sql/clickhouse_log_results.sql
  - docker/docker-compose.yml
  - docker/Makefile
  - scripts/smoke_p01_profile.sh
autonomous: true
requirements:
  - LOG-01
user_setup: []

must_haves:
  truths:
    - "维护者可 make up-p01 后看到 logs.events 与 flinklab.log_results 已初始化，且 default make up 服务集合不被 --profile p01 污染（D-08）"
    - "mvn clean package 产出可 shade 的 p01 jar；LogAiJob 可 Kafka→ParseLogJson 透传（尚无规则/AI 时至少可 submit 不崩）"
    - "JobConfig 默认 --ai.enabled=false；手写 --key 解析（D-04）"
    - "ParseLogJsonTest GREEN：合法事件保留、脏数据丢弃"
  artifacts:
    - path: "projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/LogAiJob.java"
      provides: "作业主类 Kafka→Parse 透传骨架（D-07）"
      exports: ["main"]
    - path: "projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/JobConfig.java"
      provides: "手写参数；ai.enabled 默认 false（D-04）"
      contains: "ai.enabled"
    - path: "projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/ParseLogJson.java"
      provides: "结构化 JSON 解析（D-09）"
      contains: "traceId"
    - path: "projects/p01-log-ai-platform/Makefile"
      provides: "package/submit/test 入口"
      contains: "package"
  key_links:
    - from: "LogAiJob"
      to: "logs.events"
      via: "KafkaSource + ParseLogJson.uid(p01-*)"
      pattern: "logs.events|p01-parse"
    - from: "JobConfig"
      to: "ai.enabled"
      via: "default false"
      pattern: "ai\\.enabled|false"
    - from: "smoke_p01_profile.sh"
      to: "compose --profile p01"
      via: "config --services 有无 profile 差集"
      pattern: "p01-init|profile p01"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 用独立 p01 compose profile 一键拉起可降级的结构化日志 AI 平台（默认无 AI 规则路径 + 可选 Async Ollama 旁路）并完成压测/演练/文档包，**so that** p01 达到与 p03 同等的单项目完成态且主构建零硬依赖外部模型。

<objective>
V1 骨架垂直切片（LOG-01 启动面）：独立 pom + 模型/JobConfig + Parse 透传作业 + p01-init 可起 + Makefile package/submit。

Purpose: 先让维护者能一键起 profile、打包提交作业，证明工程纪律复制自 p03；规则落库留给 V2。
Output: LogAiJob（Parse 透传）+ JobConfig/LogEvent/LogResult + ParseLogJson GREEN + Makefile + smoke_p01_profile；不实现 FeatureEnricher/RuleTagger/CH Sink 完整 verify 绿。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-p01-ai/04-CONTEXT.md
@.planning/phases/04-p01-ai/04-RESEARCH.md
@.planning/phases/04-p01-ai/04-PATTERNS.md
@.planning/phases/04-p01-ai/04-00-SUMMARY.md
@projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/VehicleAlertJob.java
@projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/JobConfig.java
@projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/ParseVehicleJson.java
@projects/p03-vehicle-monitoring/Makefile
@docker/Makefile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 模型 + JobConfig + ParseLogJson GREEN</name>
  <files>projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/model/LogEvent.java, projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/model/LogResult.java, projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/JobConfig.java, projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/ParseLogJson.java, projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/ParseLogJsonTest.java, projects/p01-log-ai-platform/pom.xml</files>
  <read_first>
    - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/JobConfig.java（arg 解析；默认 kafka/clickhouse）
    - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/ParseVehicleJson.java（脏数据丢弃；拒引号反斜杠）
    - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/model/VehicleEvent.java（POJO 约定）
    - .planning/phases/04-p01-ai/04-PATTERNS.md（JobConfig 扩展字段表；ParseLogJson 映射）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-04、D-07、D-09）
  </read_first>
  <behavior>
    - LogEvent：public 字段 service/level/message/traceId/eventTime + 无参构造（Flink POJO）
    - LogResult：在 LogEvent 上扩展 featureJson/ruleLabel/aiRisk/aiSource（默认 aiSource=DISABLED、ruleLabel=NONE、aiRisk=NONE）
    - JobConfig.from(args)：默认 ai.enabled=false；ai.endpoint=http://host.docker.internal:11434；events-topic=logs.events；kafka/clickhouse 默认对齐 p03 compose 网络（per D-04 / RESEARCH Pattern 1）
    - ParseLogJson：必填五字段；level 白名单 ERROR|WARN|INFO|DEBUG；service/traceId 拒引号与反斜杠；失败丢弃不抛（per D-09 / T-04-01）
    - ParseLogJsonTest 全部 GREEN
  </behavior>
  <action>
    实现模型、JobConfig、ParseLogJson，使 Wave 0 ParseLogJsonTest 变绿。完善 pom shade mainClass=com.flywhl.flinklab.p01.LogAiJob 与 kafka/jackson 依赖（仍禁止 Agents/Milvus）。禁止默认 ai.enabled=true。禁止把 Redis 绑进本切片。
  </action>
  <verify>
    <automated>cd projects/p01-log-ai-platform &amp;&amp; mvn -q -Dtest=ParseLogJsonTest test</automated>
  </verify>
  <acceptance_criteria>
    - JobConfig.java 含 ai.enabled 且默认解析结果为 false（源码可见 default "false"）
    - ParseLogJson.java 含 service/level/message/traceId 校验
    - LogEvent/LogResult 存在于 model 包
    - mvn -Dtest=ParseLogJsonTest 退出码 0
    - pom 仍不含 flink-agents / milvus
  </acceptance_criteria>
  <done>LOG-01：事件契约与默认 AI off 配置可编译可测（per D-04/D-09）。</done>
</task>

<task type="auto">
  <name>Task 2: LogAiJob 透传 + Makefile package/submit</name>
  <files>projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/LogAiJob.java, projects/p01-log-ai-platform/Makefile, docker/Makefile</files>
  <read_first>
    - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/VehicleAlertJob.java（KafkaSource、WM ooo=5s idleness=30s、uid 前缀、checkpoint）
    - projects/p03-vehicle-monitoring/Makefile（package/submit jar 过滤 original-）
    - docker/Makefile（submit-p03 模式）
    - .planning/phases/04-p01-ai/04-PATTERNS.md（LogAiJob 接线；uid 前缀 p01-）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-07）
  </read_first>
  <behavior>
    - LogAiJob：Kafka logs.events → ParseLogJson → assignTimestampsAndWatermarks(ooo=5s,idleness=30s)；所有算子 .uid("p01-...")；本切片可不接 Sink（或 print/丢弃侧），但必须能 flink run 启动（per LOG-01 启动面）
    - 若暂无 Sink：显式注释标明 V2 将接 CH；禁止空作业无 source
    - Makefile：package（mvn clean package）、submit（复制 jar 排除 original-、flink run -c LogAiJob、默认 --ai.enabled=false）、test
    - docker/Makefile：submit-p01 对齐 submit-p03（固定主类与 jar 名模式）
  </behavior>
  <action>
    实现 LogAiJob 最小可提交图（Parse 透传）。Makefile / docker submit-p01 入口对齐 p03。submit 默认参数必须带 --ai.enabled=false（或依赖 JobConfig 默认）。禁止同步阻塞 HTTP。禁止挂 Agents 依赖。
  </action>
  <verify>
    <automated>cd projects/p01-log-ai-platform &amp;&amp; mvn -q clean package -DskipTests &amp;&amp; test -n "$(ls target/p01-log-ai-platform-*.jar 2>/dev/null | grep -v original | head -1)" &amp;&amp; grep -q LogAiJob Makefile &amp;&amp; grep -q submit-p01 docker/Makefile &amp;&amp; grep -q 'ai.enabled' projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/JobConfig.java</automated>
  </verify>
  <acceptance_criteria>
    - LogAiJob.java 含 main、KafkaSource 或 logs.events、ParseLogJson、uid 含 p01-
    - mvn clean package -DskipTests 成功且 target 有非 original 的 p01-log-ai-platform-*.jar
    - 项目 Makefile 含 package 与 submit；docker/Makefile 含 submit-p01
  </acceptance_criteria>
  <done>LOG-01：可打包可提交的透传作业入口就位（per D-07）。</done>
</task>

<task type="auto">
  <name>Task 3: p01-init 冒烟 + profile 隔离断言</name>
  <files>docker/docker-compose.yml, docker/Makefile, projects/p01-log-ai-platform/sql/clickhouse_log_results.sql, scripts/smoke_p01_profile.sh</files>
  <read_first>
    - docker/docker-compose.yml（p01-init Wave0 钩子；对照 p03-init 完整性）
    - docker/Makefile（up-p01）
    - .planning/phases/04-p01-ai/04-PATTERNS.md（File Classification：`scripts/smoke_p01_profile.sh` role-match 行）
    - .planning/phases/01-p03/01-01-SUMMARY.md 或 scripts 中 p03 smoke 模式（若有 smoke_profile）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-08、D-09）
  </read_first>
  <behavior>
    - 确认 p01-init 幂等创建 logs.events；单 POST log_results DDL；depends_on kafka/clickhouse healthy（对齐 p03）
    - smoke_p01_profile.sh：docker compose config --services 在无 profile 时不含 p01-init；有 --profile p01 时含 p01-init；失败非 0（per D-08 / VEH-01 纪律复制）
    - OrbStack：尽量 make up-p01 后检查 topic/表（若基座已 up）；若环境未起则 smoke 仅 config 级仍须绿
  </behavior>
  <action>
    补全 p01-init 与 Wave0 差异（健康依赖、topic partitions=4 建议值）。新建 scripts/smoke_p01_profile.sh 做配置级隔离断言。禁止改 default up 目标。禁止把 milvus 写入 p01 profile。
  </action>
  <verify>
    <automated>bash -n scripts/smoke_p01_profile.sh &amp;&amp; bash scripts/smoke_p01_profile.sh &amp;&amp; cd docker &amp;&amp; docker compose config -q &amp;&amp; docker compose --profile p01 config --services | grep -q p01-init &amp;&amp; (! docker compose config --services | grep -q p01-init)</automated>
  </verify>
  <acceptance_criteria>
    - smoke_p01_profile.sh 存在、bash -n 通过、执行 exit 0
    - compose --profile p01 服务列表含 p01-init；无 profile 列表不含 p01-init
    - docker compose config -q 成功
  </acceptance_criteria>
  <done>LOG-01：独立 profile 一键钩子可配置验证且不污染 default（per D-08）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka → ParseLogJson | 不可信 JSON 日志进入作业 |
| JobConfig CLI → 作业 | 运维参数进入运行时 |
| compose profile → 基座 | p01-init 写 topic/DDL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | ParseLogJson / 未来 CH Sink | mitigate | 脏 JSON 丢弃；service/traceId 拒引号反斜杠；level 白名单 |
| T-04-05 | Tampering | pom / shade | mitigate | 主构建零 Agents/Milvus；仅 SSOT 坐标 |
| T-04-06 | Information Disclosure | JobConfig 默认口令 | accept | 复用 compose lab 凭据；不写新密钥进仓库 |
| T-04-SC | Tampering | Maven deps | mitigate | 无新 [ASSUMED]/[SUS] 包；不跑未登记 install |
</threat_model>

<verification>
- ParseLogJsonTest GREEN；mvn package 产出 jar
- smoke_p01_profile.sh 绿；compose profile 隔离成立
- JobConfig 默认 ai.enabled=false
</verification>

<success_criteria>
- 维护者具备 up-p01 + package + submit 启动面（LOG-01 部分）
- 规则落库 / verify 绿留给 04-02
</success_criteria>

<output>
Create `.planning/phases/04-p01-ai/04-01-SUMMARY.md` when done
</output>
