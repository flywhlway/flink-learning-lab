---
phase: 04-p01-ai
plan: 04
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/guardrail/GuardrailFunction.java
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/cost/BudgetGateFunction.java
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/LogAiJob.java
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/JobConfig.java
  - projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/cost/BudgetGateTest.java
  - projects/p01-log-ai-platform/README.md
autonomous: true
requirements:
  - LOG-04
user_setup: []

must_haves:
  truths:
    - "输出侧关键词/级别护栏可拦截并落库 ai_source=BLOCKED 或等价，且 guardrail_blocks Counter 可观察（D-11/D-12）"
    - "窗口/调用预算熔断后降级到规则路径，budget_trips（及 ai_calls/ai_degrades 之一组）可观察（D-11/D-12）"
    - "BudgetGateTest GREEN；README 验证节含 Prometheus 或 TM 指标核对命令（D-11）"
    - "未引入 Loki/OTel/AI Gateway（D-11）"
  artifacts:
    - path: "projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/guardrail/GuardrailFunction.java"
      provides: "输出护栏（D-12）"
      contains: "guardrail|BLOCK"
    - path: "projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/cost/BudgetGateFunction.java"
      provides: "预算熔断（D-12）"
      contains: "budget"
    - path: "projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/cost/BudgetGateTest.java"
      provides: "预算纯逻辑 GREEN"
      contains: "BudgetGate"
  key_links:
    - from: "LogAiJob"
      to: "BudgetGateFunction"
      via: "RuleTagger 之后、AsyncDataStream 之前（超限短路 AI，不进 Ollama）"
      pattern: "BudgetGateFunction"
    - from: "LogAiJob"
      to: "GuardrailFunction"
      via: "Async/规则合流之后、CH Sink 之前（输出侧 BLOCK）"
      pattern: "GuardrailFunction"
    - from: "MetricGroup p01"
      to: "Prometheus :9249 scrape"
      via: "counter ai_calls/ai_timeouts/ai_degrades/guardrail_blocks/budget_trips"
      pattern: "addGroup\\(\"p01\"\\)|guardrail_blocks|budget_trips"
    - from: "README 验证节"
      to: "curl Prom / TM metrics"
      via: "可复制观察命令"
      pattern: "9249|9090|budget_trips|guardrail"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 用独立 p01 compose profile 一键拉起可降级的结构化日志 AI 平台（默认无 AI 规则路径 + 可选 Async Ollama 旁路）并完成压测/演练/文档包，**so that** p01 达到与 p03 同等的单项目完成态且主构建零硬依赖外部模型。

<objective>
V4 护栏+预算+指标垂直切片（LOG-04）：Guardrail、BudgetGate、Flink Counter、README 观察命令。

Purpose: 证明流式 AI 演示含成本/护栏生产纪律，指标可在 Prometheus/日志核对。
Output: GuardrailFunction、BudgetGateFunction（Test GREEN）、作业接线、README 验证命令；不强制独立 Grafana JSON；不引入 Loki/OTel。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-p01-ai/04-CONTEXT.md
@.planning/phases/04-p01-ai/04-RESEARCH.md
@.planning/phases/04-p01-ai/04-PATTERNS.md
@.planning/phases/04-p01-ai/04-03-SUMMARY.md
@examples/e12-17-streaming-guardrail/
@examples/e12-18-streaming-cost-control/
@examples/e12-15-observability/
@monitoring/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BudgetGate GREEN + 熔断降级接线</name>
  <files>projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/cost/BudgetGateFunction.java, projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/cost/BudgetGateTest.java, projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/JobConfig.java, projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/LogAiJob.java</files>
  <read_first>
    - examples/e12-18-streaming-cost-control/（预算熔断样板）
    - examples/e12-15-observability/（MetricGroup counter 注册）
    - .planning/phases/04-p01-ai/04-RESEARCH.md（Pattern 4 指标命名；budget.max-ai-calls）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-11、D-12）
  </read_first>
  <behavior>
    - BudgetGateFunction（或拆出纯函数供测）：滚动窗口/全局计数达到 budget.max-ai-calls 后**短路** AI——不进入 AsyncDataStream，标记 ai_source=DEGRADED（或等价），规则标签保留，并 inc budget_trips（per D-12）
    - 指标注册分工：`BudgetGateFunction.open()` 注册 budget_trips（及可选 ai_degrades）；`OllamaRiskAsyncFunction.open()` 注册 ai_calls、ai_timeouts、ai_degrades（实际调用侧）（per D-11 / RESEARCH Pattern 4）
    - BudgetGateTest GREEN：未超限 allow；达上限 trip
    - LogAiJob 接线顺序强制：Parse→Enrich→Rule→**BudgetGate**→（allow 时）Async Ollama→合流→Guardrail→CH；BudgetGate 符号在源码中须出现在 unorderedWaitWithRetry/AsyncDataStream 之前
  </behavior>
  <action>
    实现预算熔断与指标注册，使 Wave0 BudgetGateTest 变绿。BudgetGate 必须接在 Async **之前**以短路外呼；禁止接在 AI 之后仅做事后标记。禁止引入 AI Gateway/Loki/OTel。PromQL 全名首次 submit 后从 :9249 回填 README，勿编造。
  </action>
  <verify>
    <automated>cd projects/p01-log-ai-platform &amp;&amp; mvn -q -Dtest=BudgetGateTest,RuleTaggerTest,ParseLogJsonTest test &amp;&amp; grep -qE 'budget_trips|budgetTrips|budget' src/main/java/com/flywhl/flinklab/p01/cost/BudgetGateFunction.java &amp;&amp; grep -qE 'addGroup|counter' src/main/java/com/flywhl/flinklab/p01/cost/BudgetGateFunction.java &amp;&amp; grep -qE 'addGroup|counter' src/main/java/com/flywhl/flinklab/p01/ai/OllamaRiskAsyncFunction.java &amp;&amp; python3 -c "s=open('src/main/java/com/flywhl/flinklab/p01/LogAiJob.java').read(); b=s.find('BudgetGate'); a=s.find('AsyncDataStream'); assert b&gt;=0 and a&gt;=0 and b&lt;a, (b,a)" &amp;&amp; mvn -q -DskipTests package</automated>
  </verify>
  <acceptance_criteria>
    - BudgetGateFunction 存在且含预算/熔断逻辑
    - BudgetGateTest 退出码 0
    - BudgetGateFunction 含 budget_trips（或等价）counter；OllamaRiskAsyncFunction 含 ai_calls 或 ai_timeouts/ai_degrades counter
    - LogAiJob.java 中 BudgetGate 标识符出现在 AsyncDataStream 之前
    - package 成功
  </acceptance_criteria>
  <done>LOG-04：预算熔断可测可接线（per D-12）。</done>
</task>

<task type="auto">
  <name>Task 2: GuardrailFunction + 输出侧 BLOCK</name>
  <files>projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/guardrail/GuardrailFunction.java, projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/LogAiJob.java, projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/JobConfig.java</files>
  <read_first>
    - examples/e12-17-streaming-guardrail/（Broadcast/关键词护栏）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-12：优先类比 e12-17）
    - .planning/phases/04-p01-ai/04-RESEARCH.md（Security：prompt 注入 → BLOCKED）
  </read_first>
  <behavior>
    - GuardrailFunction：输出侧检查 message/ai 相关文本是否命中 JobConfig --guardrail.keywords（或 Broadcast 控制流，优先 e12-17 Broadcast；若 Broadcast 则 topic 可选 logs.guardrail.control 且 p01-init 幂等创建——仅当实现 Broadcast 时）（per D-12）
    - 命中：标记 ai_source=BLOCKED（或 rule 侧 BLOCK）、inc guardrail_blocks；仍可落库但内容截断/脱敏（per T-04-02）
    - LogAiJob：Guardrail 在 Sink 之前
    - 默认关键词列表来自 JobConfig（RESEARCH 默认串可调）
  </behavior>
  <action>
    实现护栏并接线。Discretion：若 Broadcast 使本任务上下文过重，允许静态 JobConfig 关键词列表（D-12「均可」），但须在 SUMMARY 注明选型并保持 e12-17 概念对齐。禁止把模型输出当可信代码执行。
  </action>
  <verify>
    <automated>cd projects/p01-log-ai-platform &amp;&amp; mvn -q -DskipTests package &amp;&amp; grep -q 'BLOCK\|guardrail' src/main/java/com/flywhl/flinklab/p01/guardrail/GuardrailFunction.java &amp;&amp; grep -q 'GuardrailFunction' src/main/java/com/flywhl/flinklab/p01/LogAiJob.java</automated>
  </verify>
  <acceptance_criteria>
    - GuardrailFunction 存在且含关键词或黑名单逻辑与 BLOCK 语义
    - LogAiJob 引用 GuardrailFunction
    - package 成功
  </acceptance_criteria>
  <done>LOG-04：输出护栏可拦截可落库（per D-12）。</done>
</task>

<task type="auto">
  <name>Task 3: README 验证节 — 指标观察命令</name>
  <files>projects/p01-log-ai-platform/README.md</files>
  <read_first>
    - monitoring/README.md（Flink Prometheus scrape / :9249）
    - .planning/phases/04-p01-ai/04-RESEARCH.md（Metrics 观察命令示例）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-11）
    - projects/p01-log-ai-platform/README.md
  </read_first>
  <behavior>
    - README「验证」节增加：如何触发护栏/预算（造数或参数）、如何用 curl localhost:9249/metrics 或 Prometheus :9090/api/v1/query 观察 counter（per D-11）
    - 声明不强制独立 Grafana dashboard；可复用现有 Flink 健康面板 + CH 查询（Discretion）
    - 禁止引入未登记可观测栈
  </behavior>
  <action>
    更新 README 验证节。首次在 OrbStack submit 后，把实际可见的指标名片段回填进 README（勿虚构）。保持八段式完整。
  </action>
  <verify>
    <automated>grep -E '9249|9090|budget_trips|guardrail|ai_calls' projects/p01-log-ai-platform/README.md &amp;&amp; (! grep -E 'Loki|OpenTelemetry|AI Gateway' projects/p01-log-ai-platform/README.md) &amp;&amp; (! grep -E 'TODO|请参考官网|自行实现' projects/p01-log-ai-platform/README.md)</automated>
  </verify>
  <acceptance_criteria>
    - README 含 9249 或 9090，以及 budget_trips 或 guardrail 或 ai_calls 之一
    - 不含 Loki / OpenTelemetry / AI Gateway 作为本项目依赖
    - 无违禁词
  </acceptance_criteria>
  <done>LOG-04：成本/护栏指标观察命令可跟（per D-11）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 模型/规则输出 → Guardrail | 不可信文本进入输出侧检查 |
| BudgetGate → AI 调用开关 | 计数状态决定是否外呼 |
| Flink metrics → Prometheus | 业务 Counter 被刮取 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | Guardrail 漏拦 | mitigate | 输出侧关键词黑名单 + BLOCKED 落库；截断敏感原文 |
| T-04-03 | Denial of Service | 预算绕过打爆 Ollama | mitigate | budget.max-ai-calls + Async capacity + 默认 AI off；trip 后降级规则 |
| T-04-01 | Tampering | 护栏关键词配置 | mitigate | JobConfig/Broadcast 受控输入；不执行动态脚本 |
| T-04-05 | Tampering | 可观测栈膨胀 | mitigate | 禁止 Loki/OTel/Gateway；仅 Flink reporter + 现有 Prom |
| T-04-SC | Tampering | 无新包 | mitigate | 无 npm/pip/cargo 新依赖 |
</threat_model>

<verification>
- BudgetGateTest + 既有单测绿；package 成功
- Guardrail/Budget 接线存在；README 含观察命令
</verification>

<success_criteria>
- LOG-04：护栏+预算+Counter 可演示可观察
- 压测/演练/文档包留给 04-05
</success_criteria>

<output>
Create `.planning/phases/04-p01-ai/04-04-SUMMARY.md` when done
</output>
