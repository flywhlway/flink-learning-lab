---
phase: 04-p01-ai
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - projects/p01-log-ai-platform/pom.xml
  - projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/ParseLogJsonTest.java
  - projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/rule/RuleTaggerTest.java
  - projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/cost/BudgetGateTest.java
  - projects/p01-log-ai-platform/scripts/verify.sh
  - projects/p01-log-ai-platform/scripts/verify_ai.sh
  - projects/p01-log-ai-platform/scripts/drill_ai_degrade.sh
  - projects/p01-log-ai-platform/scripts/loadtest.sh
  - projects/p01-log-ai-platform/sql/clickhouse_log_results.sql
  - docker/docker-compose.yml
  - docker/Makefile
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05
user_setup: []

must_haves:
  truths:
    - "ParseLogJsonTest / RuleTaggerTest / BudgetGateTest 可被 surefire 发现且当前 RED（生产类尚未交付）— 建立 Nyquist 反馈环"
    - "verify.sh / verify_ai.sh / drill_ai_degrade.sh / loadtest.sh 可 bash -n，且当前默认非 0（失败态骨架）"
    - "clickhouse_log_results.sql 与 docker p01-init / make up-p01 钩子落仓；default make up 不加 --profile p01（D-08）"
  artifacts:
    - path: "projects/p01-log-ai-platform/pom.xml"
      provides: "独立模块 surefire 脚手架（D-07）"
      contains: "p01-log-ai-platform"
    - path: "projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/ParseLogJsonTest.java"
      provides: "解析契约 RED 夹具（LOG-01/02）"
      contains: "ParseLogJson"
    - path: "projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/rule/RuleTaggerTest.java"
      provides: "规则标签 RED 夹具（LOG-02）"
      contains: "AUTH_FAIL"
    - path: "projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/cost/BudgetGateTest.java"
      provides: "预算熔断纯函数 RED 夹具（LOG-04）"
      contains: "BudgetGate"
    - path: "projects/p01-log-ai-platform/scripts/verify.sh"
      provides: "CH 权威出口失败态骨架（D-10）"
      contains: "log_results"
    - path: "projects/p01-log-ai-platform/sql/clickhouse_log_results.sql"
      provides: "权威表 DDL（D-10）"
      contains: "flinklab.log_results"
  key_links:
    - from: "ParseLogJsonTest"
      to: "ParseLogJson"
      via: "JUnit 断言脏数据丢弃与必填字段"
      pattern: "ParseLogJson"
    - from: "verify.sh"
      to: "flinklab.log_results"
      via: "白名单 rule_label 拼 SQL；Kafka 仅 diag"
      pattern: "rule_label|log_results"
    - from: "docker Makefile up-p01"
      to: "p01-init profile"
      via: "docker compose --profile p01 up -d p01-init"
      pattern: "up-p01|profile p01"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 用独立 p01 compose profile 一键拉起可降级的结构化日志 AI 平台（默认无 AI 规则路径 + 可选 Async Ollama 旁路）并完成压测/演练/文档包，**so that** p01 达到与 p03 同等的单项目完成态且主构建零硬依赖外部模型。

<objective>
Wave 0（Nyquist）：落地 LOG-01–05 失败态单测/脚本夹具与 DDL/compose 钩子，建立 RED→GREEN 反馈环。

Purpose: 04-VALIDATION.md 要求先有 Parse/Rule/Budget 单测、verify/verify-ai/drill/loadtest 骨架、log_results DDL 与 p01-init 钩子；否则后续垂直切片无法自动化门禁。
Output: 最小独立 pom + RED 三测 + 四脚本骨架（非 0）+ SQL DDL + docker p01-init / up-p01 钩子；不实现 LogAiJob / FeatureEnricher / Ollama / Guardrail 完整逻辑 / baseline 实测。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-p01-ai/04-CONTEXT.md
@.planning/phases/04-p01-ai/04-RESEARCH.md
@.planning/phases/04-p01-ai/04-PATTERNS.md
@.planning/phases/04-p01-ai/04-VALIDATION.md
@projects/p03-vehicle-monitoring/pom.xml
@projects/p03-vehicle-monitoring/scripts/verify.sh
@projects/p03-vehicle-monitoring/scripts/loadtest.sh
@docker/docker-compose.yml
@docker/Makefile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED 夹具 — 最小 pom + Parse/Rule/Budget 单测</name>
  <files>projects/p01-log-ai-platform/pom.xml, projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/ParseLogJsonTest.java, projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/rule/RuleTaggerTest.java, projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/cost/BudgetGateTest.java</files>
  <read_first>
    - projects/p03-vehicle-monitoring/pom.xml（独立模块 + surefire + jackson/junit 版本；勿挂 examples 父工程）
    - projects/p03-vehicle-monitoring/src/test/java/com/flywhl/flinklab/p03/cep/PatternActivationGateTest.java（轻量 JUnit 无 MiniCluster）
    - .planning/phases/04-p01-ai/04-RESEARCH.md（Wave 0 Gaps、Validation Architecture、log_results 契约）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-07、D-09、D-12）
    - .planning/phases/04-p01-ai/04-VALIDATION.md（Wave 0 Requirements）
  </read_first>
  <behavior>
    - pom：artifactId=p01-log-ai-platform；flink 2.2.1 / kafka connector 5.0.0-2.2 / jackson 2.17.2 / junit 5.10.2；禁止 Agents/Milvus/flink-cep 坐标（per D-02/D-03/D-07）
    - ParseLogJsonTest：引用尚未存在的 ParseLogJson；断言合法 JSON 产出 service/level/message/traceId/eventTime；缺字段或含引号/反斜杠的 service 被丢弃（per D-09 / T-04-01）
    - RuleTaggerTest：引用尚未存在的 RuleTagger；断言 AUTH 类失败日志 → rule_label=AUTH_FAIL；无关 INFO → NONE 或等价（per LOG-02）
    - BudgetGateTest：引用尚未存在的 BudgetGate（或 BudgetGateFunction 包内纯函数）；断言未超限 allow；达到 maxAiCalls 触发 trip/熔断（per D-12 / LOG-04）
    - 本任务不实现生产类（留给后续切片）；单测必须 RED
  </behavior>
  <action>
    新建 projects/p01-log-ai-platform 最小 pom（复制 p03 属性区，去掉 flink-cep；shade mainClass 可先写 com.flywhl.flinklab.p01.LogAiJob）。新建三份 JUnit 测试，引用尚未存在的符号，故意让 mvn -Dtest=ParseLogJsonTest,RuleTaggerTest,BudgetGateTest 失败（RED）。禁止把测试写成永远通过的空壳。禁止引入未登记 Maven 坐标（Package Legitimacy：仅 SSOT Approved）。
  </action>
  <verify>
    <automated>test -f projects/p01-log-ai-platform/pom.xml &amp;&amp; cd projects/p01-log-ai-platform &amp;&amp; mvn -q -Dtest=ParseLogJsonTest,RuleTaggerTest,BudgetGateTest test; ec=$?; test $ec -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - pom.xml 含 artifactId p01-log-ai-platform、flink.version 2.2.1、junit；grep 不含 flink-agents 与 milvus
    - 三测试文件存在且分别含 ParseLogJson、AUTH_FAIL、BudgetGate（或 budget）断言关键字
    - mvn -Dtest=ParseLogJsonTest,RuleTaggerTest,BudgetGateTest 退出码 ≠ 0（RED）
  </acceptance_criteria>
  <done>LOG-* Nyquist RED：解析/规则/预算单测夹具就位（per D-07）。</done>
</task>

<task type="auto">
  <name>Task 2: RED 骨架 — verify / verify-ai / drill / loadtest 四脚本</name>
  <files>projects/p01-log-ai-platform/scripts/verify.sh, projects/p01-log-ai-platform/scripts/verify_ai.sh, projects/p01-log-ai-platform/scripts/drill_ai_degrade.sh, projects/p01-log-ai-platform/scripts/loadtest.sh</files>
  <read_first>
    - projects/p03-vehicle-monitoring/scripts/verify.sh（set -euo pipefail、CH 权威、白名单拼 SQL、Kafka 仅 diag）
    - projects/p03-vehicle-monitoring/scripts/loadtest.sh（--implemented 门闩失败态模式）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-06、D-10、D-13、D-14）
    - .planning/phases/04-p01-ai/04-RESEARCH.md（Pattern 3 验收双轨；Wave 0 Gaps）
  </read_first>
  <behavior>
    - 四脚本均 set -euo pipefail；可 bash -n
    - verify.sh：声明 CH flinklab.log_results + 白名单 RULE_LABEL（默认 AUTH_FAIL）；当前无作业/空表时非 0；禁止 Kafka 单独 exit 0（per D-06/D-10）
    - verify_ai.sh：声明需 Ollama + ai_source=AI；当前骨架非 0（per D-06）
    - drill_ai_degrade.sh：声明 AI off/不可用时 verify 仍绿的演练职责；骨架默认 exit 1（per D-14）
    - loadtest.sh：声明将写 docs/baseline.md；骨架默认 exit 1（per D-13）；禁止 k6/JMeter
  </behavior>
  <action>
    在 projects/p01-log-ai-platform/scripts/ 新建四脚本。骨架须可执行且默认非 0（例如缺少 --implemented 或直接 FAIL: Wave0 stub）。文件头注释写明最终职责与权威出口（CH，非 Kafka）。verify.sh 须含 rule_label 与 log_results 字样及白名单校验意图。禁止实现完整 E2E 绿路径与实测 baseline 数字。
  </action>
  <verify>
    <automated>bash -n projects/p01-log-ai-platform/scripts/verify.sh &amp;&amp; bash -n projects/p01-log-ai-platform/scripts/verify_ai.sh &amp;&amp; bash -n projects/p01-log-ai-platform/scripts/drill_ai_degrade.sh &amp;&amp; bash -n projects/p01-log-ai-platform/scripts/loadtest.sh &amp;&amp; (! bash projects/p01-log-ai-platform/scripts/verify.sh) &amp;&amp; (! bash projects/p01-log-ai-platform/scripts/verify_ai.sh) &amp;&amp; (! bash projects/p01-log-ai-platform/scripts/drill_ai_degrade.sh) &amp;&amp; (! bash projects/p01-log-ai-platform/scripts/loadtest.sh)</automated>
  </verify>
  <acceptance_criteria>
    - 四脚本存在且含 set -euo pipefail
    - verify.sh 含 log_results 与 rule_label
    - verify_ai.sh 含 ai_source 或 ai_risk 或 Ollama
    - loadtest.sh 含 baseline；drill_ai_degrade.sh 含 degrade 或 AI
    - bash -n 均通过；直接执行四脚本退出码均 ≠ 0
  </acceptance_criteria>
  <done>LOG-01–05 Nyquist RED：验收/演练/压测入口骨架就位（per D-06/D-14）。</done>
</task>

<task type="auto">
  <name>Task 3: 骨架 — log_results DDL + p01-init + make up-p01</name>
  <files>projects/p01-log-ai-platform/sql/clickhouse_log_results.sql, docker/docker-compose.yml, docker/Makefile</files>
  <read_first>
    - projects/p03-vehicle-monitoring/sql/clickhouse_alerts.sql（单语句 DDL 风格）
    - docker/docker-compose.yml（p03-init：profiles、KAFKA_IMAGE、wget --post-file 分 POST）
    - docker/Makefile（up-p03 显式 up p03-init；default up 不加 profile）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-08、D-09、D-10）
    - .planning/phases/04-p01-ai/04-RESEARCH.md（Recommended Project Structure；log_results DDL；topics logs.events）
  </read_first>
  <behavior>
    - clickhouse_log_results.sql：单语句 CREATE TABLE IF NOT EXISTS flinklab.log_results，列含 service/level/message/trace_id/event_time/feature_json/rule_label/ai_risk/ai_source/ingest_time（per RESEARCH DDL / D-10）
    - p01-init：profiles: [p01]；image ${KAFKA_IMAGE}；幂等创建 logs.events（partitions≥1）；wget --post-file 挂载 DDL（单语句）；不绑 milvus（per D-08/D-03）
    - docker/Makefile：增加 up-p01 → docker compose --profile p01 up -d p01-init；.PHONY 更新；禁止改 default up 目标加 --profile p01（per D-08）
  </behavior>
  <action>
    写入 sql/clickhouse_log_results.sql；在 docker-compose.yml 追加 p01-init（对照 p03-init，改名与路径到 p01）。Makefile 增加 up-p01。禁止把 DDL 多语句塞进一次 POST。禁止把 milvus 或 --profile ai 绑进 p01-init。compose config 须仍可通过。
  </action>
  <verify>
    <automated>test -f projects/p01-log-ai-platform/sql/clickhouse_log_results.sql &amp;&amp; grep -q flinklab.log_results projects/p01-log-ai-platform/sql/clickhouse_log_results.sql &amp;&amp; grep -q p01-init docker/docker-compose.yml &amp;&amp; grep -q 'profiles:.*"p01"\|profiles: \[\"p01\"\]\|- p01' docker/docker-compose.yml &amp;&amp; grep -q up-p01 docker/Makefile &amp;&amp; grep -q 'profile p01' docker/Makefile &amp;&amp; (! grep -E '^up:.*profile p01' docker/Makefile) &amp;&amp; cd docker &amp;&amp; docker compose config -q</automated>
  </verify>
  <acceptance_criteria>
    - SQL 含 CREATE TABLE 与 log_results 及 rule_label/ai_risk/ai_source
    - docker-compose.yml 含 p01-init、logs.events、clickhouse_log_results.sql
    - Makefile 含 up-p01 且 default up 目标行不含 --profile p01
    - docker compose config -q 成功
  </acceptance_criteria>
  <done>LOG-01 骨架：DDL + p01 profile 钩子就位且不污染 default up（per D-08）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 造数/脚本 → ClickHouse HTTP | verify 拼 SQL；init POST DDL |
| 仓库 → Maven Central | pom 依赖解析 |
| compose profile → 基座网络 | p01-init 仅本地 lab 网络 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | verify.sh RULE_LABEL | mitigate | 白名单枚举后再拼 SQL；拒绝任意环境变量注入（抄 p03 PATTERN_ID） |
| T-04-05 | Tampering | p01 pom 依赖 | mitigate | 禁止 Agents/Milvus SDK 进主 pom（D-02/D-03）；仅 SSOT 已登记坐标 |
| T-04-06 | Information Disclosure | compose/CH 凭据 | accept | 复用 lab 演示口令 flinklab；不新增云 API key；文档注明非生产 |
| T-04-SC | Tampering | Maven/PyPI installs | mitigate | 本 Wave 无新未审计包；jackson/flink 以 SSOT Approved 为准；无 [SLOP]/[SUS] |
</threat_model>

<verification>
- 三单测 surefire ≠ 0；四脚本直接执行 ≠ 0
- bash -n 四脚本通过；compose config -q 通过
- DDL + p01-init + up-p01 存在；default up 无 p01 profile
</verification>

<success_criteria>
- Wave 0 Nyquist 夹具全部可运行且为失败态
- p01 目录树与 compose 钩子落仓且不污染 default make up
- 不实现作业主类、完整 verify 绿、AI/护栏/baseline（留给 04-01+）
</success_criteria>

<output>
Create `.planning/phases/04-p01-ai/04-00-SUMMARY.md` when done
</output>
