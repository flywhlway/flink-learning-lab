---
phase: 04-p01-ai
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/LogAiJob.java
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/enrich/FeatureEnricher.java
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/rule/RuleTagger.java
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/sink/ClickHouseLogSink.java
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/model/LogResult.java
  - projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/rule/RuleTaggerTest.java
  - projects/p01-log-ai-platform/scripts/gen_log_events.py
  - projects/p01-log-ai-platform/scripts/verify.sh
  - projects/p01-log-ai-platform/Makefile
  - projects/p01-log-ai-platform/README.md
autonomous: true
requirements:
  - LOG-01
  - LOG-02
user_setup: []

must_haves:
  truths:
    - "默认 --ai.enabled=false 时，造数 rule-auth-fail 后 ClickHouse log_results 可断言 rule_label=AUTH_FAIL（D-04/D-06/D-10）"
    - "无 Ollama/Milvus 时 FeatureEnricher+RuleTagger 路径完整可演示；ai_source=DISABLED（D-04/LOG-02）"
    - "verify.sh 以 CH count 为唯一 exit 0；Kafka 仅诊断（D-10）"
    - "RuleTaggerTest GREEN"
  artifacts:
    - path: "projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/enrich/FeatureEnricher.java"
      provides: "Keyed State 规则特征（无 Redis）（LOG-02）"
      contains: "KeyedProcessFunction|RichMapFunction|ValueState"
    - path: "projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/rule/RuleTagger.java"
      provides: "规则标签 AUTH_FAIL 等（LOG-02）"
      contains: "AUTH_FAIL"
    - path: "projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/sink/ClickHouseLogSink.java"
      provides: "HTTP SinkV2 写 log_results（D-10）"
      contains: "log_results"
    - path: "projects/p01-log-ai-platform/scripts/gen_log_events.py"
      provides: "可判定 scenario 造数（D-09）"
      contains: "rule-auth-fail"
    - path: "projects/p01-log-ai-platform/scripts/verify.sh"
      provides: "CH 权威 verify GREEN 路径"
      contains: "AUTH_FAIL"
  key_links:
    - from: "LogAiJob"
      to: "ClickHouseLogSink"
      via: "Parse→FeatureEnricher→RuleTagger→Sink；ai off 旁路"
      pattern: "FeatureEnricher|RuleTagger|ClickHouseLogSink"
    - from: "verify.sh"
      to: "flinklab.log_results"
      via: "白名单 RULE_LABEL HTTP 查询"
      pattern: "rule_label|AUTH_FAIL"
    - from: "gen_log_events.py"
      to: "logs.events"
      via: "confluent-kafka produce localhost:9094"
      pattern: "logs.events|rule-auth-fail"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 用独立 p01 compose profile 一键拉起可降级的结构化日志 AI 平台（默认无 AI 规则路径 + 可选 Async Ollama 旁路）并完成压测/演练/文档包，**so that** p01 达到与 p03 同等的单项目完成态且主构建零硬依赖外部模型。

<objective>
V2 规则路径垂直切片（LOG-01+LOG-02）：富化/特征 + 规则标签 + CH Sink + 造数 + verify 绿（零 AI 依赖）。

Purpose: 证明「没有模型也能讲完故事」——默认 AI off 端到端可复现且可断言。
Output: FeatureEnricher、RuleTagger、ClickHouseLogSink、gen_log_events.py、verify.sh GREEN、README 启动/验证骨架；不实现 Async Ollama / verify-ai / 护栏预算。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-p01-ai/04-CONTEXT.md
@.planning/phases/04-p01-ai/04-RESEARCH.md
@.planning/phases/04-p01-ai/04-PATTERNS.md
@.planning/phases/04-p01-ai/04-01-SUMMARY.md
@projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/sink/ClickHouseAlertSink.java
@projects/p03-vehicle-monitoring/scripts/verify.sh
@projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py
@examples/e12-06-streaming-feature/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FeatureEnricher + RuleTagger + CH Sink 接线</name>
  <files>projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/enrich/FeatureEnricher.java, projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/rule/RuleTagger.java, projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/sink/ClickHouseLogSink.java, projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/LogAiJob.java, projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/model/LogResult.java, projects/p01-log-ai-platform/src/test/java/com/flywhl/flinklab/p01/rule/RuleTaggerTest.java</files>
  <read_first>
    - projects/p03-vehicle-monitoring/src/main/java/com/flywhl/flinklab/p03/sink/ClickHouseAlertSink.java（SinkV2 HTTP 批写、validate 拒引号反斜杠、非 2xx 抛 IOException）
    - .planning/phases/04-p01-ai/04-RESEARCH.md（Architecture：规则路径；log_results 列；Keyed State 不绑 Redis）
    - .planning/phases/04-p01-ai/04-PATTERNS.md（FeatureEnricher / RuleTagger / Sink 赋值）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-04、D-10）
    - examples/e12-06-streaming-feature 中 SessionCategoryFeature 或等价 Keyed 特征样板
  </read_first>
  <behavior>
    - FeatureEnricher：按 service（或 service+level）Keyed State 维护简易特征（如近期 ERROR 计数）写入 featureJson；不依赖 Redis/Ollama（per LOG-02 / RESEARCH A1）
    - RuleTagger：可单测纯逻辑——auth 失败文案/level=ERROR+service 含 auth → AUTH_FAIL；可选 ERROR_BURST 基于特征阈值；否则 NONE（per D-09 scenarios）
    - ClickHouseLogSink：写入 flinklab.log_results；字符串字段拒引号/反斜杠；ai.enabled=false 时 ai_source=DISABLED、ai_risk=NONE（per D-04/D-10 / T-04-01）
    - LogAiJob：Parse→Enrich→Rule→Sink；本切片不接 Async AI；算子 uid 前缀 p01-
    - RuleTaggerTest GREEN
  </behavior>
  <action>
    实现三者并改 LogAiJob 接线。Sink 复制 p03 HTTP 模式，表名 log_results，列对齐 DDL。禁止引入 Redis 客户端。禁止默认开启 AI。禁止 Kafka sink 作为 verify 权威。
  </action>
  <verify>
    <automated>cd projects/p01-log-ai-platform &amp;&amp; mvn -q -Dtest=RuleTaggerTest,ParseLogJsonTest test &amp;&amp; mvn -q -DskipTests package</automated>
  </verify>
  <acceptance_criteria>
    - FeatureEnricher/RuleTagger/ClickHouseLogSink 源文件存在
    - RuleTagger 含 AUTH_FAIL；Sink 含 log_results 且含拒引号或反斜杠校验
    - LogAiJob 引用 FeatureEnricher 与 RuleTagger 与 ClickHouseLogSink
    - RuleTaggerTest 与 ParseLogJsonTest 退出码 0；package 成功
  </acceptance_criteria>
  <done>LOG-02：无 AI 富化/规则/CH 落库路径可编译（per D-04/D-10）。</done>
</task>

<task type="auto">
  <name>Task 2: gen scenarios + verify.sh GREEN（OrbStack e2e）</name>
  <files>projects/p01-log-ai-platform/scripts/gen_log_events.py, projects/p01-log-ai-platform/scripts/verify.sh, projects/p01-log-ai-platform/Makefile</files>
  <read_first>
    - projects/p03-vehicle-monitoring/scripts/gen_vehicle_events.py（uv script、scenario、bootstrap localhost:9094、尾心跳）
    - projects/p03-vehicle-monitoring/scripts/verify.sh（CH HTTP、白名单、MIN_COUNT、Kafka diag）
    - .planning/phases/04-p01-ai/04-RESEARCH.md（造数 scenario 表；verify 伪代码）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-06、D-09、D-10）
  </read_first>
  <behavior>
    - gen_log_events.py：uv script + confluent-kafka；字段 service/level/message/traceId/eventTime；scenario 至少 rule-auth-fail（可判定 AUTH_FAIL）；支持后续 --rate/--duration 钩子（可先 stub 参数解析）（per D-09/D-13）
    - verify.sh：白名单 RULE_LABEL（AUTH_FAIL 等）后查 COUNT FROM flinklab.log_results；≥ MIN_COUNT 才 exit 0；非法 label exit 1；Kafka 仅 diag（per D-06/D-10 / T-04-01）
    - Makefile：gen / verify / match 或等价一键；文档注释 package→submit→gen→verify
  </behavior>
  <action>
    实现造数与 verify，去掉 Wave0 失败门闩。在 OrbStack 上跑通：up-p01（基座已 up）→ package → submit（ai off）→ gen rule-auth-fail → verify exit 0。负例：TRUNCATE 后 verify ≠ 0。禁止以 Kafka 消费成功单独放行。禁止编造未跑通的「已验证」。
  </action>
  <verify>
    <automated>bash -n projects/p01-log-ai-platform/scripts/verify.sh &amp;&amp; python3 -m py_compile projects/p01-log-ai-platform/scripts/gen_log_events.py &amp;&amp; cd projects/p01-log-ai-platform &amp;&amp; grep -q AUTH_FAIL scripts/verify.sh &amp;&amp; grep -q rule-auth-fail scripts/gen_log_events.py &amp;&amp; grep -q verify Makefile &amp;&amp; (command -v docker >/dev/null &amp;&amp; docker info >/dev/null 2>&amp;1 &amp;&amp; make verify || { echo "ORBSTACK_E2E_REQUIRED: run make verify on OrbStack and attach CH count evidence in SUMMARY"; false; })</automated>
  </verify>
  <acceptance_criteria>
    - gen_log_events.py 含 rule-auth-fail 与 traceId/eventTime
    - verify.sh 含白名单校验逻辑与 log_results；不含「仅 Kafka 有消息则 exit 0」
    - Makefile 含 gen 与 verify 目标
    - 执行期 SUMMARY 须记录 OrbStack：submit+gen+verify=0 与 TRUNCATE 负例 ≠0（若 CI 无 Docker 则本机执行证据写入 SUMMARY）
  </acceptance_criteria>
  <done>LOG-01+02：端到端规则路径 CH 权威验收绿（per D-06/D-10）。</done>
</task>

<task type="auto">
  <name>Task 3: README 八段式骨架（启动+验证规则路径）</name>
  <files>projects/p01-log-ai-platform/README.md</files>
  <read_first>
    - projects/p03-vehicle-monitoring/README.md（八段式结构）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-04、D-05 清单将在 V3 补全；本任务先规则路径）
    - CLAUDE.md（八段式：背景→架构→代码→启动→验证→踩坑→最佳实践→面试题+参考）
  </read_first>
  <behavior>
    - README 八段式齐全；启动节含 make up-p01 / package / submit / gen / verify
    - 验证节写明默认 AI off、CH 权威、不依赖 Ollama（per D-04/D-06）
    - 明确目录名 p01-log-ai-platform；交叉引用后续 verify-ai / 降级清单将在 V3
    - 禁止 TODO/省略/请参考官网
  </behavior>
  <action>
    撰写 README 八段式，覆盖规则路径可跟命令。降级勾选表可留「V3 将补全」以外的完整规则路径说明——但禁止用「TODO/省略/略」字样；可用「见 docs/DEGRADE-CHECKLIST.md（随 04-03 交付）」并确保该句不是违禁词。架构节简述 Parse→Enrich→Rule→CH。
  </action>
  <verify>
    <automated>test -s projects/p01-log-ai-platform/README.md &amp;&amp; grep -q 'up-p01\|verify' projects/p01-log-ai-platform/README.md &amp;&amp; grep -q 'ai.enabled' projects/p01-log-ai-platform/README.md &amp;&amp; (! grep -E 'TODO|请参考官网|自行实现' projects/p01-log-ai-platform/README.md)</automated>
  </verify>
  <acceptance_criteria>
    - README 非空且含启动与验证相关命令（up-p01、verify）
    - 含 ai.enabled 或「默认关闭 AI」表述
    - 无 TODO / 请参考官网 / 自行实现
  </acceptance_criteria>
  <done>LOG-01/02：规则路径文档可跟（per 八段式）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gen → Kafka | 合成日志 JSON |
| 作业 → ClickHouse HTTP | Sink 批写权威表 |
| verify → ClickHouse | 白名单 SQL 断言 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | ClickHouseLogSink / verify.sh | mitigate | Sink 拒引号反斜杠；verify RULE_LABEL 白名单；CH 为唯一放行 |
| T-04-02 | Information Disclosure | message 落库 | accept | 学习工程合成数据；MVP 可截断超长 message；不接真实生产日志 |
| T-04-05 | Tampering | 主 pom | mitigate | 仍禁止 Agents/Milvus |
| T-04-SC | Tampering | confluent-kafka（uv） | mitigate | RESEARCH Package Legitimacy：PyPI Approved；不引入 k6/JMeter |
</threat_model>

<verification>
- RuleTaggerTest + ParseLogJsonTest 绿；package 成功
- verify.sh / gen 契约就位；OrbStack e2e 证据进 SUMMARY
- README 八段式无违禁词
</verification>

<success_criteria>
- 无 AI 时端到端结构化日志→规则→CH 可复现（LOG-01+02）
- AI 旁路留给 04-03
</success_criteria>

<output>
Create `.planning/phases/04-p01-ai/04-02-SUMMARY.md` when done
</output>
