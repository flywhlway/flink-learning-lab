---
phase: 04-p01-ai
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/ai/OllamaRiskAsyncFunction.java
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/LogAiJob.java
  - projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/JobConfig.java
  - projects/p01-log-ai-platform/scripts/verify_ai.sh
  - projects/p01-log-ai-platform/scripts/gen_log_events.py
  - projects/p01-log-ai-platform/Makefile
  - projects/p01-log-ai-platform/docs/DEGRADE-CHECKLIST.md
  - projects/p01-log-ai-platform/README.md
autonomous: true
requirements:
  - LOG-03
user_setup:
  - service: ollama
    why: "verify-ai 需要宿主机 Ollama 与已 pull 模型（D-06）；默认 verify 不依赖"
    env_vars: []
    dashboard_config:
      - task: "确认 ollama serve 运行且 ollama list 有可用模型；必要时 ollama pull 或 submit 传 --ai.model"
        location: "宿主机终端；模型名以本机 list 为准（非固定 qwen3:8b）"

must_haves:
  truths:
    - "仅当 --ai.enabled=true 时作业调用宿主机 Ollama /api/chat 做风险分级；默认 false 零调用（D-01/D-04）"
    - "Async timeout/解析失败走 DEGRADED 仍落库，作业不因无模型而失败（D-01）"
    - "verify-ai 在 Ollama 可用时断言 CH ai_source=AI 且 ai_risk 枚举可观察；默认 verify 仍不依赖 Ollama（D-06）"
    - "显式降级核对清单覆盖：无 Ollama / 无 Milvus / AI 关闭 / AI 超时降级（D-05）"
  artifacts:
    - path: "projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/ai/OllamaRiskAsyncFunction.java"
      provides: "Async I/O → Ollama 风险分级（D-01）"
      contains: "/api/chat"
    - path: "projects/p01-log-ai-platform/scripts/verify_ai.sh"
      provides: "可选 AI 验收轨（D-06）"
      contains: "ai_source"
    - path: "projects/p01-log-ai-platform/docs/DEGRADE-CHECKLIST.md"
      provides: "显式降级勾选表（D-05）"
      contains: "Ollama"
  key_links:
    - from: "LogAiJob"
      to: "OllamaRiskAsyncFunction"
      via: "ai.enabled 分支 AsyncDataStream.unorderedWaitWithRetry"
      pattern: "unorderedWaitWithRetry|OllamaRiskAsyncFunction"
    - from: "OllamaRiskAsyncFunction"
      to: "host.docker.internal:11434"
      via: "POST /api/chat stream=false think=false"
      pattern: "api/chat|host.docker.internal"
    - from: "verify_ai.sh"
      to: "flinklab.log_results"
      via: "ai_source=AI AND ai_risk IN (...)"
      pattern: "ai_source|ai_risk"
---

## Phase Goal

**As a** 仓库维护者，**I want to** 用独立 p01 compose profile 一键拉起可降级的结构化日志 AI 平台（默认无 AI 规则路径 + 可选 Async Ollama 旁路）并完成压测/演练/文档包，**so that** p01 达到与 p03 同等的单项目完成态且主构建零硬依赖外部模型。

<objective>
V3 AI 旁路垂直切片（LOG-03）：Async Ollama 风险分级 + verify-ai + 降级核对清单。

Purpose: 交付「至少一条可观察 AI 路径」且可显式降级；主门禁仍以 verify 为准。
Output: OllamaRiskAsyncFunction、作业开关接线、verify_ai.sh、DEGRADE-CHECKLIST.md、gen ai-risk-high scenario、README 更新；不实现 Agents/Milvus/verify-rag；不实现护栏/预算（V4）。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-p01-ai/04-CONTEXT.md
@.planning/phases/04-p01-ai/04-RESEARCH.md
@.planning/phases/04-p01-ai/04-PATTERNS.md
@.planning/phases/04-p01-ai/04-02-SUMMARY.md
@examples/e11-async-io/
@examples/e12-03-streaming-inference/
@ai/chapters/03-streaming-inference.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: OllamaRiskAsyncFunction + ai.enabled 作业接线</name>
  <files>projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/ai/OllamaRiskAsyncFunction.java, projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/LogAiJob.java, projects/p01-log-ai-platform/src/main/java/com/flywhl/flinklab/p01/JobConfig.java</files>
  <read_first>
    - examples/e11-async-io 中 C2TimeoutRetryJob / FlakyEnrich（unorderedWaitWithRetry、timeout 完成降级）
    - .planning/phases/04-p01-ai/04-RESEARCH.md（Pattern 2 Async Ollama 请求体；Pitfall 伪异步/think）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-01、D-02、D-04）
    - .planning/phases/04-p01-ai/04-PATTERNS.md（AI 旁路接线）
  </read_first>
  <behavior>
    - OllamaRiskAsyncFunction：RichAsyncFunction；JDK HttpClient.sendAsync 或线程池包装 HTTP；POST {endpoint}/api/chat；body 强制 stream=false、think=false、format=json；解析 risk∈HIGH|MEDIUM|LOW → ai_source=AI；解析失败 → ai_risk=UNKNOWN、ai_source=DEGRADED；timeout() 完成 DEGRADED 记录而非抛导致作业失败（per D-01 / RESEARCH）
    - LogAiJob：仅 cfg.aiEnabled==true 时接 AsyncDataStream.unorderedWaitWithRetry（capacity/timeout/retry 来自 JobConfig）；false 时透传 ai_source=DISABLED（per D-04）
    - 禁止 Agents 坐标；禁止 SQL ML_PREDICT 作为验收主路径（per D-01/D-02）
    - 禁止在 asyncInvoke 内 Future.get() 阻塞 mailbox
  </behavior>
  <action>
    实现 Async 函数并按开关接线。默认模型名可写 qwen3:8b，但 README/清单必须说明本机可用 --ai.model 覆盖。保持 ai.enabled 默认 false。不改动默认 verify 路径语义。
  </action>
  <verify>
    <automated>cd projects/p01-log-ai-platform &amp;&amp; mvn -q -DskipTests package &amp;&amp; grep -q 'api/chat' src/main/java/com/flywhl/flinklab/p01/ai/OllamaRiskAsyncFunction.java &amp;&amp; grep -q 'unorderedWaitWithRetry\|AsyncDataStream' src/main/java/com/flywhl/flinklab/p01/LogAiJob.java &amp;&amp; grep -q 'ai.enabled' src/main/java/com/flywhl/flinklab/p01/JobConfig.java &amp;&amp; (! grep -riE 'flink-agents|milvus' pom.xml)</automated>
  </verify>
  <acceptance_criteria>
    - OllamaRiskAsyncFunction 含 /api/chat 与 timeout 或 DEGRADED 处理
    - LogAiJob 含 AsyncDataStream 或 unorderedWaitWithRetry，且存在 aiEnabled/ai.enabled 分支
    - package 成功；pom 无 agents/milvus
  </acceptance_criteria>
  <done>LOG-03：Async Ollama 旁路可打包，默认仍 AI off（per D-01/D-04）。</done>
</task>

<task type="auto">
  <name>Task 2: verify-ai + ai-risk-high 造数 + Makefile</name>
  <files>projects/p01-log-ai-platform/scripts/verify_ai.sh, projects/p01-log-ai-platform/scripts/gen_log_events.py, projects/p01-log-ai-platform/Makefile</files>
  <read_first>
    - projects/p01-log-ai-platform/scripts/verify.sh（CH 权威纪律）
    - .planning/phases/04-p01-ai/04-RESEARCH.md（Pattern 3 verify-ai；scenario ai-risk-high）
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-06）
  </read_first>
  <behavior>
    - verify_ai.sh：前置 curl Ollama /api/tags（或文档约定 endpoint）；失败则明确 exit 1 并提示非默认门禁；成功则查 CH ai_source='AI' AND ai_risk IN ('HIGH','MEDIUM','LOW') ≥ MIN_COUNT；Kafka 不单独放行（per D-06 / T-04-01）
    - gen 增加 scenario ai-risk-high（明确高危文案便于模型分级）
    - Makefile：verify-ai 目标；submit-ai 或 submit 传 --ai.enabled=true 的文档化入口
  </behavior>
  <action>
    去掉 verify_ai Wave0 门闩，实现双轨脚本。本机有 Ollama 时跑通 verify-ai 并写入 SUMMARY；若无模型，SUMMARY 记录前置失败信息且清单仍完整——不得把默认 verify 改为依赖 Ollama。
  </action>
  <verify>
    <automated>bash -n projects/p01-log-ai-platform/scripts/verify_ai.sh &amp;&amp; grep -q 'ai_source' projects/p01-log-ai-platform/scripts/verify_ai.sh &amp;&amp; grep -q 'ai-risk-high' projects/p01-log-ai-platform/scripts/gen_log_events.py &amp;&amp; grep -q 'verify-ai' projects/p01-log-ai-platform/Makefile &amp;&amp; if curl -sf http://127.0.0.1:11434/api/tags >/dev/null 2>&amp;1 || curl -sf http://host.docker.internal:11434/api/tags >/dev/null 2>&amp;1; then cd projects/p01-log-ai-platform &amp;&amp; make verify-ai; else echo "Ollama unreachable — contract checks only (D-06); SUMMARY must note LOG-03 deferred or model override"; fi</automated>
  </verify>
  <acceptance_criteria>
    - verify_ai.sh 含 ai_source 与 ai_risk 白名单意图；bash -n 通过
    - gen 含 ai-risk-high
    - Makefile 含 verify-ai
  </acceptance_criteria>
  <done>LOG-03：可选 AI 验收轨脚本就位（per D-06）。</done>
</task>

<task type="auto">
  <name>Task 3: DEGRADE-CHECKLIST + README 降级节</name>
  <files>projects/p01-log-ai-platform/docs/DEGRADE-CHECKLIST.md, projects/p01-log-ai-platform/README.md</files>
  <read_first>
    - .planning/phases/04-p01-ai/04-CONTEXT.md（D-05：四格勾选）
    - .planning/research/PITFALLS.md（AI 路径无降级）
    - ai/chapters/03-streaming-inference.md（降级论述）
    - projects/p01-log-ai-platform/README.md（现有八段式）
  </read_first>
  <behavior>
    - DEGRADE-CHECKLIST.md：勾选表至少四行——无 Ollama / 无 Milvus / AI 关闭 / AI 超时降级到规则；每行含可执行命令与预期可观察输出（CH 或日志）（per D-05）
    - 写明 REQ LOG-03 原文枚举（ML_PREDICT/Agents/Milvus）→ 本项目实现选型为 Async Ollama（per RESEARCH A5）
    - README 验证节链接清单；标明主门禁 verify vs 可选 verify-ai 前置
    - 禁止把 Milvus 绑进 p01 profile；可选「启用 AI+向量」仅文档指向 --profile ai（per D-03）
  </behavior>
  <action>
    新建清单并更新 README。Agents 仅可在「不交付/附录」说明为非硬依赖（D-02）。禁止违禁词。
  </action>
  <verify>
    <automated>test -s projects/p01-log-ai-platform/docs/DEGRADE-CHECKLIST.md &amp;&amp; grep -q Ollama projects/p01-log-ai-platform/docs/DEGRADE-CHECKLIST.md &amp;&amp; grep -q Milvus projects/p01-log-ai-platform/docs/DEGRADE-CHECKLIST.md &amp;&amp; grep -q 'ai.enabled\|AI 关闭' projects/p01-log-ai-platform/docs/DEGRADE-CHECKLIST.md &amp;&amp; grep -q DEGRADE-CHECKLIST projects/p01-log-ai-platform/README.md &amp;&amp; (! grep -E 'TODO|请参考官网|自行实现' projects/p01-log-ai-platform/docs/DEGRADE-CHECKLIST.md)</automated>
  </verify>
  <acceptance_criteria>
    - DEGRADE-CHECKLIST.md 含 Ollama、Milvus、AI 关闭/enabled、超时或 DEGRADED
    - README 链接该清单
    - 无 TODO / 请参考官网 / 自行实现
  </acceptance_criteria>
  <done>LOG-03：显式降级核对清单可勾选（per D-05）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flink TM → 宿主机 Ollama HTTP | 日志片段进入 prompt |
| 模型输出 → LogResult.ai_risk | 不可信 LLM 文本解析为枚举 |
| verify-ai → Ollama tags + CH | 可选验收轨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | prompt / 模型输出 | mitigate | system 提示仅要求 JSON risk 枚举；解析失败→UNKNOWN/DEGRADED；不把模型输出当代码执行；MVP 截断 message |
| T-04-03 | Denial of Service | Async capacity → Ollama | mitigate | 默认 ai off；capacity≤16；timeout 降级；预算熔断留给 V4 |
| T-04-05 | Tampering | 供应链 Preview Agents | mitigate | Agents 不进主 pom（D-02） |
| T-04-01 | Tampering | verify_ai SQL | mitigate | ai_risk/ai_source 白名单枚举拼 SQL |
| T-04-SC | Tampering | 无新 Maven 包 | mitigate | 仅 JDK HttpClient + 已有 jackson |
</threat_model>

<verification>
- package 成功；Async 接线与 /api/chat 存在
- verify_ai.sh / DEGRADE-CHECKLIST 契约满足
- 默认 verify 仍不依赖 Ollama（回归 04-02）
</verification>

<success_criteria>
- LOG-03：可观察 AI 旁路 + 显式降级清单；主门禁双轨分离
- 护栏/预算/指标留给 04-04
</success_criteria>

<output>
Create `.planning/phases/04-p01-ai/04-03-SUMMARY.md` when done
</output>
