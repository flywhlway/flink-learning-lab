-- e09-05 · Paimon vs Iceberg 对比小结(以注释形式呈现决策依据,非可执行脚本)。
--
-- ┌──────────────┬───────────────────────────┬──────────────────────────────┐
-- │   维度        │ Paimon                     │ Iceberg                       │
-- ├──────────────┼───────────────────────────┼──────────────────────────────┤
-- │ 更新模型      │ 原生 LSM 主键表,          │ 仅追加 + delete 文件(v2)     │
-- │              │ 流式 upsert 是一等公民      │ 更新相对更重                  │
-- │ 流读能力      │ changelog-producer 内建,   │ 增量读支持,但 changelog 语义 │
-- │              │ 天然适配 Flink 流式消费      │ 需要额外设计(如 CDC 表)       │
-- │ 与 Flink 关系 │ 阿里出品,与 Flink 生态      │ Netflix 出品,多引擎中立更强   │
-- │              │ 演进关系最紧密              │ (Spark/Trino/Flink 均一等公民) │
-- │ 小文件治理    │ compaction 内建且可配自动   │ 依赖定期 rewrite_data_files    │
-- │ 典型选型      │ 强流式写入+近实时分析、      │ 多引擎共享的中立湖仓、          │
-- │              │ Flink 为核心计算引擎的团队   │ 已有 Spark/Trino 混合栈的团队   │
-- └──────────────┴───────────────────────────┴──────────────────────────────┘
--
-- 本仓库选型:以 Flink 为核心计算引擎、强调流式 upsert 与近实时分析 ⇒ 默认 Paimon
-- (00-03 主线);Iceberg 作为多引擎中立方案的对照纳入(04),便于团队技术选型评审。
SELECT '参见本文件顶部注释对比表' AS note;
